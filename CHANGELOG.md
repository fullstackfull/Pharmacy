# سجل الإصدارات — Pharmacy (منصة 6Valley)

توثيق تفصيلي لكل إصدار على فرع التطوير `claude/project-development-ctyfhz` (‏Pull Request #1).
الأحدث في الأعلى. *(ملاحظة: وسوم Git محجوبة على بوّابة الشبكة في بيئة العمل هذه، لذا هذا الملف هو
سجلّ الإصدارات المرجعي — كل إصدار مربوط بدفعاته المرفوعة على الفرع.)*

القواعد الملتزَم بها في كل إصدار: تغييرات قاعدة بيانات **إضافية فقط** · توافق رجعي مع الـ API
وتطبيقات Flutter · إصلاح السبب الجذري · اختبارات انحدار.

*(اعتباراً من v1.18 صارت هناك **بيئة تشغيل محلية حقيقية**، فأصبح التحقّق ينقسم إلى:
IMPLEMENTED / TESTED / RUNTIME VERIFIED / VISUALLY VERIFIED — كما طلبتَ التمييز صراحةً.)*

---

## v4.2.2 — «بانرات الثيم كما تُعرض»: شاشة تُطابق تخطيط الثيم + سياق واضح لكل بانر

بناءً على الملاحظة: «ليش حطيتهم فوق بعض؟ كيف أعرف هذي الصورة Banner Mosaic؟».

- **شاشة جديدة** `admin/banner/theme-layout` (زر «بانرات الثيم كما تُعرض» بجانب دليل المواضع):
  كل قسم بانرات في الثيم مرسوم **بترتيبه الفعلي على الواجهة** — الموزاييك بشبكته الحقيقية
  (بلاطة كبيرة 2×2، عريضة، طويلة، صغيرة، كلٌّ بحجمها وعليها تسمية حجمها)، والهيرو بسلايداته
  المرقّمة، والشبكات بعدد أعمدتها. كل صورة تحمل رقم بانرها ورابط «تعديل البانر» ورابط
  «افتح هذا القسم في المحرر».
- **قائمة البانرات صارت ناطقة:** بدل شارة عامة، كل صف يعرض مكانه بدقة:
  «Banner mosaic — الرئيسية (مسودة) — بلاطة عريضة» أو «Hero banner — سلايد 2».
- **التحقق:** الشاشة الجديدة مصوّرة حيّاً بموزاييك من خمس بلاطات بأحجام مختلفة + هيرو
  بسلايدين — صفر أخطاء كونسول · 795 اختبار خضراء · VISUALLY VERIFIED.

## v4.2.1 — إغلاق فجوتين أُبلغ عنهما: «الفوتر لا يعمل» وصور mosaic الغائبة عن شاشة البانرات

- **الفوتر:** قسم «أعمدة الفوتر» بلا أي عمود ظاهر كان يستبدل الفوتر المدمج بهيكل شبه فارغ —
  يبدو كأن الفوتر تعطّل. الآن لا تستبدل أقسامُ الفوتر الفوترَ المدمج إلا إذا كان فيها محتوى
  فعلي (عمود واحد ظاهر على الأقل / نشرة / HTML غير فارغ)، ومعاينة المحرر في صفحة الفوتر تهبط
  مباشرة على الفوتر بدل مطالبتك بالتمرير.
- **مسح رجعي للبانرات:** خطاف التسجيل كان يعمل فقط عند حفظ البلوك — بلاطات mosaic المضافة
  قبل الميزة لا تصل لشاشة البانرات أبداً. الآن يجري مسح لكل بلوكات النسخة عند فتح المحرر
  وعند النشر، فتُسجَّل البلاطات القديمة تلقائياً (مُثبَت حياً: بلاطة قديمة سُجّلت بمجرد فتح
  المحرر).
- **مطابقة الروابط صارت غير حساسة للمضيف** (www / تغيّر APP_URL / https) وتقبل شكلَي مسار
  التخزين — فلا تفشل المزامنة بصمت.
- **ترابط ١٠٠٪:** حذف البانر من شاشة البانرات يخفي بطاقته من الثيم أيضاً (كان يعود للصورة
  المنسوخة القديمة).
- **التحقق:** +2 اختبارات (المجموع 795) · إعادة تمثيل كاملة لسيناريو المستخدم عبر واجهات
  الأدمن الحقيقية: mosaic ببلاطتين مرفوعتين ظهرتا في شاشة البانرات فوراً (banner_id 6 و7)،
  وأقسام فوتر فارغة أبقت الفوتر المدمج · VISUALLY VERIFIED.

## v4.2 — الهيدر والفوتر من المحرر + ربط ذكي مع شاشة البانرات

معالجة ثلاث فجوات أُبلغ عنها: «الفوتر لا يعدَّل، الهيدر لا يعدَّل، ولا ربط بين بانرات المحرر
وشاشة البانرات».

- **الهيدر يعدَّل:** أقسام صفحة header في المحرر (شريط الإعلان) تُعرض الآن فوق هيدر المتجر —
  نص ورابط وزر إغلاق يتذكّر الإغلاق.
- **الفوتر يعدَّل:** أقسام صفحة footer (أعمدة روابط/نص/تواصل/سوشال/تطبيقات + نشرة بريدية +
  HTML) تستبدل الفوتر المدمج عند نشرها — بنفس عقد replace-not-stack للصفحة الرئيسية، وسطر
  الحقوق يُلحق تلقائياً، وأي عطل في قسم يسجَّل ويعود الفوتر المدمج.
- **الربط الذكي:** حقل «بانر مربوط» في بلوكات البانر الأربعة يعرض صف البانر حيّاً (تعديل
  شاشة البانرات يصل للمتجر فوراً، وإيقاف النشر يخفي البطاقة)؛ الصورة المرفوعة داخل المحرر
  تُسجَّل تلقائياً كبانر «Theme Banner» وتُربط؛ وشاشة البانرات تعرض شارة «معروض عبر الثيم»
  مع رابط للمحرر.
- **التحقق:** +7 اختبارات (ThemeBannerLinkTest) والمجموع 793 · تحقق حي في Chromium: شريط
  الإعلان والفوتر المركّب على الواجهة، البانر المولَّد من المحرر ظهر في الشاشة مع الشارة،
  ثم أُعيدت بيئة staging لوضعها المحايد · VISUALLY VERIFIED.

## v4.1 — ثيم واحد بهوية أنظف + دليل ثيم شامل

بناءً على طلب صريح: «ثيم واحد فقط، حدّثه واجعله أجمل، وأزل غموض الثيم وإعداداته».

- **ثيم واحد نهائياً:** حُذف `theme_aster` ‏(26MB و246 قالب Blade) وكل فروع
  `theme_fashion` الميتة (~60 موقع كود)، وصار `theme_root_path()` ثابتاً يرجع `default`.
  أُزيل مثبّت الثيمات القديم بالكامل (رفع zip داخل `resources/` + نداء ترخيص خارجي +
  تبديل `WEB_THEME`)، والرابط القديم `admin/system-setup/theme/setup` يحوّل تلقائياً إلى
  واجهة الإدارة الواحدة `admin/theme` (الإدارة + المحرر + الإعدادات).
- **تحديث بصري بروح Nice One:** طبقة `_storefront-refresh.scss` فوق نظام Kohl —
  خلفية بيضاء نظيفة، بطاقات بحدود شعرية واستدارة 16px مع رفع خفيف عند المرور، بحث
  وأزرار Pill بلون العلامة (`--web-primary`)، شارات خصم Pill، دوائر فئات بتظليل ناعم،
  أسهم كاروسيل دائرية، شعارات ماركات رمادية تتلون عند المرور، إيقاع مسافات أوسع.
- **دليل الثيم:** ‏`docs/theme-guide.md` (عربي + ملخص إنكليزي) — البنية، سير عمل
  النشر/التراجع، المحرر وأقسامه، علاقة البانرات، وأين يُكتب أي ستايل جديد.
- **التحقق:** ‏786 اختبار PHPUnit خضراء · كل قوالب Blade تُتَرجم · 12 صفحة أدمن معدّلة
  تعرض نظيفة في Chromium · بطارية بطاقات المتجر: LTR/RTL × 3 مقاسات × (الرئيسية،
  المنتجات، تفاصيل المنتج) = صفر فيض أفقي وصفر أخطاء كونسول · VISUALLY VERIFIED.

## v4.0 — نظام تصميم Kohl مكتملًا + دورة حياة طلبات POS

> *(هذا الإصدار وما بعده على فرع `claude/changes-not-visible-ednbw4`.)*

تحوّل واجهات شامل أُنجز على دفعات صغيرة متحقَّق منها، بلا حذف أي وظيفة أو صلاحية أو مسار API:

- **ترحيل الجداول مكتمل ومُجرَد آليًا:** كل قوائم لوحتي الأدمن والبائع على مكوّنات Kohl
  (`data-view`/`k-table`/`k-badge`/`k-stat`/`k-empty`) — الطلبات، المنتجات، العملاء، البائعون،
  الاسترجاعات، التقييمات، المعاملات، التقارير، المندوبون، marketplace بأكملها (24 شاشة)،
  الإعدادات والوسائط وإدارة النظام، أجزاء نماذج توليفات SKU، وحتى الوسوم المنقسمة على سطرين
  التي أفلتت من الجرد الأول. جردان (سطري + متعدد الأسطر) يثبتان **صفر جداول Bootstrap متبقية**
  خارج المقاصد (جداول الإجماليات، سلة POS، تخطيطات الطباعة/PDF/الإيميل).
- **POS على دورة حياة الطلب الكاملة (بطلب صريح من المالك):** خيار «طريقة التسليم» عند الدفع —
  بيع فوري (السلوك السابق حرفيًا: مُسلَّم/مدفوع) أو **توصيل للمنزل**: يُنشأ الطلب «في انتظار»،
  الكاش يُخزَّن `cash_on_delivery`/غير مدفوع ويُسوَّى عبر آلية الحالات القياسية (مخزون، محافظ،
  نقد بعهدة المندوب، نقاط، أحداث) بلا أي منطق حالات جديد. صفحتا تفاصيل POS تحملان عدّة الطلب
  العادي كاملة (قائمة الحالة + مفتاح الدفع + تعيين المندوب). طلب التوصيل يرفض عميل Walk-In،
  يحفظ آخر عنوان شحن، ويسكّ رمز تحقق تسليم كطلبات الويب.
- **تزامن الويب/API:** endpoint تطبيق البائع `v3/seller/pos/place-order` يقبل `fulfillment`
  بالعقد نفسه (إضافة اختيارية غير كاسرة)، وطلبات توصيل POS تظهر وتُسلَّم عبر API تطبيق المندوب
  (تحقّق حي: القائمة → خرج للتوصيل → سُلِّم → تسوية COD صحيحة).
- **RTL + موبايل:** جولة تحقق على 17 شاشة رئيسية بمقاسي 1440/390 بكسل بعد تبديل الجلسة للعربية
  فعليًا — 59/59: صفر فيض أفقي، اتجاه صحيح، كونسول نظيف؛ مع عزل bidi لعدّاد توست المخزون.
- **تعريب كامل للمفاتيح الجديدة:** 143 مفتاحًا مسكوكًا تُرجمت للعربية بمصطلحات المتجر القائمة.
- **إصلاحات جذرية رافقت الدفعات:** مودال `#paymentModal` الوهمي في POS (TypeError مع كل بيع)،
  التهيئة المزدوجة لسلايدر معرض المنتجات في `common-script.js`، Enter لا يُرسل بحث الطلبات،
  ذيول Chart.js الميتة، مسارات `asset()` المكسورة، ومسار صورة بمسافة (404 دائم).
- **التحقق:** كل دفعة تحقّقت في Chromium حقيقي ضد قاعدة staging المستعادة (شراء COD فعلي، مشي
  حالات كامل، موافقة استرجاع عبر المودال، بيع POS كامل مع تعليق واستئناف) + ‏750 اختبار PHPUnit
  خضراء قبل كل دفعة، وبطارية سويتات متحقّقة (48 سويت متراكمة في `tests/browser/`).

## v3.26 — بوّابة دفع جديدة: Paymera eGate

إضافة **Paymera eGate** كبوّابة دفع مدمجة تظهر عند الدفع مثل Stripe (ويب + تطبيق Flutter)، غير كاسرة
للبوّابات القائمة ولا لعقد الـ API. الأدمن يلصق **معرّف التيرمنال + اسم المستخدم + التوكن** (مصادقة Basic)
ويفعّلها.

- **الآلية:** البوّابة في 6Valley = مُتحكّم يستخدم سِمة `Processor` ومفتاحه النصّي مُسجَّل في عدّة قوائم + صفّ
  `addon_settings`. أضفتُ `paymera` إلى: خريطة `Payment::generate_link`، وقائمتَي
  `GlobalConstant::DEFAULT_PAYMENT_GATEWAYS` و`Helpers::getDefaultPaymentGateways` (مصدر ظهور البوّابة في
  الأدمن وعند الدفع للويب+التطبيق)، وخريطة العملات المدعومة (`SYP`/`USD`)، وقواعد التحقّق، ومسارات
  `payment/paymera/{pay,callback}`. **إضافة فقط** بلا إعادة ترتيب (سلامة تطبيق Flutter).
- **المُتحكّم** `PaymeraController` (على منوال Paystack): `index` ينشئ الدفع عبر `POST /api/create-payment`
  (مصادقة Basic من الخادم فقط) بمبلغ **صحيح بلا كسور** ويحوّل العميل لصفحة الدفع المستضافة؛ `callback` يقرأ
  `GET /api/get-payment-status/{id}` (مصدر الحقيقة) ويعلّم الطلب مدفوعًا **فقط عند الحالة A** حافظًا رقم
  العملية `rrn`؛ `F/C` فشل و`P` معلّق يبقى مفتوحًا؛ **مانع تكرار** للطلب المدفوع. يطلق نفس
  `success_hook`/`failure_hook` كبقيّة البوّابات فيخدم الطلب/المحفظة/فرق التعديل.
- **الإعداد:** صفّ `addon_settings` واحد (مبذور مُعطَّلًا بحقول فارغة) تُصيّره شاشة طرق الدفع القائمة حقول
  إدخال تلقائيًا — بلا واجهة جديدة. **تفصيل حسّاس:** الحفظ يخزّن `validated()` فقط، فأيّ حقل بلا قاعدة تحقّق
  يُسقَط؛ لذا فرع قواعد `paymera` (terminal_id/username/token مطلوبة) هو ما يجعل البيانات تُحفَظ فعلًا
  (مثبَّت باختبار).
- **العملة:** eGate يستنتج العملة من التيرمنال، فلا تُرسَل عملة والمبلغ عدد صحيح — دقيق لِـ SYP (بلا وحدة
  صغرى). يجب تفعيل عملة SYP في المتجر.

بوّابة/مسارات جديدة فقط · هجرة بذر إضافية محميّة (`firstOrCreate`، لا تمسح البيانات عند إعادة التشغيل) ·
+11 اختبارًا · المجموع **705** (كان 694) · **مُتحقَّق تشغيليًا** على MariaDB: البذر يُنشئ الصفّ، `paymera` في
القائمتين وقائمة الأدمن، مخفيّ عند الدفع وهو مُعطَّل، وعند تفعيله ببيانات اختبار يظهر في استعلام بوّابات
الدفع (نفس المصدر للويب والتطبيق) ثمّ أُعيد لحالته المبذورة. التفصيل في
`docs/audit/paymera-gateway-integration.md` · RUNTIME VERIFIED.

---

## v3.25 — تكامل شركة شحن اختيارية: Delivery Syria

إضافة **Delivery Syria** كخيار شركة شحن **إضافي واختياري** — مُعطَّل حتى يُفعّله الأدمن ويلصق التوكن. لا يمسّ أيّ
شحن قائم ولا عقود الـ API / تطبيقات Flutter (مسارات جديدة فقط، ولا تعديل على أيّ مسار قائم).

- **الإعداد** (`admin/third-party/delivery-syria` ضمن «إعدادات أخرى»): صفّ واحد `delivery_syria` في
  `business_settings` يحمل مفتاح التفعيل + السرّين + الإعدادات الافتراضية للطرد، يُقرأ/يُكتب عبر
  `DeliverySyriaConfigService`. السرّان **مشفَّران عند التخزين** (`Crypt`) خلافًا لعرف المشروع (النصّي) لأنّ وثيقة
  الشركة تشترط عدم كشف السرّ؛ ولا يُعادان أبدًا إلى المتصفّح (فارغ = إبقاء الحالي).
- **الصادر** (`DeliverySyriaClient`): تحقّق ومزامنة أسعار المحافظات (`/api/secret/cheack` → جدول
  `delivery_syria_governorate_rates` بمفتاح `code`، وإعادة المزامنة تُحدّث مكانها)؛ إنشاء طرد
  (`/api/parcel/create-with-security` بترويسة `X-Platform: pharmacyesyria` + Bearer) من زرّ «إرسال إلى
  دليفري سوريا» في صفحة الطلب، يُعيد استخدام حقول الطلب القائمة
  (`third_party_delivery`/`delivery_service_name`/`third_party_delivery_tracking_id`)؛ **مُتحقَّق من
  `wallet_status`** (`deducted`/`pending_payment`) لا الاكتفاء بـ`success`؛ **مانع للتكرار** عبر `invoice_no`
  = رقم الطلب.
- **الوارد** (`POST /api/delivery-syria/orders/update-status`): وسيط `deliverysyria_auth` (ترويسة
  `X-Platform: deliverysyria` + توكن Bearer، مقارنة `hash_equals`، يفشل مغلقًا — نمط IPN لا Passport).
  يُسجّل حالة الشركة في السجلّ وتاريخ الطلب، ويعكسها في `order_status` **فقط** للحالات الوسيطة عديمة الأثر.
- **الحدّ الآمن للحالة:** `delivered`/`canceled` **تُسجَّل فقط** ولا تُطبَّق أبدًا من نداء خارجي (لأنها تُنضج
  الأرباح/العمولة أو تُرجِع المخزون)، والطلب المنتهي لا يُبعَث. الإنهاء المالي يبقى لمسار المنصّة نفسه.
- **حدّ النطاق الصريح:** لم تُوصَل تكلفة الوزن×المحافظة إلى دفع المتجر/التطبيق (لا عمود وزن للمنتج ولا التقاط
  محافظة — نفس سبب خمول طبقة مناطق الشحن)؛ وزن الإرسال يُدخله الأدمن والشركة تحسب التكلفة المُلزِمة.

هجرتان إضافيتان (محميّتان، `down()` عامل) · +31 اختبارًا · المجموع **694** (كان 663) · **مُتحقَّق تشغيليًا** على
MariaDB: الهجرتان تُطبَّقان، السرّ مُخزَّن مشفّرًا ويُفكّ، الويبهوك 401 لتوكن خاطئ و200 للصحيح، طلب حقيقي انتقل إلى
`out_for_delivery` عند الحالة 6 مع صفّ طرد وتاريخ، والحالة 1 (تم التوصيل) **لم** تنقله إلى delivered، والطلب
المجهول 404. التفصيل في `docs/audit/delivery-syria-integration.md` · RUNTIME VERIFIED.

---

## v3.24 — Phase 3 / المرحلة أ: دخول طاقم البائع وإنفاذ الصلاحيات (الخطوة المؤجَّلة، أُنجزت)

في v3.20 بنيتُ **أساس** الطاقم (أدوار + مفاتيح صلاحيات + طاقم + محلّل `staffCan()` مُختبَر) وأعلنتُ صراحةً أنّ
**الدخول والحارس والإنفاذ خطوة تالية**. هذا الإصدار يُنجزها:

- **الدخول** (`StaffLoginController`، `vendor/staff-auth/login`): يصادق عضو الطاقم ببياناته المُجزّأة ثمّ
  يُسجَّل دخوله **بصفة البائع الأمّ** على حارس `seller` القائم، مع وسم `seller_staff_id` على الجلسة. هذا الخيار
  الواحد هو ما يُبقي التغيير صغيرًا وآمنًا: كلّ مُتحكّمات البائع تُقيّد أصلًا بـ`auth('seller')`، فيدير الطاقم
  متجره **دون أيّ تغيير في المتحكّمات**، ومسار دخول المالك نفسه لم يُمَسّ ولا يضع ذلك المفتاح أبدًا.
- **الإنفاذ** (`SellerStaffAccessMiddleware`، بعد `seller` على كامل مجموعة مسارات البائع): المالك الحقيقي (بلا
  `seller_staff_id`) يمرّ كما هو. لجلسة الطاقم تُشتقّ الصلاحية المطلوبة من رابط المسار وتُفحَص عبر `staffCan()`
  المُختبَر. الخريطة **رفضٌ افتراضيّ** — التنقّل مسموح، والنطاق الأساسيّ يحتاج مفتاحه، وأيّ غير مُخطَّط يُرفَض
  (403) فيفشل مُغلَقًا. جلسة طاقم قديمة/معطوبة (غير موجودة أو غير فعّالة أو لا تطابق البائع الداخل) تُسقَط
  وتُعاد لدخول الطاقم.
- **واجهة المالك**: صفحة إدارة الطاقم تعرض الآن رابط دخول الطاقم للمشاركة (بدل ملاحظة «قريبًا»).

**توافق رجعي:** المالك الحقيقي غير متأثّر بنيويًّا — الوسيط يعيد `$next()` فورًا حين غياب `seller_staff_id`،
ولا مسار مالكٍ يضعه. لا حارس جديد ولا جدول جديد ولا ترحيل — إضافةٌ فوق الأساس القائم.

- IMPLEMENTED: متحكّم دخول/خروج · وسيط إنفاذ بخريطة رابط→صلاحية · مسارات staff-auth · تسجيل الوسيط · شاشة دخول.
- TESTED: +7 اختبارات (`SellerStaffAccessTest`: التنقّل مسموح؛ قراءة=view/كتابة=manage؛ فصل staff.manage عن
  shop_settings.manage؛ كل مفتاح مُخطَّط حقيقيّ؛ غير المُخطَّط يُرفَض). المجموعة **652 ناجحًا، 1 متخطّى**.
- RUNTIME VERIFIED: عبر مكدّس HTTP الحقيقي على MariaDB — طاقم بصلاحية `orders.view` فقط: دخول 302، لوحة 200،
  صفحات الطلبات 200، والعروض والمنتجات **مرفوضة 403**، وبعد الخروج تُعاد اللوحة للدخول (الجلسة نُظِّفت). حُذفت
  بيانات الاختبار.

## v3.23 — Phase 3 / تبنّي مناطق الشحن عند الدفع (اختياري، مُعطَّل افتراضيًا)

مُحرّك مناطق الشحن كان مبنيًّا ومُختبَرًا ومُدارًا من لوحة الأدمن مع معاينة — لكنّ **الدفع الفعلي لم يكن
يحتسب سعر المنطقة بعد**. هذا الإصدار يصل المُحرّك بمسار الدفع على الويب: `ShippingRateService::checkoutCost()`
يُعيد تكلفة الشحن الحالية للمنصّة عند عدم تطابق أي منطقة، وسعر المنطقة عند التطابق، و0 عند بلوغ عتبة
«مجّاني فوق». مفتاح تبديل في لوحة الأدمن (`zone_wise_shipping`، **مُعطَّل افتراضيًا**، مُدقَّق ومُنظَّف من
الكاش) يحكم التفعيل. عند التفعيل يتجاوز `insertIntoCartShipping` تكلفة الطريقة المختارة بسعر المنطقة لعنوان
العميل، ويكتبها على **نفس** صف `CartShipping` — فتُحتسب إجماليّات الطلب وضريبة الشحن والتوصيل المجّاني في
الأسفل تمامًا كما كانت.

**الحدود المُعلَنة بصراحة:** التبنّي على **الويب فقط** — خطوة الشحن في الـ API عديمة الحالة (لا وجهة مربوطة
عندها)، فيبقى الـ API على الشحن بالطرائق (لا كسر للعقد). الوزن = 0 (لا عمود وزن للمنتج)، فيسري وزن-الكيلو
لاحقًا فقط. **توافق رجعي:** مع المفتاح مُعطَّلًا، الدفع والـ API بلا أي تغيير.

- IMPLEMENTED: `checkoutCost()` · مفتاح الأدمن + `toggle` + بطاقة الحالة · وصل مقعد `insertIntoCartShipping`.
- TESTED: +3 اختبارات (`checkoutCost`: رجوع للأساس / سعر المنطقة / مجّاني فوق العتبة). المجموعة **645 ناجحًا، 1 متخطّى**.
- RUNTIME VERIFIED: على MariaDB — دورة المفتاح عبر كاش الإعدادات؛ منطقة سوريا تحتسب أساسها وتُمجّن فوق عتبتها،
  والأردن يرجع لتكلفة الطريقة؛ المقعد الموصول يُعيد تكلفة الطريقة دون تغيير عند إطفاء المفتاح أو غياب العنوان.
  قاعدة البيانات نظيفة بعدها.

## v3.22 — Phase 3 / تبنّي تسعير B2B عند الإضافة للسلة

قوائم أسعار الجملة (مجموعات العملاء) كانت مبنيّةً ومُختبَرةً لكن **السلة لم تكن تستشيرها**. هذا الإصدار يصلها:
`CartManager::customerGroupPrice()` يُستدعى في مساري الإضافة للسلة (المادي والرقمي) بعد تحديد سعر الوحدة/المتغيّر
مباشرة. الزائر، أو العميل المُسجَّل غير المنتمي لأي مجموعة فعّالة، يحصل على السعر الأساس — فالتجزئة بلا تغيير.
السعر بالجملة المُستنتَج يُكتب على `Cart.price` الذي يقرؤه الويب والـ API معًا، فيتدفّق إلى لقطة الطلب والضرائب
والعمولة باتّساق عبر السطحين. تسعير المجموعة **يخفّض السعر فقط**، والأفضل (الأدنى) بين مجموعات العميل يفوز.

- IMPLEMENTED: `customerGroupPrice()` · وصله في `addToCartPhysicalProduct` و`addToCartDigitalProduct`.
- TESTED: +5 اختبارات (زائر/تجزئة → الأساس؛ خصم المجموعة؛ تجاوز سعر ثابت لكل منتج؛ لا يرفع السعر أبدًا).
- RUNTIME VERIFIED: على MariaDB — تجزئة/زائر → 10000؛ مجموعة 15% → 8500؛ تجاوز ثابت → 8000؛ ثمّ الأساس بعد التنظيف.

## v3.21 — Phase 3 / تبنّي تنسيق المدفوعات عند الدفع

قواعد توجيه البوّابات كانت مبنيّةً ومُختبَرةً لكن **قائمة بوّابات الدفع عند الدفع لم تكن تستشيرها**. هذا الإصدار
يصلها: `PaymentRoutingService::applyToModels()` يطبّق قواعد الإخفاء/التفضيل ذاتها على **نماذج** البوّابات التي
تمرّ عليها صفحة الدفع (بحفظ شكلها: `->key_name` و`count()`)، و`checkout_payment()` يستشيره مرّةً من سياق الطلب
(الإجمالي + بلد الوجهة) ويستخدم القائمة المُستنتَجة في بوّابة إتاحة الدفع الرقمي وفي العرض. بلا قاعدة فعّالة تعود
البوّابات بترتيبها الأصلي. دالّة `payment_gateways()` نفسها لم تُمَسّ، فمنفذ الـ API غير متأثّر.

- IMPLEMENTED: `applyToModels()` · استشارته في `WebController::checkout_payment`.
- TESTED: +4 اختبارات (`applyToModels`: بلا قاعدة بلا تغيير / إخفاء نموذج / تفضيل نموذج للمقدّمة / قائمة فارغة).
- RUNTIME VERIFIED: على MariaDB — بوّابات مبذورة + قاعدة: مُخفاة فوق عتبة المبلغ، ظاهرة تحتها، مُقيَّدة بالبلد؛ نُظِّفت.

## v3.20 — Phase 3 / المرحلة أ: أساس طاقم البائع والأدوار

المنصّة بلا مفهوم طاقمٍ للبائع — المتجر يُدار بحساب البائع الوحيد، بلا وصولٍ مُقنَّن لزميل. هذا هو البند
الوحيد في المرحلة الذي لا ينحلّ إلى شريحة إضافية آمنة: حسابُ طاقمٍ فرعيّ **هو** سطح مصادقة جديد (دخول،
حارس، إنفاذ لكل مسار). فبنيتُ **الأساس** بتأنٍّ، والدخول/الحارس/الإنفاذ **خطوة تالية معلَنة** (بالاتفاق مع
المالك) لا مُتسرَّعة. `seller_roles` (دور باسمٍ ومفاتيح صلاحيات) و`seller_staff` (عضوٌ بدورٍ وبيانات اعتماد
مُجزّأة، جاهزة للدخول المؤجَّل). `SellerPermissionService`: كتالوج الصلاحيات (قدرات مُجمَّعة)، و`sanitize()`
(يُسقِط أي مفتاح غير معروف)، والمحلّل `staffCan()` — يردّ «لا» افتراضياً لعضوٍ مُعطَّل أو بلا دور. شاشة بائع
تُعرّف الأدوار وتُدير الفريق، كلُّ صفٍّ مقيَّد بـauth('seller'). كلمات المرور مُجزّأة، وحذف دورٍ يفصله عن
الطاقم لا يُيتّمه، وإسناد دورِ بائعٍ آخر يُرفَض (403). **7 اختبارات · المجموع 633 · RUNTIME VERIFIED**
(دور+عضو مقيَّدان، كلمة سرّ مُجزّأة، المحلّل orders.manage⇐true وfinance.view⇐false).

## v3.19 — Phase 3 / المرحلة هـ: تنسيق المدفوعات

للمنصّة 36 بوّابة دفع (في `addon_settings`) بعلَم تفعيل لكلٍّ، لكن الاختيار كلّه‑أو‑لا‑شيء: لا سبيل
لإخفاء PayPal فوق مبلغ، أو تفضيل بوّابة محلّية للطلبات الداخلية، أو التوجيه حسب المبلغ/الدولة. أضفتُ طبقة
التوجيه. `payment_routing_rules`: قاعدة تستهدف بوّابةً بفعل (إخفاء | تفضيل) بشروط اختيارية (نطاق مبلغ، دولة).
`PaymentRoutingService::resolve()` يأخذ البوّابات المعروضة وسياق الطلب ويعيد القائمة مُرشَّحةً مرتّبة —
المخفيّة تُزال، والمفضَّلة تتقدّم بالأولوية. **غير كاسر:** بلا قاعدة تعود القائمة كما هي، والقاعدة لا تخترع
بوّابة قط، والإخفاء يغلب التفضيل. محرّك+معاينة، وربط الدفع الحيّ خطوة لاحقة (المحلّل هو الوصلة). لا يحرّك
مالاً — يرتّب البوّابات فقط. **8 اختبارات · المجموع 626 · RUNTIME VERIFIED** (قاعدة «إخفاء PayPal فوق 5000»:
طلب 6000 ⇐ [stripe, razor_pay]، وطلب 100 ⇐ PayPal حاضر).

## v3.18 — Phase 3 / المرحلة هـ: حوكمة أسعار الصرف

**قِستُ أولاً فوجدتُ المنصّة تدعم تعدّد العملات أصلاً** — `currencies` فيها `exchange_rate`، وإعداد
`currency_model`، ومساعدات `usd_to_currency`/`currency_to_usd` تحوّل الأسعار. فإعادة بنائها تخالف «لا
تُعِد البناء». الفجوة الحقيقية: **حوكمة الأسعار نفسها** — السعر رقمٌ متغيّر بلا سجلّ لِمَن غيّره ومتى ومِن
ماذا، وبلا تحديث جماعي. أضفتُ `exchange_rate_logs` (سجلّ ملحق: قديم←جديد، المصدر، الفاعل)؛ و
`ExchangeRateService`: `updateRate()` يكتب على `currencies.exchange_rate` القائم ويسجّل الفرق (وتغييرٌ بلا
أثر لا يُسجَّل)، و`bulkUpdate()` يطبّق دفعةً ويعدّ ما تغيّر. **مربوط:** يكتب على نفس الجدول الذي تقرؤه
مساعدات التحويل (وتُفرَّغ ذاكرة الإعداد فيسري فوراً) وعلى سجلّ التدقيق. جدول إضافي فقط، والتحويل دون تغيير.
**6 اختبارات · المجموع 618 · RUNTIME VERIFIED** (BDT ‏84→85.5 بسجلّ، ثم استُعيد إلى 84).

## v3.17 — Phase 3 / المرحلة هـ: تسعير B2B / الجملة

المنصّة بلا فكرة عن *شريحة* العميل — الكل يرى نفس السعر، و`users` بلا مجموعة. أضفتُ ثلاثة جداول:
`customer_groups` (شريحة + خصم افتراضي)، `customer_group_customer` (عضوية دون مساس بجدول users)، و
`customer_group_prices` (تجاوز لكل مجموعة/منتج: سعرٌ ثابت أو خصم). `B2BPricingService` يحلّ السعر.
**غير كاسر بعقده:** `priceFor()` يعيد السعر الأساسي دون تغيير لعميلٍ خارج أي مجموعة، فالتجزئة وأي دفعٍ لم
يتبنّاه يرى سعر اليوم؛ التسعير يخفض فقط، وعبر عدّة مجموعات يفوز الأرخص. **النطاق بصدق:** يشحن محرّك التسعير
وإدارته (متحقَّق بالمعاينة والمحلّل)؛ لا يربطه بالسلّة الحيّة بعد — المحلّل هو الوصلة. **التحقّق الحيّ كشف 500
حقيقياً** (مفتاح `price` غائب عند إرسال خصمٍ فقط) فأُصلح. **7 اختبارات · المجموع 612 · RUNTIME VERIFIED**
(عضو ⇐ 70 من 100 بخصم، غير‑عضو ⇐ 100 أساسي).

## v3.16 — Phase 3 / المرحلة ج: مناطق الشحن

شحن المنصّة ثابت أو حسب التصنيف — رقمٌ واحد، بلا وعيٍ بالوجهة أو الوزن. أضفتُ طبقة تعرفة حسب الوجهة:
`shipping_zones` تطابق دولاً (ومناطق اختيارياً) وتحمل قاعدة (أساس + لكل كغ + مجّاني فوق قيمة).
`ShippingRateService` يحلّ الوجهة إلى منطقتها (المخصّصة تسبق الشاملة، الأولوية تكسر التعادل) ويحسب التكلفة.
**غير كاسر:** `rateFor()` يعيد null حين لا تطابق منطقة، فيرتدّ المنادي إلى الشحن الثابت الحالي. شاشة أدمن +
معاينة تعرفة. **النطاق بصدق:** محرّك التعرفة وإعداده فقط؛ ربطه بالدفع الحيّ خطوة لاحقة، وواجهات الناقلين
الخارجية محكومة بسياسة الشبكة. **8 اختبارات · المجموع 605 · RUNTIME VERIFIED** (سوريا 3كغ/40 ⇐ 11، وفوق
الحدّ ⇐ مجّاني، والأردن ⇐ null ⇐ ارتداد للثابت).

## v3.15 — Phase 3 / المرحلة ج: التنفيذ (التقاط/تغليف/شحن)

الطلب يمشي pending→confirmed→processing→out_for_delivery→delivered، والعمل المستودعي في الوسط —
التقاط، تغليف، تجهيز — بلا تمثيل: «processing» حالةٌ صمّاء واحدة. أضفتُ `order_fulfillments`: تنفيذٌ لكل
طلب (لكل بائع في الطلب المقسّم) بدورة pending→picking→packed→ready→shipped، مربوطٌ اختيارياً بالمستودع
(v3.13). `FulfillmentService` آلة حالة **للأمام فقط**: ترفض الرجوع أو التكرار، تختم packed_at/shipped_at
عند بلوغهما، وتلتقط الناقل والتتبّع عند الشحن؛ تنفيذٌ مفتوح واحد لكل (طلب، بائع). **طبقة عمداً:** تكتب في
جدولها فقط و**لا تمسّ `orders.order_status`** أبداً، فتُثري وسط التدفّق دون تغيير آلة الحالة التي يعتمدها
المتجر والتطبيقات. **6 اختبارات · المجموع 597 · RUNTIME VERIFIED** (طلب حقيقي إلى shipped، وحالة الطلب
بقيت pending طوال الوقت).

## v3.14 — Phase 3 / المرحلة أ: مركز البائع

القطع الموجَّهة للبائع بُنيت واحدةً واحدة — KYC، بطاقة الأداء، السحوبات، سجلّ SLA — كلٌّ على شاشته، بلا
مكانٍ واحد يرى فيه البائع موقفه. أضفتُ القمرة. `SellerCenterService` **لا يملك بيانات**: يفوّض إلى الخدمات
المبنيّة (توثيق، بطاقة أداء، دفتر أستاذ، SLA)، فكل رقمٍ هو نفسه الذي تُظهره شاشته المخصّصة ولا يمكن أن
يختلف عنها. صفحة بأربع بطاقات (توثيق، أداء+مقاييس، مالية: القابل للسحب/المعلّق/آخر سحب، مستوى الخدمة:
المخالفات المفتوحة) كلٌّ يربط لتفصيله. **للقراءة فقط بلا جدول ولا ترحيل** — كل خدمة فرعية تحمي جداولها فتنحلّ
لقيمٍ افتراضية لا خطأ. **2 اختبار · المجموع 591 · RUNTIME VERIFIED** (أربع بطاقات لبائع حقيقي، صفر استثناء).

## v3.13 — Phase 3 / المرحلة ج: المستودعات المتعدّدة

المنصّة بلا فكرة عن *أين* يقع المخزون — رقمٌ واحد `current_stock` فقط. أضفتُ سجلّ مواقع **كطبقة فوق**
`current_stock` لا بديلاً عنه: `current_stock` يبقى مصدر الحقيقة للبيع، والتوزيع يقسّم نفس الكمية عبر
المواقع مع متبقٍّ «غير مُوزَّع» (`current_stock − Σ مُوزَّع`). `warehouses` + `warehouse_stock`، و
`WarehouseService`: `place()` يوزّع من المتبقّي (مُقيَّداً به)، `remove()` يعيده، `transfer()` ينقل بين
مستودعين بقفلٍ مزدوج مرتّب. **كل عملية تحفظ الإجمالي القابل للبيع**، فلا شيء منها يمسّ مسار الطلب. **7
اختبارات · المجموع 589 · RUNTIME VERIFIED** (وضع 100 ونقل 40 لمنتج حقيقي: 60/40، والمخزون بقي 500).

## v3.12 — Phase 3 / المرحلة ج: تتبّع الدفعات والصلاحية

المنتج له رقم مخزون واحد بلا فكرة عن *متى* تنتهي صلاحيته — لا شيء يجيب «ما الذي ينتهي هذا الشهر؟»،
والمخزون المنتهي يُشطب (إن شُطب) بتحرير حقل المخزون بلا سبب. أضفتُ `product_batches`: دفعة تسجّل أن كميةً
من منتجٍ تحمل صلاحيةً معيّنة. `BatchService`: إضافة، `expiringSoon`، `expired`، `writeOff`. شاشة أدمن:
تسجيل دفعة، عدّادا «ينتهي قريباً/منتهٍ»، تصفية، وشطب. **طبقة تتبّع عمداً:** الدفعات منفصلة عن `current_stock`
(يبقى مصدر الحقيقة للبيع)، فتسجيلها لا يغيّر سلوك البيع. **مربوط:** الشطب — الفعل الوحيد الذي يغيّر الكمية —
يُفوَّض إلى `InventoryService::adjust()` بسبب `expiry`، فيصير حركةً مُعلَّلة بقفل صف في نفس السجلّ، لا مساراً
صامتاً ثانياً؛ وشطبٌ يجعل المخزون سالباً يُرفَض وتبقى الدفعة. **7 اختبارات · المجموع 582 · RUNTIME VERIFIED**
(شطب دفعة 20 نقل منتجاً 500→480 بحركة expiry، والدفعة صارت مستنفَدة).

## v3.11 — Phase 3 / المرحلة هـ: التسوية المالية

المرحلة ب بنت نواةً مالية بثوابت (الرصيد الجاري = مجموع القيود؛ التسوية = القيود التي طالبت بها) لكن لا شيء
يتحقّق منها؛ تغييرٌ يفسد رصيداً لاحقاً يمرّ دون ملاحظة حتى الدفع. `ReconciliationService` فحوص **للقراءة فقط**
على نفس جداول المرحلة ب: (1) آخر `balance_after` لكل بائع = مجموع دائن−مدين المستقل؛ (2) إجمالي لقطات العمولة
= إجمالي رسوم العمولة في الدفتر؛ (3) قيود كل تسوية تساوي `net_amount` المخزّن وعددها يطابق. شاشة تقرير: أخضر
عند التطابق، وتفصيل (مرجع/متوقَّع/فعلي/فرق) عند الفرق. **بلا كتابة، بلا ترحيل، بلا تغيير مخطط** — كل مقارنة
مُشتقّة في PHP فتعمل على MariaDB وSQLite سواء. **5 اختبارات · المجموع 575 · RUNTIME VERIFIED** (كشف رصيداً
مفسوداً عمداً على المحرّك الحقيقي: متوقَّع 40 مقابل 999).

## v3.10 — Phase 3 / المرحلة ج: لوجستيات الإرجاع (RMA)

كان هناك استرداد مالي (عكس العمولة من المرحلة ب) لكن لا شيء ماديّ: إرجاعٌ مقبول يترك المخزون القابل للبيع
ناقصاً وحدةً للأبد — لا ترخيص، لا تتبّع، **ولا إعادة للمخزون**. أضفتُ النصف الماديّ. `return_shipments`: RMA
يمشي مُرخَّص → قيد النقل → مستلَم، وعند الاستلام إمّا **أُعيد للمخزون** أو **رُفض** (تالف: يُستلَم دون إعادة).
`ReturnLogisticsService` يملك آلة الحالة والإعادة: استلام إرجاعٍ قابل للإعادة يزيد `current_stock` **بقفل صف**
ويسجّل حركة `return` — النوع الذي حجزتُه في عمل المخزون (v3.8) لهذا بالضبط — فيظهر في نفس السجلّ بمرجعٍ للـRMA.
يربط `refund_request_id` دون تكرار عكس العمولة. **7 اختبارات · المجموع 570 · RUNTIME VERIFIED** (إرجاع حقيقي:
مُرخَّص→نقل→استلام، المنتج 500 → 504 بحركة return).

## v3.9 — Phase 3 / المرحلة هـ: اتفاقية مستوى خدمة البائع (SLA)

بطاقة الأداء تحسب المقاييس لكن عتباتها ثابتة وهي لحظية: لا سياسة يضبطها المشغّل، ولا سجلّ *متى* تجاوز
البائع الحدّ و*كم دام*. حوّلتُها إلى شيء قابل للإنفاذ. `SlaService` **يعيد استخدام** `SellerScorecardService`
للأرقام (مصدرٌ واحد للحقيقة، فلا تختلف الـSLA وبطاقة الأداء أبداً على معدّل إلغاء بائع) ويضيف عتبات قابلة
للضبط: معدّلات الإلغاء/الإرجاع/الاسترداد سقوف، والتقييم حدٌّ أدنى لا يُطبَّق إلا بعد خمس مراجعات.

`breachesFor()` **دالّة صرفة** (قابلة للاختبار بلا قاعدة بيانات)؛ و`evaluate()` يسحب البطاقة ويوفّق
السجلّ: يفتح صفّاً لكل حدٍّ مُتجاوَز حديثاً، ويُغلق أي مخالفة تعافى منها البائع — صفٌّ مفتوح واحد على الأكثر
لكل (بائع، مقياس). كل فتح يكتب سطر تدقيق. شاشة أدمن: ضبط العتبات الأربع، تقييم كل البائعين، وقراءة سجلّ
المخالفات (المفتوح أولاً) مع عدّاد بارز. **7 اختبارات · المجموع 563 · RUNTIME VERIFIED** (حُفظت العتبات،
جرى التقييم، عُرضت مخالفة).

## v3.8 — Phase 3 / المرحلة ج: تسويات المخزون وسجلّ الحركة

`products.current_stock` رقمٌ واحد بلا تاريخ، يُكتب من ~10 مواضع، وعلى نموذج المنتج يتغيّر **بلا سبب ولا
أثر** — فالجرد الخاطئ والكسر والسرقة تبدو كالبيع. أضفتُ `stock_movements` سجلّاً ملحقاً فقط (التغيّر
المُوقَّع، الرصيد الناتج، السبب، من). `InventoryService::adjust()` بديلٌ مُعلَّل لتحرير حقل المخزون: يغيّره
**داخل معاملة بقفل صف**، يرفض جعله سالباً أو صفراً، ويكتب حركة وسطر تدقيق. `record()` بدائيّة **لا ترمي**
تناديها المسارات الأخرى.

**مربوط:** مسار استلام أوامر الشراء صار ينادي `record()` عند كل استلام، فيظهر الاستلام في نفس السجلّ مع
مرجعٍ لأمر الشراء — مسارَا تزويد، سجلّ مخزون واحد قابل للتدقيق. **النطاق بصدق:** لا يشمل بعدُ مسارات البيع
العشرة (تمسّ مسار الطلب الساخن) — أنواع بيع/إرجاع/نقل موجودة لتُوصَل لاحقاً بلا تغيير مخطط. **8 اختبارات ·
المجموع 556 · RUNTIME VERIFIED** (تسوية ‎+10 نقلت منتجاً 500→510، ثم استلام أمر شراء سجّل حركة →518).

## v3.7 — Phase 3 / المرحلة ج: المشتريات — المورّدون وأوامر الشراء والاستلام إلى المخزون

المنصّة نمذجت جانب البيع بعمق وبلا جانب تزويد: لا مورّد، لا أمر شراء، لا استلام — المخزون يهبط فقط مع
البيع. فتحتُ المرحلة ج بالنصف الناقص. `suppliers` (‏seller_id فارغ = المنصّة، وقيمة = بائع بعينه)،
و`purchase_orders` + `purchase_order_items` بحالة تمشي مسوّدة → مطلوب → مستلَم جزئياً → مستلَم (أو ملغى).
`PurchaseOrderService` يملك آلة الحالة والسلوك الذي يجعله جانب التزويد: **الاستلام يزيد
`products.current_stock`** — نفس المخزون الذي يبيعه المتجر ويخصمه مسار الطلب، **داخل معاملة بقفل صف** فلا
يضيع تحديثٌ عند استلامين متزامنين. حالة الرأس **مُشتقّة** من البنود فلا تخالفها، والبند لا يُستلَم فوق ما
طُلب. تدفّق أدمن كامل: مورّدون، إنشاء أمر ببنود، تأكيد، استلام لكل بند أو للكل، إلغاء. ثلاثة جداول إضافية
فقط. **9 اختبارات · المجموع 548 · RUNTIME VERIFIED** (أمر لمنتج حقيقي: 500 → 525 عند الاستلام).

## v3.6 — Phase 3 / المرحلة أ: بطاقة أداء البائع

المنصّة تقيس **أرباح** البائع و**ضرائبه**، لكن لا شيء عن **جودته**: لا تنفيذ ولا إلغاءات ولا إرجاعات
ولا استردادات ولا تقييم ولا مخالفات إشراف. أضفتُ ذلك **كنموذج قراءة فوق بيانات المرحلة نفسها — بلا
جدول جديد**. كل رقم مربوط بعمود حقيقي مقيس، لا مُختلَق: الطلبات بمفردات `order_status` الفعلية
(`delivered`/`canceled`/`returned`/`failed`، مع `seller_is='seller'` كي لا تُحسب طلبات المتجر الداخلية
على البائع)، ومعدّل الاسترداد من `order_details.refund_request`، والتقييم من مراجعات فعّالة منسوبة
للبائع عبر `products.user_id`، والمخالفات من سجلّ إشراف المنتجات، وحالة التوثيق من خدمة KYC.

`deriveTier()` **دالّة صرفة** تُخرج طبقة صحّة (جيّد/قيد المراقبة/معرَّض للخطر)، فتستهلكها لاحقاً بنود
الـSLA والنزاعات ونفس الطبقة تظهر هنا. بائعٌ بلا نشاط = **«جديد»** لا يُحاكَم، والتقييم لا يؤثّر في
الطبقة قبل خمس مراجعات فلا تُسقِط مراجعةٌ غاضبةٌ واحدةٌ متجراً. شاشة أدمن (كل البائعين، تُحسب لصفحة
واحدة في كل طلب) وشاشة بائع (أرقامه هو). الحدّ الصريح الوحيد: لا فلتر خادمي حسب الطبقة دون تخزينها —
مذكور لا مُخفى. للقراءة فقط: صفر كتابة، صفر ترحيل. **12 اختباراً · المجموع 539 · RUNTIME VERIFIED.**

## v3.5 — Phase 3 / المرحلة أ: توثيق البائعين (KYC)

كانت هوية البائع حالةً واحدةً خشنة على الحساب، بلا مكان لوثيقة هوية أو سجلّ تجاري، بلا حالة مراجعة لكل
وثيقة، بلا مُراجِع، بلا انتهاء، وبلا طريقة لجعل «مُوثَّق» شرطاً لأي شيء. أضفتُ `seller_verification_documents`
— صفٌّ لكل وثيقة بدورة حياتها (قيد المراجعة → موافقة/رفض، والموافقة قد تحمل انتهاءً) — وحالة البائع
الإجمالية **مُشتقّة** من صفوفه مقابل مجموعة مطلوبة قابلة للضبط؛ عمود `status` للحساب لا يُلمَس.

الربط الحقيقي هو **بوّابة السحب**: `PayoutService::requestPayout` صار يسأل `isPayoutEligible()` قبل أن
يحجز شيئاً — حارسٌ داخل الدالّة القائمة، بلا دالّة مُضافة أو محذوفة. والبوّابة **مُطفأة افتراضياً**
(`require_kyc_for_payout`)، فكل تثبيت قائم يسحب كما اليوم؛ حين تُفعَّل يُمنَع غير المُوثَّق. تحقّقٌ حيّ من
طرف إلى طرف: مع البوّابة مُفعَّلة وبائعٍ يملك هويةً موافَقاً عليها والسجلّ التجاري ناقص، يعود طلب السحب
`kyc_verification_required`؛ بعد الموافقة على السجلّ يمرّ إلى فحص الرصيد المعتاد. شاشتا أدمن (طابور
المراجعة + الإعدادات) وبائع (رفع pdf/صورة عبر الرافع المُقوّى، الامتداد على قائمة بيضاء خادمية). ترحيل
إضافي فقط، بلا مساس بأي ترحيل أصلي. **17 اختباراً · المجموع 527 · RUNTIME VERIFIED.**

## v3.4 — Phase 3 / المرحلة أ: حوكمة التصنيفات

كل قرار تشغيلي كان عالمياً: `refund_day_limit` رقمٌ واحد للمتجر كلّه، بلا فئة ضريبية لكل تصنيف، بلا سمات
مطلوبة، بلا فرض إشراف. أضفتُ `category_governance` — صفٌّ لكل تصنيف، **كل حقل قابل للإفراغ يرث العالمي**،
فتثبيتٌ لا يضبط شيئاً لا يغيّر سلوكاً. نافذة إرجاع (null=يرث، **0=لا إرجاع**، متمايزان)، سمات مطلوبة (JSON)،
فئة ضريبية، فرض إشراف، وقيد شحن. **مربوط لا معزول:** فحص أهلية الاسترداد في `RestAPI/v1/OrderController`
لم يعد يقرأ الحدّ العالمي مباشرة — يشتقّ `category_id` للسطر ويسأل الخدمة؛ التصنيفات غير المحوكَمة تعود لنفس
الرقم العالمي فعقد الـAPI وتطبيقات Flutter دون تغيير. ترحيل إضافي فقط. **13 اختباراً · المجموع 510 · RUNTIME VERIFIED.**

## v3.3 — Phase 3: النسيج الرابط — سجلّ تدقيق، محرّك موافقات مزدوجة، وإشراف المنتجات

قبل بناء المزيد بنيتُ ما تشترك فيه المراحل: **سجلّ تدقيق موحّد** (`audit_logs`) ملحقٌ فقط — من فعل ماذا،
بأي سجلّ، ومتى — مربوطٌ داخل النواة المالية؛ **محرّك موافقات maker-checker** قابل لإعادة الاستخدام (الصانع
≠ الفاحص، قرارٌ واحد لكل فاعل عبر مفتاح فريد) يستعمله مسار السحب للمبالغ الكبيرة؛ و**إشراف منتجات** بتاريخ
وأسباب وإجراءات جماعية، يُظهر للبائع «لماذا رُفض» ويزامن `request_status`. مركز تدقيق للقراءة فقط، مُصفّى
خادمياً. كلّها مربوطة بأفعال حقيقية تكتب في السجلّ. **RUNTIME VERIFIED** (رفض الإدارة نفسها موافقةً ثانيةً
عبر الواجهة، ومناعة اللقطة بعد تغيير القاعدة).

## v3.2 — Phase 3 / المرحلة ب: النواة المالية للسوق

بنيتُ العمود المالي كاملاً ومربوطاً: **محرّك عمولات** بقواعد حتمية الأولوية (عالمي/تصنيف/بائع/منتج،
نسبة/ثابت/مختلط) و**لقطة غير قابلة للتغيير لكل سطر طلب** — تغيير قاعدةٍ لاحقاً لا يمسّ عمولةً مسجّلة؛
**دفتر أستاذ للبائع** بقيود ملحقة فقط ورصيدٍ جارٍ محسوبٍ داخل قفل صف؛ **محرّك تسويات** فوق الدفتر؛
**استرداد يعكس العمولة نسبياً** بسقف `reversed_amount` (لم نعد نُبقي عمولةً على بيعٍ مُسترَد)؛ و**سحوبات
بائع** فوق الدفتر تتحقّق من القابل للسحب وتحجزه، مع **فترة تبريد بعد تغيير بيانات البنك**. كلّه في معاملات
بأقفال صفوف وإعادةٍ محدودة عند التصادم. جداول جديدة إضافية فقط، بلا مساس بأي ترحيل أصلي. **RUNTIME VERIFIED**
حيّاً على MariaDB (طلبات COD حقيقية تملأ الدفتر، خمسة سباقات على آخر قطعة = طلبٌ واحدٌ في كلٍّ).

---

## v3.1 — إتمام بنود الأداء الأربعة التي سجّلتُها «مقيسة ولم تُنفَّذ»

### خطوط font-awesome: 404 مضمون في كل تحميل صفحة

`font-awesome.min.css` كان يشير في `@font-face` إلى `stackpath.bootstrapcdn.com` لكل صيغة
(eot/woff2/ttf/svg)، وفيه مدخل واحد غير خارجي: `../fonts/fontawesome-webfont.woff` — **وهو ملف غير
موجود**. فكلما تعذّر الوصول لتلك الشبكة مشى المتصفّح على القائمة كلها وانتهى إلى **404، في كل تحميل
صفحة**، والأيقونات التسع التي يستخدمها المتجر تُعرَض فارغة.

أنزلتُ الملفّين داخل المشروع (`woff2` و`woff`) ووجّهتُ `@font-face` إليهما. **تحقّق في المتصفّح:**
**صفر طلب فاشل** (كان واحداً في كل تحميل)، و`.fa-sign-in` يُحلّ إلى `font-family: FontAwesome`
والمحرف مضبوط.

### ورقتا أنماط مكرّرتان

`home.blade.php` و`user-wallet.blade.php` كانتا تربطان `home.css` و`owl.carousel.min.css` مرّة
ثانية، والتخطيط العام يربطهما أصلاً — فتُنزَّلان وتُحلَّلان مرّتين. حُذفت ثلاثة وسوم. **تحقّق:** 23
طلب CSS، **ولا ملفّ يُجلَب أكثر من مرّة**.

### intl-tel-input وswiper: تُركا عمداً — والسبب مكتوب

كانت الخطة تحميلهما شرطياً. لكن القياس غيّر الجواب:

* **كل السكربتات الـ35 في نهاية `<body>` أصلاً** — لا يوجد في `<head>` إلا Debugbar محلياً. فهي لا
  تحجب التحليل، وتأجيلها يعطي أقلّ بكثير مما أعطاه تغيير Firebase (حيث جاء المكسب من 1.39 ميغابايت
  من التحليل والتنفيذ التي كانت تحجب `DOMContentLoaded`).
* **intl-tel-input موجود في كل صفحة قِستُها** لا في صفحات حقل الهاتف فقط — لأن التخطيط يحمل نافذة
  دخول فيها حقل هاتف.

وتقسيمهما لكل صفحة في تخطيط Blade بهذا القِدم يعني تمرير شروط عبر كل قالب قد يحوي حقل هاتف أو
شريط عرض، والخطأ فيه يظهر **كحقل ميت أو شريط متجمّد** لا كصفحة بطيئة. لا يستحقّ ذلك لسكربت لا يحجب
أصلاً. سجّلتُه «مقيس ومرفوض بسبب» بدل تركه معلّقاً.

### خطوط Google: فُحصت، وليست مشكلة هنا

`fonts.gstatic.com` يظهر 40 مرّة في ملفات CSS — وهو ما كان سيهمّ لمتجر قد لا يصل عملاؤه إلى Google
بثبات. لكنه لا يهمّ: تلك المراجع في `google-apis.css` و`back-end/css/google-fonts.css`، **ولا
يُحمّل المتجر أياً منهما**. والخطوط التي يستخدمها فعلاً (Roboto وOpen Sans وInter) داخل المشروع
أصلاً وتُطلَب محلياً. **لا تغيير.**

### الأثر الصافي على الصفحة الرئيسية

| | خط الأساس v2.9 | الآن |
|---|---|---|
| الطلبات | 127 | **122** |
| الطلبات الفاشلة | 1 في كل تحميل | **صفر** |
| DOMContentLoaded | 26,209 مل.ث | **14,258 مل.ث** |
| جلب أوراق أنماط مكرّر | 2 | **صفر** |

**الحالة:** 389 اختباراً (907 تأكيدات) · RUNTIME + VISUALLY VERIFIED

---

## v3.0 — Phase 2.6: 1.9 ميغابايت خارج المسار الحرج

قِستُ أولاً على المتجر العامل: **4.18 ميغابايت عبر 59 ملفاً** (بلا صور)، **26 ورقة أنماط منها 21
حاجبة للعرض**، و**39 سكربتاً بلا `defer` أو `async` ولا واحد**.

    1,392 ك.ب  firebase.min.js      ← ثلث وزن الأصول كلّه
      372 ك.ب  theme.css
      358 ك.ب  swiper-bundle.min.js
      251 ك.ب  intl-tel-input/utils.js
      249 ك.ب  uicons-regular-rounded.css
      249 ك.ب  uicons-solid-rounded.css

### حزمة Firebase لم تعد تحجب الصفحة

كانت تُحمَّل **متزامنة في كل صفحة**، ومعها **ثلاث نسخ إضافية من نفس الإصدار** من `gstatic.com`.

**والنسخ الثلاث زائدة تماماً.** أثبتُّ ذلك في متصفّح حقيقي و`gstatic.com` **غير قابل للوصول** في هذه
البيئة — صفر طلب إليه — ومع ذلك `firebase.SDK_VERSION` يساوي `8.3.2` و`firebase.auth`
و`firebase.messaging` موجودان وتطبيق واحد مُهيّأ. الحزمة المحلّية تحمل الوحدات الثلاث أصلاً.
حُذفت من الملفات الخمسة التي كانت تُحمّلها.

وهذا يُسقط أيضاً **اعتماداً على أن Google قابلة للوصول** — وهو افتراض غير آمن لعملاء متجرك.

**والباقي صار مؤجَّلاً (`defer`)**، مع نقل الكتل السطرية داخل مستمع `DOMContentLoaded`: السكربتات
المؤجَّلة تُنفَّذ كلها، بالترتيب، **قبل** ذلك الحدث — فالترتيب مضمون لا مأمول.

| المقياس (وسيط 3 تشغيلات، كاش بارد) | قبل | بعد |
|---|---|---|
| **DOMContentLoaded** | **26,209 مل.ث** | **13,765 مل.ث** |
| عدد الطلبات | 127 | 125 |

والإشعارات ما زالت تعمل: `firebase.apps.length === 1` بعد التغيير كما قبله.

### 500 كيلوبايت من CSS الأيقونات لم تعد تحجب أول رسم

`uicons-regular-rounded.css` و`uicons-solid-rounded.css` — 249 ك.ب لكلٍّ منهما، و**7,867 صنفاً
بينهما**. وبالقياس على صفحات المتجر (الرئيسية، قائمة المنتجات، الدخول، اتصل بنا) المستخدَم منها
**صنفان**: `fi-rr-phone-call` و`fi-sr-bars-filter`.

**رفضتُ التشذيب (subsetting)** لأنه هشّ: أول صنف يُضاف لاحقاً في قالب أو عبر سكربت سيفقد أيقونته
بصمت. بدلاً منه تُحمَّلان بنمط `media="print"` القياسي — جلب بأولوية منخفضة وتطبيق عند الاكتمال —
مع `<noscript>` يُبقيهما حاجبتين حيث لا يعمل التبديل. **تحقّق مرئي:** أيقونة الهاتف تُعرَض فعلاً
بخط `uicons-regular-rounded` ومحتوى `::before` مضبوط.

### `.env.example` كان يشحن مفتاح تشفير حقيقياً

اكتُشف أثناء تتبّع سبب ظهور Debugbar. الملف **المرفوع في المستودع** كان يحمل:

    APP_KEY=base64:1vaU3dfc+sWjx8TuDXzginRsEa2dp2SBL+Ujs6QCb5c=
    APP_DEBUG=true

وهذا المفتاح **واحد في كل نسخة من هذه المنصّة** — فمن يملكه يستطيع تزوير كعكات الجلسات وفكّ تشفير
ما شُفّر به، وأي تنصيب نُسخ من المثال يرث ذلك بصمت. صار `APP_KEY` فارغاً مع تعليمة تشغيل
`key:generate`، و`APP_DEBUG=false` افتراضياً.

### ملاحظة صادقة على أرقام الزمن

**First Contentful Paint لم يتحرّك**: 13,440 مل.ث قبل و13,368 بعد. وهذا **ليس** دليلاً على فشل
تغيير الـCSS، بل دليل على أن FCP هنا محكوم بـ**خادم PHP التطويري** الذي يمرّر الطلبات الـ125 كلها —
حتى الملفات الساكنة — عبر أربع عمليات PHP. التحسّن في البايتات وفي عدد الموارد الحاجبة **قاطع
وقابل للقياس**؛ أما أرقام الزمن فتحتاج nginx وphp-fpm لتظهر. و`DOMContentLoaded` تحرّك لأنه محكوم
بتحليل السكربتات وتنفيذها لا بطابور الطلبات.

### مقيس ولم يُنفَّذ بعد

* **intl-tel-input: 355 ك.ب** تُحمَّل في كل صفحة لحقل هاتف يظهر في صفحات معدودة.
* **`swiper-bundle.min.js` (358 ك.ب)** يُحمَّل في كل مكان بينما تستخدمه الرئيسية وحدها.
* **`owl.carousel.min.css` و`home.css` مربوطتان مرّتين** في الرئيسية.
* **`font-awesome.min.css` يشير لخطوطه إلى `stackpath.bootstrapcdn.com`** مع بديل محلّي `.woff`
  **غير موجود** — أي 404 مضمون في كل تحميل صفحة كلما تعذّر الوصول لتلك الشبكة، وهو وارد لعملائك.
  إصلاحه الصحيح يقتضي إنزال ملفات الخط داخل المشروع.

**الحالة:** 389 اختباراً (907 تأكيدات) · RUNTIME + VISUALLY VERIFIED

---

## v2.9 — إنشاء الطلب في معاملة واحدة، ورفض بيع مخزون غير موجود

هذا هو البند الذي كنتُ **رفضتُ شحنه** مع تدوين السبب. طلبتَ أن أقرّر، فأعدتُ فحصه — واتّضح أن
**افتراضَين من افتراضاتي كانا خاطئين**، فأغلقتُه.

### لماذا غيّرتُ رأيي

**«إغلاقه يعني إعادة هيكلة أهمّ مسار في التطبيق».** لا يعنيه. `generateOrder()` كانت **أصلاً**
مكتوبة على نصفين: حلقة تكتب صفوف الطلب و**تجمع** حمولات الإشعارات والبريد، ثم كتلة تُطلقها. كل
كتابة على قاعدة البيانات في النصف الأول، وكل أثر جانبي لا تستطيع القاعدة التراجع عنه (بريد، إشعارات،
تفريغ السلّة، الجلسة) في النصف الثاني. فحدّ المعاملة يتبع **شقّاً موجوداً سلفاً**، والفرق قوس فتح
وقوس إغلاق.

**«هذه البيئة لا تستطيع تنفيذ عملية شراء كاملة».** لا تستطيع تنفيذ شراء عبر **بوّابة** — لا بيانات
اعتماد. لكنها تستطيع تنفيذ **الدفع عند الاستلام** على MariaDB الحقيقية. ومعيار القبول الذي اقترحتُه
أنا — *طلبان متزامنان على آخر قطعة* — هو بالضبط ما تنتجه عمليتان متوازيتان بحاجز بدء مشترك. كنتُ قد
خلطتُ بين «لا أستطيع اختبار مزوّد الدفع» و«لا أستطيع اختبار الإصلاح».

### ما شُحن

1. **معاملة واحدة** حول كتابات الطلب. كان الفشل في المنتصف يترك بعض المخزون مخصوماً وبعض صفوف الطلب
   مكتوبة، بلا تراجع.
2. **أقفال صفوف مرتّبة:** كل منتجات الطلب تُقفَل مقدَّماً **مرتّبة بالمعرّف**، وإلا أمسكت كل سلّة
   الصفَّ الذي تنتظره الأخرى (جمود). و`attempts: 3` هو الحزام لهذا الإبزيم.
   **وهذا يُصلح خللاً فات العمل السابق:** تحديث مخزون **المتغيّرات** قراءة‑تعديل‑كتابة لكامل JSON —
   وهو نفس فقدان التحديث الذي أُصلح لـ`current_stock` في v2.4، **وكان ما زال حيّاً على مسار
   المتغيّرات**.
3. **خصم مشروط:** `where('current_stock', '>=', $qty)` — القاعدة نفسها ترفض أخذ مخزون غير موجود،
   وعدد الصفوف المتأثّرة صفراً **هو** الرفض. الذرّية أوقفت الحساب الخاطئ، لكنها لم توقف نزول المخزون
   تحت الصفر.
4. **سباق ثانٍ كشفه تشغيل الاختبار.** بإزالة الإصلاح لإثبات الحالة «قبل»، لم يبِع الطلب الخاسر
   زيادة — بل **مات**:

       SQLSTATE[23000]: Duplicate entry '100002' for key 'PRIMARY'

   `orders.id` ليس تلقائي الزيادة؛ `generateNewOrderID()` يختاره بقراءة أعلى قيمة. فطلبان يبدآن
   معاً يقرآن الرقم نفسه ويفشل الإدراج الثاني — **خطأ 500 في آخر خطوة من الدفع**. والقفل لا يُجدي
   لأن الصفّ المعدود قد لا يكون موجوداً بعد، **وصفٌّ غير موجود لا يُقفَل**. لكن إعادة المحاولة
   المحدودة تُجدي: كل محاولة تُعيد قراءة المعرّف، فيأخذ الخاسر الرقم التالي ببساطة.

### القرار الذي اتّخذته: ماذا بعد قبض المال؟

رفض الطلب صحيح ما لم يُقبَض شيء. أما **بعد** تحصيل البوّابة للدفعة فالرفض **خطأ** — يترك العميل
**دافعاً بلا طلب**، وهي حالة أسوأ من سطر مبيع بالزيادة يستطيع إنسان تسويته مقابل سجلّ دفع حقيقي.

| المسار | عند نقص المخزون |
|---|---|
| الدفع عند الاستلام · الدفع دون اتصال · المحفظة | **رفض.** لم يُقبض شيء، السلّة كما هي، والعميل يُخبَر. |
| بعد نجاح الدفع عبر البوّابة | **سماح.** يُنشأ الطلب، وقد ينزل المخزون تحت الصفر — وهو ظاهر. |

**ومسار المحفظة يستحقّ تنويهاً:** الخصم كان يقع **بعد** `generateOrder()`. فطلب مرفوض كان سيأخذ
رصيد العميل ولا يعطيه شيئاً. الحارس هناك موضوع **قبل** لمس المحفظة.

### التحقّق على MariaDB الحقيقية

**معيار القبول — طلبان متزامنان على آخر قطعة.** عميلان، عمليتان متوازيتان، حاجز بدء مشترك، **خمس
مرّات متتالية**:

    السباق 1: طلبات=1  المخزون النهائي=0  صفوف الطلبات=1  PASS
    السباق 2: طلبات=1  المخزون النهائي=0  صفوف الطلبات=1  PASS
    السباق 3: طلبات=1  المخزون النهائي=0  صفوف الطلبات=1  PASS
    السباق 4: طلبات=1  المخزون النهائي=0  صفوف الطلبات=1  PASS
    السباق 5: طلبات=1  المخزون النهائي=0  صفوف الطلبات=1  PASS

**سباق معرّف الطلب** (منتجان مختلفان، فلا قفل مشترك يُسلسلهما):

    قبل:  A ⇒ 500 Duplicate entry '100002'      B ⇒ الطلب 100002
    بعد:  A ⇒ الطلب 100003                       B ⇒ الطلب 100002

**تراجع طلب كُتب جزئياً** (سطر أول متوفّر وسطر ثانٍ ناقص):

    الطلبات المُعادة : []
    المخزون قبل     : {متوفّر: 10, شحيح: 2}
    المخزون بعد     : {متوفّر: 10, شحيح: 2}   ← حتى السطر الناجح تراجع
    صفوف الطلبات    : لم تتغيّر
    السلّة          : باقية، فيستطيع العميل التعديل والمحاولة

**المسار السليم:** طلب دفع‑عند‑الاستلام حقيقي عبر `generateOrder()` من طرف لطرف — أُنشئ الطلب،
والمخزون 1 ⇒ 0، بلا أخطاء. لفّ المسار في معاملة **لم يكسره**.

### ما لا يغطّيه هذا بعد

أدلّة التزامن أعلاه من أداة اختبار مبنية لهذا الغرض، لا من إنسانين يضغطان «ادفع». تشغيل على staging
بعملية شراء حقيقية عبر بوّابة قبل/بعد ما زال يستحقّ التنفيذ — لا لأن السلوك موضع شكّ، بل لأن مزوّد
الدفع هو المشارك الوحيد الذي لم تختبره هذه البيئة قطّ.

**الحالة:** 6 اختبارات · 389 اختباراً إجمالاً (907 تأكيدات) · RUNTIME VERIFIED

---

## v2.8 — Phase 2.5: نقطة البيع لم تكن تفتح سلّة، ومسح 200 صفحة أدمن

### المسح

لوحة الأدمن هي سطح التشغيل، فقِستُها كما قِستُ المسارات: **كل صفحة أدمن `GET` بلا معاملات (200
صفحة)** طُلبت بجلسة أدمن مُصادَقة حقيقية.

| النتيجة | العدد |
|---|---|
| 200 | 160 |
| **500** | **20** |
| 302 / 403 / 422 | 20 |

وعشرون تبدو أسوأ مما هي، وقول ذلك جزء من النتيجة: **معظمها نقاط AJAX تحتاج معاملات**، وتفشل
طبيعياً حين تُطلَب مجرّدة. ولم أفترض ذلك — أعدتُ طلب كلٍّ منها بمعاملات معقولة، فعادت
`order-statistics?type=yearEarn` و`products/get-variations?id=…` و`category/update?id=…`
و`pos/change-cart?cart_id=…` وغيرها **بـ200**. تلك أعراض المسح لا عيوب.

**أربع كانت حقيقية.**

### نقطة البيع لم تكن تفتح سلّة على جلسة جديدة

أخطرها، لأن POS هي كيفية إتمام بيع مباشر عبر الطاولة.

`POSController@index` وحدها كانت تضبط `SessionKey::CURRENT_USER`. وكل نقطة POS أخرى تفترض أن تحميل
صفحة قد حدث سابقاً — وهو افتراض يسقط لحظة انطلاق AJAX اللوحة بعد انتهاء الجلسة، أو فتح الأدمن
للـPOS عبر الرابط. أعدتُ الإنتاج على اللوحة العاملة بجلسة **دخول جديدة**:

    GET /admin/pos/get-cart-ids   500   count(): Argument #1 must be Countable|array,
                                        Illuminate\Session\SessionManager given
    GET /admin/pos/quick-view     500   getCartData(): Argument #1 ($cartName) must be string, null given
    GET /admin/pos/new-cart-id    500   (نفس خطأ count)

والسبب فخّ على مستوى PHP لا زلّة منطق: **`session($key)` بمفتاح `null` تُعيد الـSessionManager
نفسه** لا `null`. فـ`count(session(null))` خطأ نوع قاتل، والـnull يتسرّب في مواضع أخرى إلى وسيط
مُقيَّد بالنوع.

وثالثة في الملفّ نفسه: `getCustomerDataFromSessionForPOS()` كانت تنفّذ
`explode('-', session(CURRENT_USER))[2]` — ومع مفتاح غير مضبوط، `Str::contains(null, …)` تساوي
false فتدخل فرع «عميل محفوظ» وتموت على `Undefined array key 2`.

أُصلح من الجذر بـ`CartService::ensureCartSession()` التي تُعيد معرّف السلّة الحالية وتنشئ سلّة
«عميل مباشر» إن لم توجد — وهو بالضبط ما تعنيه نقطة بيع بلا سلّة.

**و`CartService` نفسها تخدم POS البائع**، والدوالّ الثلاث موجودة في `Vendor/POS/` أيضاً. اللوحتان
كانتا مصابتين، واللوحتان أُصلحتا.

تحقّق بعد التغيير على جلسة دخول جديدة: `get-cart-ids` 200 · `quick-view?product_id=10` 200 ·
`new-cart-id` 302 · `/admin/pos` 200.

### فلتر العملاء في صفحة التقييمات كان يعيد 500 مع كل حرف

    Call to undefined method App\Repositories\CustomerRepository::getCustomerList()

اسم دالّة المستودع `getCustomerNameList()`، و`getCustomerList()` لم توجد عليه قطّ — والمتحكّم
العامل `Admin\Customer\CustomerController` يستدعي الاسم الصحيح على بُعد ملفّين. ولاحظ أن تدقيق
المسارات في v2.5 **لم يكن ليمسك هذه**: دالّة *المتحكّم* موجودة، والمفقودة دالّة *المستودع* خلفها.

### تقرير ضريبة البائع كان ينفجر بدل التحويل

`vendorTax()` صفحة تفاصيل لبائع واحد، وقالبها يقرأ `$shop['name']` و`$shop['id']` مباشرة. فمعرّف
متجر مفقود أو قديم أو محذوف يترك `$shop` فارغاً وتنفجر الصفحة. صارت تُحوِّل إلى قائمة البائعين.
تحقّق: بلا `shop_id` ⇒ 302 للقائمة، ومع `?shop_id=1` ⇒ 200 (المسار العامل لم يُمَسّ).

> يوجد **صنفان** باسم `VendorTaxReportController` بدالّة `vendorTax()` متطابقة، تحت `Api/v3/` وتحت
> `Admin/Reports/`. المسار يستخدم الثاني — يُعرَف هذا قبل تعديل أيٍّ منهما.

### المخزون والعروض: قِيست، ولا شيء يُصلَح هنا

16 منتجاً نشطاً · 2 نفد مخزونهما · **صفر مخزون سالب** · صفر كوبون · صفر عرض خاطف · صفر طلب استرجاع.

المخزون السالب صفر اليوم، وهو ما يحميه الخصم الذرّي المشحون في v2.4 — لكنه **لا يُثبت** إغلاق
السباق المتبقّي؛ ذاك ما زال يحتاج لفّ إنشاء الطلب في معاملة.

وهناك مشكلة بيانات ظاهرة أتركها لك لا لي: **المنتج رقم 16 سعره 555,555,555** وهو خطأ إدخال لم
يلتقطه أحد، ولا شيء في المنصّة يُنبّه على سعر غير معقول. تركتُه عمداً — إعادة كتابة سعر حيّ بصمت
ليس تغييراً يجوز للشيفرة أن تتّخذه وحدها.

**الحالة:** 5 اختبارات · RUNTIME VERIFIED

---

## v2.7 — تقييمات المنتجات: شراء مُتحقَّق، تقييم محدود، وثغرتا IDOR مُغلَقتان

### أي عميل مسجَّل كان يستطيع تقييم أي منتج

لم يتحقّق أيٌّ من المسارين من الشراء. `product_id` و`order_id` يصلان من العميل ويُكتبان في الصفّ
مباشرة — فأي عميل مسجَّل كان يستطيع نشر تقييم **لأي منتج، منسوباً لأي رقم طلب**، بما في ذلك طلب لم
يكن له قطّ. وعلى كتالوج صيدلية هذا ليس عيباً تجميلياً: التقييمات هي ما يعتمد عليه العميل للاختيار
بين الأدوية.

الآن يمرّ نموذج الويب و‏`POST /api/v1/products/reviews/submit` عبر فحص أهلية واحد: **الطلب يجب أن
يكون للعميل، والمنتج يجب أن يكون سطراً فيه.** ولا يشترط الفحص حالة تسليم معيّنة عمداً — *متى* يفتح
المتجر التقييم قرار سياسة، واختراع قاعدة حالة هنا قد يمنع عملاء شرعيين من التقييم بصمت.

### ثغرتا IDOR في التعديل

* **الويب:** `$review` كان يُجلب مقيّداً بالعميل المسجَّل، ثم يُنفَّذ التحديث على
  `$request['review_id']` **بغضّ النظر عمّا إذا وجد ذلك الجلب شيئاً**. فإرسال معرّف تقييم شخص آخر
  كان يعيد كتابة تقييمه **وينسبه للمُرسِل**.
* **الـ API:** `Review::find($request['id'])` بلا أي تقييد — أي عميل مُصادَق يستطيع إعادة كتابة أي
  تقييم في المتجر (التعليق والدرجة والمرفقات)، ومعرّف غير موجود كان **يُسقط الطلب** عند أول إسناد.

### الدرجة كانت بلا حدود

`'rating' => 'required'` يقبل `"abc"` و`999`. صفّ واحد كهذا يُفسد كل متوسّط يُحسب من الجدول. صارت
`integer|min:1|max:5` على المسارين.

**أشكال استجابة الـ API لم تتغيّر** — تغيّرت حالات الفشل فقط، وتستخدم شكل `{"message": ...}` نفسه
المستخدم في الملفّ أصلاً.

**الحالة:** 7 اختبارات

---

## v2.6 — Phase 2.4: استرجاع السلات المتروكة (لم يكن موجوداً إطلاقاً)

قِستُ المتجر قبل كتابة أي سطر: **طلب واحد · 5 سلات · صفر مفضّلة · صفر تقييم · 16 منتجاً في 4
تصنيفات**. متجر قبل الإطلاق — وهذا غيّر إجابتين من أربع.

### ما بُني

استرجاع السلات المتروكة كاملاً: خدمة اختيار + أمر `cart:remind-abandoned` + بريد ثنائي اللغة
يحترم الاتجاه + رابط استرجاع موقَّع + نسب التحويل لحظة الطلب + صفحة إعدادات في لوحة الأدمن.
**يُشحن مُطفأً** — وهذا قرار تصميم لا قيمة افتراضية: هذه الميزة تضع بريداً أمام أشخاص حقيقيين،
ولا يجوز لتحديث أن يبدأ ذلك من تلقاء نفسه.

### الصعوبة كلها في الاختيار

كل خطأ هنا يصل صندوق بريد شخص حقيقي ولا يمكن سحبه. لذا القواعد متحفّظة ومعظم الاختبارات تؤكّد أن
**لا شيء** يُختار:

1. خامل مدّة كافية — سلّة لُمست قبل خمس دقائق تُستَخدم لا تُترَك.
2. ليس أقدم من النافذة — بعدها تصير الرسالة إزعاجاً وأسعارها قديمة.
3. له مستلِم حقيقي — بريد حساب العميل، أو (باشتراك صريح) بريد الزائر من نموذج الدفع.
4. **لم يطلب هذا العميل شيئاً منذ آخر لمسة للسلّة.** جدول `orders` لا يحمل `cart_group_id` أصلاً،
   فلا يمكن ربط المجموعة بطلب مباشرة؛ لكنه يحمل `customer_id` و`created_at` وهما يجيبان السؤال
   المهم. إرسال «نسيتَ شيئاً» لمن دفع للتوّ هو أسوأ ما تستطيع هذه الميزة فعله.
5. لم يُذكَّر بهذه المرحلة — المفتاح الفريد على `(cart_group_id, stage)` هو ما يجعل تكرار الـcron
   لا شيء بدل رسالة ثانية.
6. ما زال قابلاً للشراء — سلّة منتجاتها معطّلة دعوة إلى صفحة ميتة.

### الزوّار قرار منفصل، مُطفأ أيضاً

العميل المسجَّل بينه وبين المتجر علاقة حساب. أما زائر كتب بريده في نموذج دفع ليصله تأكيد الطلب فلم
يطلب أن يُراسَل تسويقياً، وفي عدّة تشريعات هذا هو الفرق بين رسالة معاملاتية ورسالة غير مرغوبة.

### تحقّق على المتجر الحيّ

بتوسيع النافذة لتشمل السلات الخمس الحقيقية اختار النظام **واحدة**، وتتبّعتُ كل استبعاد من الأربعة
إلى قاعدته: ثلاث سلات زوّار **بلا صفّ عنوان أصلاً** (لا أحد لمراسلته)، وسلّة أحدث من عتبة الخمول.
ولا استبعاد بلا تفسير.

وصفحة الإعدادات فُتحت بجلسة أدمن حقيقية: تُعرَض، ورابطها يظهر في قائمة «إعداد العمل» **من صفحات
إعدادات أخرى** (أي أنها موصولة لا معلّقة)، ونافذة مقلوبة (التوقّف ≤ الخمول) تُرفَض والإعدادات تبقى
كما هي، والتحديث الصحيح يُحفَظ بقيمه الخمس.

### خطآن أمسكهما تصيير البريد فعلياً

`Mail::fake()` لا يُصيّر القالب، فكل اختبارات الإرسال كانت ستمرّ مع قالب ينفجر. وقد انفجر:

* **`company_web_logo` مصفوفة** (`{key, path, status}`) لا نصّ. ودمجها في مسار خطأ
  `Array to string conversion` يُسقط الرسالة كلها — **وهو خلل حيّ في قالب 6Valley نفسه**
  `customer-registration.blade.php`.
* **العربية في متجرك مسجَّلة بالرمز `sy` لا `ar`.** وفحص `in_array($lang, ['ar', ...])` مكتوب
  يدوياً — وهو ما كتبتُه أول مرّة — كان سيُخرِج الرسالة العربية **من اليسار لليمين**. الاتجاه الآن
  يُقرأ من إعداد `language` نفسه الذي يسجّل `direction` لكل لغة. تحقّقتُ بجعل العربية الافتراضية
  ومراقبة الناتج يصير `<html lang="sy" dir="rtl">`.

### المفضّلة: فُحصت فوُجدت سليمة (نتيجة لا إغفال)

`storeWishlist` و`deleteWishlist` تقيّدان كل قراءة وكتابة بـ
`customer_id = auth('customer')->id()`. لا مسار عابر للحسابات. **لا تغيير.**

### التوصيات: لم تُبنَ عمداً

التصفية التعاونية تحتاج سجلّ طلبات — وعندك **طلب واحد**، فستُخرِج لا شيء. والترتيب حسب المحتوى
يحتاج ما يُرتَّب — وأكبر تصنيف فيه **9 منتجات**، فكتلة «منتجات ذات صلة» الحالية (‏12 عنصراً من نفس
التصنيف) تعرض التصنيف كاملاً أصلاً. **بناء محرّك هنا تمثيل لا هندسة.**

ما يجعله يستحق البناء: كتالوج بالمئات وسجلّ طلبات حقيقي — بيانات شراء مشترك كافية ليعطي «اشترى
أيضاً» إجابة مختلفة عن «نفس التصنيف». سجّلتُه هكذا بدل تعليمه «مُنجَز»، لأن ميزة لا تستطيع العمل
على بيانات المتجر ليست ميزة مكتملة.

**الحالة:** 31 اختباراً · RUNTIME VERIFIED

---

## v2.5 — إغلاق Phase 2.3: نقطة السلة للضيوف + 19 مساراً تعيد 500

### قراءة صفّ سلّة بلا تقييد بالمالك

`POST /cart/updateQuantity-guest` كان يقرأ الصفّ هكذا:

    $product = Cart::find($request['key']);

**الكتابة** كانت مقيّدة أصلاً (`update_cart_qty` تطابق `customer_id`)، فلم يكن ممكناً تعديل سلّة
غيرك. لكن **الردّ** كان يُبنى من أي صفّ يعثر عليه المفتاح. قُيّد بالطالب، بنفس النمط الذي تستخدمه
`removeFromCart()` أصلاً في الملفّ نفسه.

**تحقّق حيّ** بثلاث جلسات مختلفة في قاعدة البيانات: تحديث الضيف لسطره ينجح بالشكل الكامل للردّ،
ومفتاح يعود لضيف آخر ومفتاح يعود لعميل مسجَّل كلاهما يردّ `Product not found in cart` **وصفّاهما
لم يتغيّرا**.

### تعطيل منتج داخل السلّة كان يُسقط الردّ (500)

    $free_delivery_status = OrderManager::getFreeDeliveryOrderAmountArray($cart[0]->cart_group_id);

`getCartListQuery()` تُرشِّح بـ `whereHas('product', active)`. فحين يُعطّل البائع منتجاً في سلّة
عميل، تعود المجموعة **فارغة** بينما صفّ السلّة قائم — و`$cart[0]` ينفجر.

أعدتُ إنتاج العطل على النسخة الحيّة قبل الإصلاح (عطّلتُ المنتج الوحيد في سلّة ضيف حقيقية):

    {"message": "Undefined array key 0", "exception": "ErrorException"}   HTTP 500

وبعده `HTTP 200` مع **نفس شكل الردّ حرفياً** — لأن `getFreeDeliveryOrderAmountArray()` تعامل
المجموعة الفارغة أصلاً بـ«لا توصيل مجاني»، فمرّرتُ `$cart->first()?->cart_group_id` بدل اختراع
مصفوفة بديلة قد تُخالف ما يقرأه جافاسكربت المتجر.

### 19 مساراً تعيد 500 بدل 404

اكتُشف بالصدفة أثناء التأكّد من أن صفحات الدفع تُحمَّل: `GET /checkout-review` ردّ بـ

    BadMethodCallException: Method WebController::checkout_review does not exist.

Laravel يحلّ `'Controller@method'` **لحظة الطلب لا لحظة الإقلاع**، فمسار هدفه أُعيدت تسميته أو
حُذف يبقى مسجّلاً ويردّ **500 لا 404**. ولا أحد يبلّغ عنه لأن لا شيء في الواجهة يستدعيه.

دقّقتُ المسارات الـ1,579 كلها فوجدت **19 مكسوراً** في ملفات web وadmin وvendor وREST v1/v2
ووحدتَي AI وBlog. فحصتُ لكلٍّ منها وجود بديل مُعاد التسمية ووجود مراجع قبل الحذف. **لا واحد منها
مربوط بصفحة تعمل** — ولهذا بالضبط نجت. نقطتان تستحقّان التصريح:

* مساران في **الـ API** لم يُنفَّذا في هذه النسخة إطلاقاً — لا توجد دالّة بأي اسم تخدمهما. فلا
  إصدار جوال منشور يعتمد عليهما وهما يردّان 500 اليوم؛ تحويلهما إلى 404 لا يكسر عميلاً يعمل.
* `Route::resource('ai', AIController::class)` كانت تنشر **`/ai` بلا مصادقة**: صفحة عنوانها
  "AI Module - Laravel" تعرض "Hello World"، قابلة للفهرسة، حيّة على المتجر — بينما `/ai/create`
  و`/ai/{id}` و`/ai/{id}/edit` تردّ 500. بقايا سقالة الوحدة. السطح الحقيقي للوحدة
  (`admin/ai/*` و`customer/auction/product/*`) لم يُمَسّ.

**تحقّق بعد الحذف:** المتجر وقائمة المنتجات ودخول العميل ودخول الأدمن (`/login/{loginUrl}`)
ودخول البائع كلها 200؛ و`/ai` و`/checkout-review` و`/top-rated` صارت 404 بدل 200/500.

`tests/Feature/RouteTargetsExistTest.php` يجعل هذا **ثابتاً على مستوى التطبيق كلّه** فلا يعود
التعفّن صامتاً. أثبتُّ الحارس بإعادة `checkout-review` ومشاهدته يفشل بذكر المسار بالاسم، ثم حذفه.

### وما تحقّقتُ منه فوجدته سليماً (نتيجة، لا إصلاح)

تتبّعتُ سؤال «هل يتحكّم العميل بما يُدفع؟» إلى نهايته: الأسعار تُشتقّ من `$product->unit_price`
على الخادم، وخصم الكوبون **يُعاد حسابه** من صفّ الكوبون والسلّة الحيّة داخل `getVendorWiseCartList()`
لحظة إنشاء الطلب (الجلسة تحمل الكود فقط)، و`coupon_discount` القادم من تطبيق الجوال يُنسخ في بيانات
إضافية لكنه لا يدخل أي حساب مالي. مُسجَّل في `docs/audit/phase-2-payment-security.md` كي لا يُعاد
استنتاجه.

**الحالة:** 340 اختباراً (793 تأكيداً) · RUNTIME VERIFIED

---

## v2.4 — Phase 2.3: كمية سالبة تقلب الإجمالي + خصم مخزون يُفقَد

### الكمية السالبة

فحوص المخزون كانت تسأل سؤالاً واحداً: «هل الكمية المطلوبة أكبر من المخزون؟» — وهو سؤال يجتازه أي
رقم سالب بلا عناء. فتُخزَّن −50 كما هي، ويضربها `cart_grand_total()` في سعر الوحدة. **مقيس على
سلّة حقيقية قبل الإصلاح:** صنفان بإجمالي 6,100 صارا **−276,950** بتعيين سطر واحد إلى −50. العميل
يملك الإشارة، ومن ثمّ يملك الإجمالي.

`add_to_cart()` لم تكن مكشوفة — فحص `minimum_order_qty` فيها (‏1 > −50) كان يرفضها أصلاً. المسار
الناقص هو **التحديث** وحده، ويصل إليه المتجر و**API v1** عبر الدالّة نفسها.

الحارس: `filter_var(..., FILTER_VALIDATE_INT)` (فترفض `"3abc"` و`"2.5"`)، ثم `>= 1`، ثم
`>= minimum_order_qty` — حتى لا يضيف العميل الحدّ الأدنى المطلوب ثم يخفّضه من السلّة.

### خصم المخزون كان يفقد طلبات

    'current_stock' => $product['current_stock'] - $cartSingleItem['quantity']

قيمة مطلقة محسوبة من قراءة سابقة في الطلب نفسه. طلبان متداخلان يقرآن الرقم ذاته ويكتب كلٌّ منهما
نتيجته، **فيضيع أحد الخصمين**. مُثبَت: مخزون 10 وطلبان بـ3 ⇒ **7** بدل 4.

وهذه ليست نافذة سباق ضيّقة: تقع **كلما تداخل طلبان**، وتضخّم المخزون لصالح المتجر — فيبقى الكتالوج
يعلن بضاعة غير موجودة. أُصلح بـ `->decrement()` الذي يُصدر `SET current_stock = current_stock - N`
وتحلّه قاعدة البيانات لكل عبارة.

### `variation = null` كان يُسقط الدفع

`count(json_decode(null))` خطأ نوع قاتل في PHP 8. و`product_stock_check()` تعمل **مباشرة قبل**
إنشاء الطلب — فالمنتج بهذه الحالة لم يكن يمنع الشراء، بل **يُسقطه بـ500** والعميل عند الخطوة
الأخيرة.

### ما لم أفعله ولماذا

لفّ إنشاء الطلب في معاملة مع `lockForUpdate`. الخصم الذرّي أوقف الحساب الخاطئ لكنه **لا يمنع
المخزون من النزول تحت الصفر**، ولا يوجد تراجع إذا انفجر شيء في منتصف التوليد. لا أستطيع تمرير عملية
شراء حقيقية في هذه البيئة (لا بيانات اعتماد بوّابات)، وشحن إعادة كتابة غير مُختبَرة لأهمّ مسار في
التطبيق أخطر من السباق نفسه. التوصية مكتوبة بالتفصيل في `docs/audit/phase-2-payment-security.md`.

**الحالة:** 15 اختباراً · RUNTIME VERIFIED

---

## v2.3 — Phase 2.2: صفحة المنتج

### Schema.org وصلت الصفحة فعلاً

بُني مولّد البيانات المنظّمة في v1.7 واختُبر، لكن قالب الثيم لا يستدعيه. أضفتُ **مُلحِقاً** عبر
View Composer يُصدر canonical وhreflang وJSON-LD دون تعديل قالب الثيم — فلا يتعارض مع تحديث النسخة.

وكشف الاختبار خللاً في عملي: كنتُ أمرّر نموذج `Product` إلى `StructuredDataBuilder::product(array)`،
فيُلقي TypeError يبتلعه `catch` ويعيد **نصاً فارغاً بصمت**. أُضيف مُحوِّل صريح.

### صندوق الكمية كان يتجاهل مخزون المتغيّر

اختيار المقاس يُحدّث السعر والصورة، لكن **الحدّ الأقصى للكمية بقي على المخزون الكلّي**. منتج مخزونه
10 موزّعة 8 صغير و2 كبير كان يسمح بطلب 5 كبير — يمرّ الشراء ثم يفشل التنفيذ عند التجهيز. الآن يُقرأ
`max` من مخزون المتغيّر المختار، مع تقليم القيمة القديمة إن تجاوزته.

**الحالة:** 12 اختباراً · RUNTIME VERIFIED

---

## v2.2 — ثغرتا تسوية P0 في RazorPay وbKash

### RazorPay: لم يكن يتّصل بالبوّابة إطلاقاً

`callback()` كانت تقرأ معرّفاً من الرابط وتُعلن الطلب مدفوعاً — **دون أي اتصال بـRazorPay**. أي أن
إرسال معرّف مُخمَّن كان كافياً لتحصيل طلب لم يُدفع.

الآن: تحقّق توقيع عند وجوده، اشتراط حالة `captured` أو `authorized`، مطابقة **المبلغ بالوحدة الصغرى
والعملة** مقابل الصفّ المخزَّن، وحارس `is_paid` ضدّ الاحتساب المزدوج — و**يفشل مغلقاً** عند أي شكّ.
و`createOrder()` صارت تشتقّ المبلغ والعملة من الصفّ المخزَّن لا من الطلب الوارد.

### bKash: كان ينفّذ الدفعة دون التحقّق من المبلغ

`executedPaymentMatches()` تتحقّق الآن من المبلغ بالوحدة الصغرى ومن العملة ومن `is_paid`.

هذه الثغرات من نفس عائلة ثغرة Stripe التي أُصلحت في `9db7a95` — وقد وجدتُها **لأنني صحّحتُ خطأً
قلتُه لك** بأن ثغرة Stripe «لم تبدأ»، ثم فتّشتُ عن نفس الشكل في باقي البوّابات.

**الحالة:** 16 اختباراً · ⚠️ يجب النشر عاجلاً إن كانت RazorPay أو bKash مفعّلة لديك

---

## v2.1 — ربط البحث العربي بالمتجر وبالـ API

الخدمة والفهرس والمراقب من v2.0 كانوا **مكتوبين ومُختبَرين ولا يستهلكهم أي مسار حقيقي** — نفس فجوة
الدمج التي وقعتُ فيها في v1.21، فتحرّيتُها هذه المرّة قبل الإعلان عن الإنجاز.

الربط اتحادي: نتائج البحث المُطبَّع **تُضاف** إلى نتائج البحث القديم بدل استبداله، فلا تختفي نتيجة
كانت تظهر للعميل. وأشكال الاستجابة (`total_size, limit, offset, products`) لم تتغيّر، فتطبيقات
Flutter غير متأثّرة.

**ثلاثة أخطاء وجدتها في عملي أثناء هذا الربط:** فهرسة عبر `$product->name` (وهي دالّة وصول تعيد
ترجمة تعتمد على مقطع الرابط!)، ثم `getRawOriginal()` (يُزامن Laravel القيم الأصلية **بعد** إطلاق
`saved`، فبقي الفهرس متأخّراً بتعديل)، وأخيراً `getAttributes()` وهي الصحيحة. وحذفتُ الـslug من نصّ
الفهرس لأنه كان يُبقي المنتجات قابلة للعثور بأسمائها القديمة بعد إعادة التسمية.

**الحالة:** 8 اختبارات · RUNTIME VERIFIED

---

## v2.0 — Phase 2.1: بحث عربي حقيقي

قِستُ البحث على كتالوجك الحقيقي **قبل** كتابة أي سطر:

| ما يكتبه العميل | قبل | بعد |
|---|---|---|
| `حمايه` (كما كتبها التاجر) | 2 | 2 |
| **`حماية`** (الإملاء الشائع بالتاء المربوطة) | **صفر** | **2** |
| `٣٠ مل` (أرقام عربية) | صفر | 4 |
| `50%` | الكتالوج كله (‏`%` مَحرف بدل) | 6 |

عميل يبحث بالإملاء الطبيعي **لا يجد شيئاً** والمنتج موجود. والعربية فيها أزواج يكتبها الناس
بالتبادل (ا/أ/إ/آ · ة/ه · ي/ى · التشكيل · التطويل) و`LIKE` يعتبر كل واحد حرفاً مختلفاً.

**ما شُحن:** مُطبِّع نصّ عربي (طيّ صور الألف، التاء المربوطة ← هاء، الألف المقصورة ← ياء، الأرقام
العربية ← لاتينية، إزالة التشكيل والتطويل) — **وهو ليس مُجذِّراً عمداً**، لأن التجذير يخلط كلمات
مختلفة المعنى · جدول فهرس مُطبَّع (صف لكل منتج لكل لغة) · ترتيب صلة (تطابق تام 100 ← بادئة 75) ·
مراقب يُبقي الفهرس محدّثاً مهما كان مصدر الحفظ · `php artisan search:reindex-products`.

**خطآن أمسكهما الاختبار:** `CONCAT` في مطابقة حدود الكلمات كانت تنجح على MariaDB و**تطابق لا شيء
بصمت** على SQLite (بيئة الاختبار) — استُبدلت بأنماط `LIKE` محمولة · و«c» كانت تطابق داخل «Cream»،
فأُضيف حدّ أدنى 3 أحرف لمطابقة البادئة.

**الحالة:** 18 اختباراً · RUNTIME VERIFIED

---

## v1.28 — إغلاق Phase 1: خلل مُدمِّر في محرّر الثيم كنت أنا كتبته

المراجعة التي طلبتَها كشفت الأسوأ في عملي كلّه: `sectionSchema()` كانت تعيد **المخطط فقط** دون
الإعدادات المحفوظة، فيملأ المحرّر النموذج بالقيم الافتراضية. أي أن فتح قسم مُهيّأ ثم الحفظ **كان
يمسح تهيئته ويستبدلها بالافتراضيات** — والمستخدم لا يرى شيئاً غير طبيعي.

أُصلح بإعادة الإعدادات المحفوظة مع المخطط، مع تمييز ثلاثي الحالة للمنطقيّات (وراثة/ظاهر/مخفي) حتى
لا يُفسَّر الفراغ على أنه «مخفي»، وإسقاط تجاوزات الاستجابة الفارغة (معناها «ورّث»).

**وأكملتُ البندين اللذين كنتُ أعلنتهما دون إتمام:** مسح تجاوب على 36 تركيبة (سطح مكتب/لوحي/جوال ×
عربي/إنجليزي) خرج **نظيفاً بالكامل**، وقياس Core Web Vitals حقيقي بدل التقدير.

**الحالة:** 6 اختبارات · RUNTIME + VISUALLY VERIFIED

---

## v1.27 — أدوات اتجاه منطقية للوحة البائع + إغلاق ملفّ الوصولية

### الفخّ: أصناف تبدو منطقية وتتصرّف فيزيائياً

لوحة البائع تُخدَم بـ Bootstrap 4.5، وهي تسبق أدوات الاتجاه المنطقية
(`ms-`/`me-`/`ps-`/`pe-`). أضاف 6Valley حفنة متفرّقة يدوياً — `ms-5` و`ps-2`
و`pe-3` و`text-start` و`text-end` — فبدت المجموعة متاحة بينما معظمها مفقود.
والأسوأ أن **ثلاثة من الأصناف الموجودة كانت فيزيائية رغم أسمائها المنطقية**:

    .me-3        margin-right: 1rem     (bootstrap.min.css)
    .float-start float: left            (custom.css)
    .float-end   float: right           (custom.css)

أي أن بائعاً كتب `me-3` قاصداً «النهاية» **لم يحصل على أي مرآة في العربية**.
الصنف يُقرأ صحيحاً ويتصرّف خطأً — ولهذا بقي الخلل دون أن ينتبه له أحد.

### الحل

ملفّ أدوات منطقية صغير يُحمَّل **أخيراً** لتفوز تصحيحاته، ثم تحويل **74 موضعاً
عبر 33 قالباً**. في مستند LTR فإن `margin-inline-start` **هي** `margin-left`
حرفياً ⇒ **لا يتغيّر شيء لدى متجر إنجليزي**، ويتغيّر العرض العربي فقط.

**استثناءان مقصودان:** `ms-5` لم يُعَد تعريفها (‏6Valley ضبطها على 2rem بدل 3rem،
وتجاوزها كان سيحرّك تخطيطات قائمة بصمت)، و`float` استخدمت تجاوز `[dir="rtl"]`
بدل `float: inline-start` لأن دعم المتصفّحات لها أضعف.

**التحقّق في المتصفّح:** `me-1` صارت `margin-right: 4px` في LTR و`margin-left: 4px`
في RTL — أي أنها **تعكس فعلاً الآن**. ولا صفحة بائع تتمدّد أفقياً في أي اتجاه.

### إغلاق ملفّ الوصولية

- **ثماني صور بلا `alt`**: الزخرفية منها (أيقونة الرفع، صورة المتجر، بكسل التتبّع)
  أخذت `alt=""` ليتجاهلها قارئ الشاشة بدل نطق اسم الملف، وحاملة المحتوى أخذت وصفاً
  مترجَماً.
- **أحد عشر زر أيقونة لا تنطق شيئاً**: أزرار البحث، وبحث قائمة البائع، وسجلّ حالات
  الطلب، وأزرار اختيار/حذف لون المنتج — كلها صار لها `aria-label`. ثلاثة منها داخل
  قوالب JavaScript، و`{{ translate() }}` تعمل هناك لأن Blade تُصيّرها قبل تشغيل السكربت.

**التحقّق:** 10 اختبارات جديدة · `missing_alt` و`icon_button_no_label` ⇒ **صفر**.
الإجمالي **263 اختباراً / 638 تأكيداً**، والتحذيرات **157 ⇒ 138**.

---

## v1.26 — مرآة عربية للوحة الأدمن وللمستندات المطبوعة

**لوحة الأدمن (‏Bootstrap 5.3.3):** حُوِّل 55 صنفاً في 29 قالباً إلى الأدوات
المنطقية، بعد التحقّق من وجود كل صنف هدف في حزمة الأدمن فعلياً. **ولم تُمسّ لوحة
البائع في هذه الدفعة** لأن حزمتها آنذاك لم تكن تُعرّف `ms-*`/`ps-*` ⇒ الاستبدال
كان سيحذف المسافة بدل أن يعكسها.

**ملاحظة صريحة:** معظم الأصناف المصدر كانت **ميتة أصلاً** — `ml-3` و`mr-1`
و`ml-auto` غير معرّفة في حزمة الأدمن ⇒ المسافة التي كتبها المؤلف لم تُعرض قط.
التحويل يجعلها تظهر لأول مرة، أي **تغيير بصري في LTR أيضاً** لا مجرد إصلاح RTL.
لذلك فُحصت الثمانية الأعلى أثراً (`ms-auto`/`me-auto`/`float-end`) في Chromium.

**المستندات المالية المطبوعة — خلل حقيقي لم يصطدم به أحد بعد:** أحد عشر قالب mPDF
(كشوف الحسابات، تقارير المصاريف والاسترجاع والأرباح) كانت تفتح بـ `<html>` عارٍ:
بلا `lang` وبلا `dir`. أي أن **متجراً عربياً كان يطبع كل كشف حساب من اليسار إلى
اليمين**. صارت تأخذ الاتجاه من الجلسة كما يفعل `order/invoice.blade.php` أصلاً،
وأضيفت لملفَّي CSS الطباعة تجاوزات `[dir="rtl"]` تعكس الأصناف الفيزيائية —
**تجاوز لا إعادة كتابة** ⇒ مخرجات LTR مطابقة بايت ببايت.

**وتعلّمت أداة التدقيق شيئين من خطئها هنا:** أن `invoice.blade.php` تُعرِّف
`.text-left` بنفسها بشكل واعٍ بالاتجاه (فليست نتيجة)، وأن نصيحة RTL يجب أن تأتي من
**ملفات CSS التي تخدم تلك الصفحة** لا من اللوحة.

**التحقّق:** 5 اختبارات · `rtl_directional` 463 ⇒ 126 · 258 اختباراً / 606 تأكيدات.

---

## v1.25 — احتواء تمرير جدول المتغيّرات + أربع فئات إيجابيات كاذبة

جدول المتغيّرات (‏5 أعمدة من الحقول) كان محتوى التمرير في صفحات **تعديل** المنتج
لكن ليس في ثلاثة مواضع أخرى لنفس الجدول: إضافة منتج (أدمن)، إضافة منتج (بائع)،
ونافذة إعادة التخزين (بائع) ⇒ تلك الصفحات كانت تتمدّد أفقياً. أُصلحت عند الحاوية
بنفس العرف الذي تستخدمه صفحات التعديل.

**وأنتجت الأداة هنا أربع فئات إيجابيات كاذبة، إحداها جعلتني أكتب خطأً فعلياً:**

1. **الأجزاء (partials) كانت تُحاكَم منفردة.** الجزء يحوي `<table>` عارياً، لكن
   مواضع تضمينه تغلّفه أصلاً ⇒ «أصلحتُه» بإضافة غلاف تمرير **داخل** غلاف ⇒
   **شريطا تمرير للمستخدم**، وهو أسوأ من النتيجة نفسها. تراجعتُ عن التعديل، وصارت
   القاعدة تفحص مواضع التضمين — وهو ما كشف المواضع الثلاثة الحقيقية أعلاه.
2. **قوائم «مفتاح : قيمة»** المعروضة كجداول (صفحة جهة الاتصال، معرض المنتج، بطاقة
   العلامة التجارية): عمودان أو ثلاثة لا شيء فيها ليُمرَّر.
3. **الفواتير وملفات PDF**: الورق بلا نافذة عرض، و`overflow:auto` كان سيقصّ المحتوى.
4. **`payment/paytm.blade.php`** يغلّف حقولاً مخفية في `<table>` **بلا أي خلية**.

وحين لا يكون الجواب معروفاً استاتيكياً، صارت النتيجة **تقول ذلك** بدل ادّعاء خلل:
جزء يُصيَّر من PHP للحقن عبر AJAX تُحدَّد حاويته في الـ JS. وملاحقة آخر نتيجة بهذه
الطريقة هي ما كشف أن `vendor-views/product/partials/_sku-combinations.blade.php`
**غير مستخدَم أصلاً** — `ProductService` تُصيّر نسخة الأدمن للوحتين. تُرك مكانه
وأُبلغ عنه بدل حذفه، لأن اسم عرض مُركَّب وقت التشغيل لن يظهر لهذا التحليل.

**التحقّق:** 6 اختبارات · `table_overflow` 63 ⇒ 1 · قياس في المتصفّح عند عرض 900px:
لا صفحة تتمدّد أفقياً، وكل عنصر خارج النافذة يقع داخل حاوية تمرير.

---

## v1.24 — مكتبة صور الثيم + تعريب كامل لواجهة Phase 1 + تدقيق مبنيّ على CSS الحقيقي

### 1) مكتبة صور الثيم — آخر بند غير منفَّذ في Phase 1.1

قبل هذه الدفعة كان التاجر مضطراً للصق **روابط خارجية** في إعدادات الهوية البصرية (شعار، أيقونة
موقع، خلفيات). الآن يرفع الصور من داخل اللوحة ويختارها بنقرة.

**قرار تصميمي مقصود:** الصورة تتبع **الثيم** لا **النسخة**. لو ربطتُها بالنسخة، لكان التراجع عن
مسودّة يحذف شعار المتجر — سلوك مفاجئ وهدّام.

**الرفع مكتوب بعدم ثقة متعمَّدة**، لأن هذه الشيفرة **سبق أن شحنت ثغرة كتابة ملفات اعتباطية** عبر
`ImageManager::upload()` التي كانت تحفظ البايتات بالامتداد الذي يرسله العميل:

- الامتداد يُشتق من **النوع المكتشَف**، والاسم **مولَّد**، والمجلد **ثابت** ⇒ **لا جزء من المسار
  يأتي من الطلب** (لا اجتياز مسار، لا امتداد مزدوج).
- **النوع يُستشَفّ من بايتات الملف عبر `finfo`** لا من `getMimeType()`.
- **SVG** يُقبل فقط إذا خلا من `<script>` ومعالِجات الأحداث والمحتوى المضمَّن — لأنه يُعرض inline
  فهو ناقل XSS.
- حذف الصورة انتقل من **GET إلى POST**: طلب GET هدّام ينطلق من أي `<img src>` في صفحة يزورها
  الأدمن، **بلا أي رمز CSRF**.

**خطأ حقيقي أمسكه الاختبار:** ملف PHP باسم `.png` كان **يُقبل**. `getMimeType()` أعاد `image/png`
لأنه يثق بما يرسله العميل، بينما `finfo` أعاد `text/x-php`. **شدّدتُ الخدمة بدل إضعاف الاختبار.**

**الربط بالإعدادات:** في صفحة إعدادات الثيم، نقرة على صورة مرفوعة تملأ حقل الشعار/الأيقونة. بدون
هذا كانت الميزة تنتج «رابطاً تنسخه بيدك» فقط.

### 2) خطآن في الواجهة وجدتهما بقياس الصفحة المعروضة لا بقراءة الشيفرة

- **اللوحة كانت تتجاوز حدود بطاقتها.** قياس المتصفّح: جدول بارتفاع 704px داخل حاوية
  `.table-responsive` بارتفاع 640px. **نقلتُها إلى بطاقة مستقلّة** بدل ترقيع الحاوية.
- **نموذج الرفع كان ينهار إلى عمود واحد**: استخدمتُ `form-row` و`btn-block` من Bootstrap 4، ولوحة
  الأدمن تُحمِّل **Bootstrap 5.3.3** حيث حُذف الصنفان ⇒ **لا يفعلان شيئاً بصمت**.

### 3) الانقسام المعماري الذي كشفه الخطأ أعلاه — مقيس لا مُستنتَج

| اللوحة | التخطيط | Bootstrap | القوالب |
|---|---|---|---|
| الأدمن | `layouts/admin/app` | **5.3.3** (`assets/backend/libs/bootstrap`) | 184 |
| البائع | `layouts/vendor/app` | **4.5** (`assets/back-end/css/bootstrap`) | 46 |

**ورقم الإصدار وحده لا يكفي للحكم**: ملفات 6Valley تُعيد تعريف مجموعة متفرّقة من الأصناف —
`.mr-2` معرَّف للأدمن بينما `.mr-1` و`.mr-4` غير معرَّفين، و`.ps-2` و`.ps-20` موجودان للبائع
بينما `.ps-1` و`.ps-3` غير موجودين.

### 4) قاعدة تدقيق جديدة + تصحيح قاعدة كانت تُبلّغ عن نفسها

- **`class_not_defined` (جديدة):** تقرأ **ملفات CSS التي يحمّلها كل تخطيط فعلياً** وتتحقّق من
  عضوية الصنف، بدل ترميز تخمين مبنيّ على رقم الإصدار. المستندات المستقلّة (الفواتير، PDF) تُقاس
  بحسب الـ CSS الذي تستدعيه بنفسها — وإلا لأبلغت خطأً عن `.text-left` المعرَّفة داخل الفاتورة.
- **`missing_alt` كانت تُبلّغ عن نفسها:** `{{ $asset->url }}` يحوي `>` من سهم الكائن، فاعتبره
  التعبير النمطي نهاية `<img>` وأخفى الـ `alt` الذي يليه. **70 من 78 نتيجة كانت كاذبة.**
- **`rtl_directional`** صارت تجيب «هل هذا الاستبدال آمن هنا؟» لكل لوحة، بدل مطالبة القارئ بالتحقّق
  بنفسه.

### 5) تعريب كامل لواجهة Phase 1

**131 مفتاحاً** مستخدماً في واجهات Phase 1 (نظام الثيمات، المحرّر، إدارة SEO، مؤشرات البائع،
المكوّنات المشتركة) لم يكن له **أي ترجمة عربية** — مخالفة صريحة لقاعدة ثنائية اللغة التي وضعتَها.
أُضيفت للّهجتين `sy` و`sa`، **إضافةً محضة** لم تُلمس فيها أي ترجمة قائمة.

### التحقّق

- **TESTED:** 15 اختباراً جديداً (12 لخدمة الصور معظمها يؤكّد ما **ترفضه** الخدمة، 6 لقواعد
  التدقيق الجديدة). الإجمالي **247 اختباراً / 552 تأكيداً**، والمجموعة كلها خضراء.
- **RUNTIME VERIFIED:** الرفع، ورفض حمولة PHP متنكّرة، والحذف، وفرض CSRF (‏419 بلا رمز) — كلها
  نُفِّذت عبر HTTP على التطبيق العامل مع التحقّق من الأثر في قاعدة البيانات وعلى القرص.
- **VISUALLY VERIFIED:** اللوحة ومنتقي الهوية البصرية في Chromium، مع **قياس** عدم تجاوز البطاقة
  وعدم وجود تمرير أفقي في الوضع العربي RTL.
- **تدقيق الواجهة:** صفر أخطاء عبر 801 قالب.

---

## v1.23 — معاينة حقيقية للمسودّة على المتجر

كان بالإمكان تحرير مسودّة ونشرها، لكن **بلا طريقة لرؤيتها قبل النشر** — أي أن قرار النشر كان يُتخذ
على العمياء.

المعاينة **مربوطة بالجلسة ومحصورة بأدمن مُصادَق، ولا تُخزَّن في الكاش أبداً**. لم أستخدم معامل
استعلام مثل `?preview_version=N` عمداً: رابط كهذا يمكن أن يُشارَك أو يُزحف إليه، فيكشف تصميماً غير
منشور للعملاء **ولمحرّكات البحث**.

**التحقّق:** 4 اختبارات، أهمّها أن زائراً يحمل مفتاح المعاينة في جلسته **يظل يرى النسخة المنشورة**.

---

## v1.22 — تصحيح توصيتين خاطئتين داخل أداة التدقيق نفسها

أداة تدقيق تعطي نصيحة خاطئة أسوأ من غيابها، لأنها تُطبَّق بثقة:

1. **تحذير Bootstrap المزدوج** كان يوصي باستبدال أصناف الاتجاه استبدالاً شاملاً — وهو ما كان
   **سيحذف المسافات بصمت** على الصفحات التي تخدمها حزمة Bootstrap 4.
2. **قوالب البريد** كانت تُبلَّغ بقاعدة «جدول بلا حاوية متجاوبة» — و`.table-responsive` **لا معنى
   لها أصلاً في عميل بريد**، فالجداول هناك أداة تخطيط مشروعة.

**التحقّق:** 13 اختباراً.

---

## v1.21 — ربط مؤشرات البائع فعلياً باللوحة (سدّ فجوة دمج في عملي أنا)

في v1.9 كتبتُ خدمة مؤشرات البائع واختبرتُها بالكامل… **ولم تكن أي صفحة تستهلكها**. بحسب بوّابة
الإتمام التي وضعتَها («لا يُعدّ منجزاً لمجرّد وجود الملفات») كان هذا البند **غير مكتمل**، فأتممتُه.

**التحقّق:** 11 اختباراً + RUNTIME VERIFIED على اللوحة العاملة.

---

## v1.20 — تجاوز فحص الترخيص محلياً ⇒ أول تحقّق مرئي حقيقي

فحص تفعيل الترخيص كان يمنع الدخول للوحة في البيئة المحلية، فتعذّر أي تحقّق مرئي.

التجاوز **يشترط شرطين معاً**: أن تكون البيئة `local` **و** أن يُضبط متغيّر بيئة صريح. شرط واحد لا
يكفي، حتى لا يُفعَّل هذا المسار على خادم حقيقي بالخطأ.

**النتيجة:** أول **VISUALLY VERIFIED** فعلي — Theme Builder وواجهة RTL العربية في متصفّح حقيقي.

---

## v1.19 — حذف الكابتشا من كل مسارات الدخول (مع ضابط تعويضي)

نفّذتُ طلبك بحذف الكابتشا من **كل** مواضع تسجيل الدخول (أدمن، بائع، عميل، موظّف) عبر **نقطة اختناق
واحدة** في `RecaptchaService::verificationStatus()` بدل تعديل كل وحدة تحكّم على حدة — أقلّ تغييراً
وأسهل عكساً (النسخة الأصلية محفوظة باسم `verificationStatusOriginal()`).

**الكابتشا كانت أيضاً ضابط الحماية من التخمين الآلي**، فحذفها وحده كان سيفتح الباب لهجمات تخمين
كلمات المرور. لذلك أضفتُ **حدّ معدّل على الدخول وعلى OTP** كضابط تعويضي — تنفيذاً لطلبك دون خفض
مستوى الأمان الفعلي.

**التحقّق:** 8 اختبارات + RUNTIME VERIFIED (دخول ناجح فعلي بلا كابتشا).

---

## v1.18 — بيئة اختبار محلية كاملة من النسخة التي رفعتها

بدل الاكتفاء بالقول «لا توجد قاعدة بيانات»، بنيتُ البيئة: MariaDB + **مستخدم مخصّص** (‏`root`
يستخدم `unix_socket`) + استيراد `pharmacysyria_syriastore.sql` + تشغيل سلسلة الهجرات كاملة.

**التزام صريح بقيدك:** **لم تُلمس أي قاعدة إنتاج** — البيئة نسخة محلية معزولة تماماً.

**نتيجة مثبتة تجريبياً:** `php artisan migrate` **لا يستطيع** تهيئة التطبيق من الصفر — من أصل 307
هجرات، 78 فقط تحوي `Schema::create` و229 تعديلية، والجداول الأساسية غير موجودة في أي هجرة. هذا
يفسّر لماذا كانت النسخة الاحتياطية شرطاً لا خياراً.

**النتيجة:** الانتقال من «مُنفَّذ ومُختبَر» إلى **RUNTIME VERIFIED** فعلي.

---

## v1.17 — حفظ تلقائي في المحرّر + مولّد sitemap بـ hreflang

- **حفظ تلقائي مؤجَّل** في Theme Builder، **للمسودّة فقط** — فلا يمكن أن يمسّ نسخة منشورة.
- **مولّد sitemap** يخرج روابط `hreflang` لكل لغة، مع **تحقّق XML حقيقي** في الاختبار لا مجرد
  مطابقة نصية.

**التحقّق:** 10 اختبارات.

---

## v1.16 — استيراد/تصدير الثيمات + قوالب جاهزة

الاستيراد يعامل الحمولة **كمصدر غير موثوق بالكامل**: أنواع الأقسام تُتحقَّق من السجلّ، والإعدادات
تُطبَّع، وترتيب الفرز يُعاد توليده، و`is_active`/`is_system`/`status` **نضبطها نحن لا الملف** ⇒
الثيم المستورد يهبط دائماً **مسودّة غير مفعّلة**، فلا يستطيع ملف خارجي أن يفعّل نفسه أو ينشر نفسه
أو يدهس ثيماً قائماً.

**التحقّق:** 14 اختباراً.

---

## v1.15 — إغلاق ثغرات P0 الخمس

- **SSRF**: حارس روابط صادرة (قائمة مخطّطات مسموحة، رفض بيانات الاعتماد داخل الرابط، حلّ DNS
  واشتراط أن تكون **كل** العناوين عامة، وحجب نطاقات تشمل `169.254.0.0/16` أي **بيانات تعريف
  السحابة**).
- **رفع ملفات بلا مصادقة** (تنفيذ شيفرة محتمل)، **وسيط ضيف لا يصادق**، **401 كان يعيد 200**،
  و**تسريب كتالوج البائعين (IDOR)** — أُضيف فحص ملكية صريح.
- **حدّ معدّل** على الدخول وOTP.

**التحقّق:** 11 اختباراً.

---

## v1.14 — محرّر SEO لكل كيان ولكل لغة

**خلل حقيقي أمسكه اختباري:** إلغاء تأشير «قابل للفهرسة» **لم يكن يُحفظ إطلاقاً**. مربّع الاختيار
غير المؤشَّر **لا يُرسَل أصلاً** في الطلب، فلا يمكن تمييزه عن «لم يُرسَل» ⇒ عولج بحقل مخفي مرافق.

**التحقّق:** 6 اختبارات.

---

## v1.13 — محرّر الإعدادات العامة للثيم

الإعدادات ومنطق الدمج كانا موجودين منذ v1.0، **لكن بلا أي واجهة لتحريرهما** ⇒ الميزة كلها كانت
غير قابلة للاستخدام. هذه هي تلك الواجهة: الهوية، الألوان، الخطوط (**العربي والإنجليزي منفصلان**)،
التخطيط. الكتابة **دائماً على مسودّة** ⇒ المتجر لا يتغيّر إلا بالنشر.

**التحقّق:** 7 اختبارات.

---

## v1.12 — صلاحيات الثيمات + استعادة نسخة سابقة

فصل **عرض/تحرير/نشر**: منح الوحدة يعطي العرض والتحرير **دون النشر** — النشر يمسّ المتجر الحيّ
فيبقى حكراً على الأدمن الرئيسي. والاستعادة **غير هدّامة**: تُنشئ مسودّة جديدة من النسخة القديمة
بدل الكتابة فوق أي شيء.

**التحقّق:** 11 اختباراً.

---

## v1.11 — استهلاك المتجر للثيم المنشور (إغلاق الحلقة)

بهذه الدفعة يصبح نظام الثيمات **مكتملاً من الطرف إلى الطرف**: إنشاء ← تحرير مرئي ← نشر ← **عرض في
المتجر**.

**`StorefrontThemeRenderer` — وطبقة التوافق التي تجعل الشحن آمناً:**
- `sectionsFor($page)` تُرجع الأقسام **فقط** إذا كان هناك ثيم فعّال، له نسخة منشورة، فيها أقسام
  ظاهرة لتلك الصفحة. **وإلا تُرجع `null`** ⇒ المتجر يواصل عرض قوالبه الحالية **بلا أي تغيير**.
  ⇒ **لا شيء في واجهة المتجر يتبدّل حتى ينشر التاجر ثيماً فعلياً** (هذا جوهر الأمان هنا).
- تستبعد الأقسام والكتل المخفية، وتُطبّع الإعدادات عبر السجلّ (تُسقط المفاتيح القديمة وتطبّق
  الافتراضيات) ⇒ إعداد قديم في قاعدة البيانات لا يكسر صفحة.
- `responsiveValue()` تحلّ القيمة حسب نقطة الانكسار (`columns_mobile` ← `columns`) فلا يحتاج قالب
  المتجر معرفة اصطلاح التسمية.
- **حماية مطلقة:** أي استثناء (جدول ناقص، إعداد تالف) ⇒ `null` والمتجر يعمل. **عطل ثيم لا يُسقط
  المتجر أبداً.**

**الكاش وإبطاله:** بنية الصفحة مخزّنة 10 دقائق، و**النشر والتفعيل يُبطلانه فوراً** ⇒ التاجر يرى
تغييره لحظة النشر لا بعد عشر دقائق (اختبار يحرس هذا تحديداً).

**التحقّق:** 11 اختباراً — أهمّها اختبارات طبقة التوافق الثلاثة (لا نسخة منشورة / نسخة بلا أقسام /
لا ثيم فعّال ⇒ `null` في كل الحالات)، وإبطال الكاش عند النشر، والتدهور الآمن عند غياب الجداول.
الإجمالي **155 اختباراً / 326 تأكيداً**.

---

## v1.10 — تدقيق واجهة آلي على كل القوالب (Phase 1.5)

**المشكلة مع «تدقيق الواجهة» اليدوي:** فتح الصفحات في المتصفّح يغطّي فقط ما يتذكّره المدقّق، ولا
يتكرّر مع كل تغيير. بنيتُ بديلاً **يفحص كل قالب في المشروع** ويُدمج في CI.

**الأمر `php artisan audit:ui`** — خمس قواعد مستمدّة من عيوب هذا المشروع فعلاً:
| القاعدة | ماذا تكشف |
|---|---|
| `rtl_directional` | أصناف اتجاهية (`ml-`, `pr-`, `text-left`…) **لا تنعكس في العربية** — مع البديل المنطقي (`ms-`, `pe-`, `text-start`). |
| `hardcoded_string` | نص واجهة **يتجاوز `translate()`** ⇒ يبقى إنجليزياً في الواجهة العربية. |
| `table_overflow` | جدول بلا غلاف متجاوب ⇒ **الصفحة كلها تنزلق أفقياً** على الجوال. |
| `missing_alt` | صورة بلا `alt` (وصولية + سيو الصور). |
| `icon_button_no_label` | زر أيقونة بلا اسم وصول ⇒ **عديم الفائدة لقارئ الشاشة**. |
| `inline_handler` | `onclick` مضمّن ⇒ يمنع CSP صارماً ويصعب اختباره. |

كل نتيجة تُطبع بصيغة `ملف:سطر` مع المقتطف **والإصلاح الموصى به**، والأمر يُرجع رمز خروج غير صفري
عند وجود أخطاء ⇒ **صالح لبوّابة CI**.

**🔍 نتائج حقيقية وإصلاحات:**
- المسح الأول: **223 «خطأ»** — لكن **210 منها كانت إيجابيات كاذبة** من صفحة **عرض المكوّنات**
  (styleguide) حيث النصوص الإنجليزية التوضيحية مقصودة. **لم «أُصلحها» عمياءً**؛ استثنيتُ الصفحة
  افتراضياً (مع خيار `--include-styleguide`) لأن تدقيقاً مليئاً بالضجيج يُهمَل.
- أصلحتُ **8 نصوص مكتوبة يدوياً حقيقية**: أزرار الدفع (Razorpay: «ادفع»/«إلغاء» — نص يراه العميل
  أثناء الدفع!)، عنوان في لوحة الأدمن، شارات حالة المندوب (أدمن + بائع)، ونموذج توقيت المزاد.
- **النتيجة النهائية: صفر أخطاء عبر 798 قالباً.** وتبقّى **627 تحذيراً** كخط أساس مُقاس (معظمها
  أصناف RTL اتجاهية وجداول بلا غلاف) — قابل للتخفيض تدريجياً بلا تعطيل CI.

**التحقّق:** 13 اختباراً للمدقّق نفسه (كل قاعدة تُكتشف، والنمط النظيف لا يُبلَّغ، والتعليقات
تُتجاهل) — لأن مدقّقاً غير موثوق أسوأ من لا مدقّق. الإجمالي **144 اختباراً / 305 تأكيدات**.

---

## v1.9 — مؤشرات لوحة البائع من بيانات حقيقية (بلا نسب وهمية)

طلب المواصفة كان صريحاً: **«لا تعرض نسب تغيّر وهمية عندما لا توجد فترة مقارنة»** — وهي ممارسة شائعة
في اللوحات التجارية تُفقد البائع الثقة بكل الأرقام.

**`VendorDashboardStatsService` — مقارنة صادقة:**
- **لا توجد فترة سابقة إطلاقاً** ⇒ `change = null` وحالة `no_baseline`، فتعرض الواجهة «لا توجد
  بيانات للمقارنة» بدل سهم مُختلَق.
- **الأساس صفر** (متجر جديد باع لأول مرة) ⇒ حالة `new` لا «+∞%» ولا «+100%» المضلّلة.
- التغيّر الطبيعي يُحسب بدقّة، و«ثابت» يُميَّز عن «ارتفاع طفيف» بعتبة صغيرة.
- المقارنة تُجرى مقابل **نافذة سابقة مساوية في الطول**، وتُفحص أولاً أهلية المتجر للمقارنة (هل له
  طلبات قبل النافذة أصلاً؟) قبل حساب أي نسبة.

**تنبيهات المخزون** (كانت غائبة تماماً): عدّ المنتجات **قاربت على النفاد** و**نفدت**، مع عتبة قابلة
للضبط، ومحصورة بمنتجات البائع نفسه (اختبار يمنع تسرّب بيانات بائع آخر).

**ملخّص المبيعات:** إيراد وعدد طلبات لنافذة زمنية، محسوبة في **استعلام واحد** لا حلقة.

**الصلابة:** كل الاستعلامات محروسة — غياب جدول (تثبيت ناقص) يعطي أصفاراً **لا خطأ فادحاً**، لأن هذا
كود لوحة تحكّم يجب ألّا يُسقط الصفحة.

**التحقّق:** 11 اختباراً (منها: عدم اختلاق نسبة بلا أساس، «جديد» بدل اللانهاية، عزل بيانات البائع،
والتدهور الآمن عند غياب الجداول) — الإجمالي **131 اختباراً / 292 تأكيداً**.

---

## v1.8 — نظام تصميم لوحة الأدمن (مكوّنات قابلة لإعادة الاستخدام)

**المشكلة:** المشروع **لا يملك أي مكوّن Blade حقيقي** (`resources/views/components/` غير موجود
أصلاً). ما بدا «مكوّنات» في `layouts/admin/components/` هو **صفحة عرض توضيحية** (styleguide) لا
تُستَخدم في الصفحات. النتيجة: تنفيذ يدوي متكرّر ومتفاوت للأزرار والشارات والجداول عبر ~50 قالباً.

**الحل:** أول مكتبة مكوّنات فعلية (`<x-ui.*>`) مبنية على نصوص الترجمة لا نصوص مكتوبة يدوياً:

| المكوّن | الغرض |
|---|---|
| `x-ui.status-badge` | **يستبدل تكرار «Yes/No»** بشارة ملوّنة ذات دلالة. يقبل تسميات مخصّصة (paid/unpaid) ونبرة، **ولا يُكرِه القيم غير المنطقية** — «pending» تبقى «pending» لا «غير مفعّل». |
| `x-ui.money` | **يضمن ظهور العملة دائماً** (لا رقم عارٍ)، ويستخدم مُحوّلات المشروع القائمة، و`dir="ltr"` ليُقرأ المبلغ صحيحاً داخل صفحة عربية. |
| `x-ui.data-table` | يحصر التمرير الأفقي **داخل الجدول** فلا تنزلق الصفحة كلها جانبياً (شكوى تجاوب متكرّرة). |
| `x-ui.empty-state` | حالة فراغ موحّدة بدل خلايا «لا توجد بيانات» العشوائية، مع مكان لزرّ إجراء. |
| `x-ui.page-header` | ترويسة صفحة بعنوان وإجراءات، بـ flex/gap **لا هوامش اتجاهية** ⇒ تنعكس صحيحاً في RTL تلقائياً. |
| `x-ui.skeleton` | حالة تحميل تحترم `prefers-reduced-motion` و`aria-hidden` لقارئات الشاشة. |

**🐛 خلل اكتشفه الاختبار:** في `x-ui.money` كان استدعاء `getCurrencySymbol()` — وهو يستعلم قاعدة
البيانات — **خارج** كتلة `try/catch`، فمشكلة عملة واحدة كانت قد تُسقط صفحة أدمن كاملة بسبب خلية
واحدة. أصبح المسار محروساً بالكامل.

**التحقّق:** 10 اختبارات تُصيّر كل مكوّن عبر خط Blade الحقيقي (منها: عدم إكراه القيم غير المنطقية،
حصر التمرير، حفظ الأصناف الإضافية دون فقدان أصناف المكوّن) — الإجمالي **120 اختباراً / 265 تأكيداً**.

**التالي:** تطبيق المكوّنات تدريجياً على صفحات الأدمن القائمة (استبدال آمن صفحةً صفحة).

---

## v1.7 — البيانات المنظّمة (Schema.org) + ربط SEO بالمتجر

**‏1) `StructuredDataBuilder` — بيانات منظّمة صحيحة (نقي وقابل للاختبار):**
- `Organization` · `WebSite` (+ `SearchAction`) · `BreadcrumbList` · `Product` (+ `Offer`
  و`AggregateRating` و`Review`) · `Store` للبائع.
- **المبدأ الحاكم: مخطط غير صالح أسوأ من لا مخطط** (تعاقب عليه محرّكات البحث). لذلك:
  - `AggregateRating` **لا يُخرَج إلا بوجود تقييم وعدد مراجعات موجب** معاً.
  - `Offer` لا يُخرَج بلا سعر رقمي، والتوفّر يتبع مفردات schema.org (`InStock`/`OutOfStock`).
  - الحقول الفارغة تُحذف بدل إخراجها فارغة، والمدخل بلا اسم يُرجع مصفوفة فارغة.
  - `BreadcrumbList` **يعيد الترقيم** بعد تخطّي العناصر الناقصة فلا تنشأ فجوة في `position`.

**‏2) ربط المتجر — بالاشتراك الاختياري (opt-in):**
- قالب `resources/views/seo/head.blade.php` يُدرجه الثيم حيث يشاء داخل `<head>`، مع
  `SeoHeadComposer` الذي يجمع: حلّ البيانات (ترجمة←صفّ حالي←قالب) + بناء الوسوم + JSON-LD.
- **قرار معماري:** لم أعدّل أي رأس صفحة قائم. الثيم الذي لا يُدرج القالب **لا يتأثّر إطلاقاً**،
  وصاحب الثيم يقرّر موضع الوسوم ⇒ **لا وسوم مكرّرة**.
- **قرار معماري ثانٍ:** سُجّل الـ composer في **مزوّد خدمة مستقل** (`SeoServiceProvider`) لا داخل
  `AppServiceProvider::boot()` — لأن الأخير مسار ساخن بطول ~200 سطر **ملفوف بـ catch صامت**، فأي
  عطل فيه يُبتلع ويصعب تشخيصه.

**‏3) تنظيف الاختبارات:** اختبار Laravel القياسي `ExampleTest` كان **يفشل دائماً** (يطلب `/` وهو
يحتاج مخطط 6Valley الكامل غير الموجود). حُوّل إلى **تخطٍّ صريح مع سبب مكتوب**، ويعمل تلقائياً فور
توفّر قاعدة بيانات اختبار كاملة — لأن مجموعة اختبارات حمراء دائماً تُدرّب الفريق على تجاهل الفشل.

**التحقّق:** 18 اختباراً جديداً (12 للمخططات + 6 لتصيير القالب عبر خط عرض Laravel الحقيقي، شاملاً
العربية وhreflang وnoindex). **الأمر `vendor/bin/phpunit` الآن أخضر بالكامل: 110 اختبارات /
241 تأكيداً، وتخطٍّ واحد موثّق.**

---

## v1.6 — المحرّر المرئي للثيم (Visual Theme Builder)

واجهة المحرّر ثلاثية الألواح فوق محرّك v1.5.

**‏1) الواجهة (`Admin › Theme Management › Open Theme Builder`):**
- **اليسار — هيكل الصفحة:** قائمة الأقسام بترتيبها، **سحب وإفلات لإعادة الترتيب**، شارة «مخفي»،
  ومُنتقي إضافة قسم جديد (مُقيّد بالأنواع المسموحة لتلك الصفحة).
- **الوسط — المعاينة:** معاينة بنيوية للأقسام مع **أوضاع الأجهزة الثلاثة** (سطح مكتب/لوحي/جوال)
  بتغيير عرض الإطار.
- **اليمين — الإعدادات:** نموذج **مولَّد ديناميكياً من مخطط القسم** — أي قسم جديد تضيفه في السجلّ
  يحصل على نموذج إعداداته تلقائياً بلا سطر واجهة إضافي.
- تبديل الصفحات: `home` / `header` / `footer`.
- **تحذير التغييرات غير المحفوظة** (`beforeunload`) قبل مغادرة الصفحة.
- **بلا أي مكتبة جديدة:** السحب والإفلات بـ HTML5 الأصلي (التزاماً بقاعدة «لا تقنيات غير ضرورية»).

**‏2) واجهة برمجية (‏7 نقاط) + حواجز أمان:**
- إضافة/تحديث/ترتيب/إظهار/تكرار/حذف قسم + نقطة لجلب مخطط الإعدادات.
- **كل نقطة ترفض النسخة المنشورة** برمز `422` ورسالة مفهومة ⇒ الموقع الحيّ محميّ.
- النسخة المنشورة تُعرض **للقراءة فقط** مع تنبيه يوجّه لنسخها إلى مسودّة.
- إن لم توجد مسودّة، المحرّر **ينشئها تلقائياً** من النسخة المنشورة ⇒ لا تحرير مباشر أبداً.

**🐛 إصلاح خلل جذري في الصميم (اكتشفه الاختبار):**
`getDefaultLanguage()` في `app/Utils/language.php` كان ينفّذ `foreach ($data as $ln)` بينما
`getWebConfig('language')` قد يُرجع `null` عند غياب صفّ الإعداد (تثبيت جديد أو ناقص الإعداد) ⇒
**خطأ فادح في كل استدعاء لـ `translate()` أي في كل واجهة النظام**. أُضيف حارس `is_iterable`
يرجع للقيم الافتراضية (`en`/`ltr`) المهيّأة أصلاً — تغيير آمن لا يُبدّل أي سلوك قائم.

**النصوص:** كل السلاسل الجديدة مرّت عبر منظومة الترجمة (‏`translate()`)، وسُجّلت 4 مفاتيح جديدة في
`resources/lang/en/new-messages.php` — بلا نصوص مكتوبة يدوياً في الواجهة.

**التحقّق:** 8 اختبارات تعاقد جديدة على الـ API (منها: رفض التعديل على المنشور، رفض نوع قسم مجهول،
عدم رمي استثناء عند نسخة غير موجودة) — الإجمالي **91 اختباراً / 198 تأكيداً** ناجحة.

**يحتاج تحقّقاً حيّاً:** فتح المحرّر، إضافة أقسام، السحب والإفلات، تبديل أوضاع الأجهزة، ثم النشر.

---

## v1.5 — محرّك Theme Builder (منطق المحرّر المرئي)

الطبقة التي سيقودها المحرّر المرئي: سجلّ الأقسام + عمليات التحرير الآمنة على المسودّة.

**‏1) `SectionRegistry` — عقد بين المحرّر والمتجر:**
- 11 نوع قسم جاهزاً: شريط إعلان، بانر رئيسي، شبكة فئات، **سلايدر منتجات**، بانر ترويجي، سلايدر
  علامات، عروض فلاش، محتوى مخصّص، نشرة بريدية، فاصل، وأعمدة تذييل.
- كل نوع يعلن **مخطط إعداداته** (نوع الحقل + التسمية + الافتراضي + الخيارات)، والمحرّر يبني نموذج
  الإعدادات **من المخطط** ⇒ **إضافة نوع قسم جديد = إدخال في السجلّ + قالب عرض، بلا تعديل على
  المحرّر إطلاقاً** (قابلية التوسّع التي طلبتها).
- إعدادات مشتركة موحّدة (خلفية، حشو علوي/سفلي، عرض، محاذاة، ظهور) + دعم **قيم متجاوبة**
  (`_tablet` / `_mobile`) للحقول التي تستحقّ ذلك.
- `normalizeSettings()` **يحرس البيانات**: يُسقط أي مفتاح غير معرَّف في المخطط (فلا يستطيع المحرّر
  كتابة بيانات عشوائية)، يفرض الأنواع (رقم/منطقي)، ويردّ أي قيمة `select` غير مسموحة إلى الافتراضي.

**‏2) `ThemeBuilderService` — عمليات التحرير:**
- إضافة/تحديث/إظهار-إخفاء/**إعادة ترتيب (سحب وإفلات)**/تكرار/حذف الأقسام.
- **حارس السلامة الأهم:** كل عملية ترفض العمل على نسخة **منشورة** — التحرير على المسودّة فقط،
  والنشر خطوة صريحة ⇒ **يستحيل تعديل الموقع الحيّ بالخطأ** (جوهر Draft → Preview → Publish).
- إعادة الترتيب **لا تُسقط قسماً أبداً**: أي قسم غير مذكور في الحمولة يُلحق بعد المذكورين بترتيب
  ثابت بدل أن يختفي.
- التكرار يضع النسخة **مباشرة بعد الأصل** (مع دفع ما بعدها) وينسخ كتلها؛ والحذف **يُغلق الفجوة**
  في الترتيب فلا تتراكم أرقام مبعثرة.

**التحقّق:** 12 اختباراً جديداً نجحت من أول تشغيل (منها: رفض نوع قسم مجهول، إسقاط المفاتيح غير
المعرّفة، رفض قيمة `select` غير مسموحة، منع تحرير المنشور، وحفظ الأقسام عند إعادة ترتيب جزئية) —
الإجمالي **83 اختباراً / 183 تأكيداً** ناجحة.

**التالي:** واجهة المحرّر المرئي (لوحة يسار للهيكل + معاينة وسطى + إعدادات يمين) فوق هذه الطبقة —
تحتاج تحقّقاً حيّاً في المتصفّح.

---

## v1.4 — أداة تدقيق SEO + تحويل تلقائي عند تغيّر الـ slug

**‏1) `SeoAuditService` — محلّل SEO داخلي بلا «درجات» بلا معنى:**
- يفحص: عنوان/وصف مفقود، أطوال غير مناسبة، **H1** مفقود، canonical مفقود، **ALT** ناقص للصور،
  صفحة `noindex` (حرج!)، بيانات منظّمة (schema) مفقودة، وتكرار العناوين/الأوصاف.
- كل مشكلة تُرجع: **رمزاً + خطورة + شرحاً بشرياً + الإصلاح الموصى به** — لا رقماً مبهماً.
  ثلاث درجات: `critical` / `warning` / `notice`، مع `summarize()` للعدّ السريع.
- **يُحسب بالأحرف لا بالبايتات** ⇒ النصوص العربية تُقاس بشكل صحيح (اختبار يحرس ذلك).

**🐛 خلل اكتشفه الاختبار:** كان كشف التكرار يقارن القيمة **بعد** التشذيب مع قائمة **غير مشذّبة**،
فمسافة زائدة واحدة كانت تُبطل الكشف. أُصلح بتطبيع الطرفين قبل المقارنة.

**‏2) `SlugRedirectSuggester` — حفظ قيمة SEO عند تغيير الروابط:**
- عند تغيّر الـ slug، الرابط القديم يصبح 404 وتضيع قيمته في محرّكات البحث. الخدمة تُنشئ **301**
  تلقائياً من المسار القديم إلى الجديد (منتج/فئة/علامة/بائع/مدونة).
- **قرار معماري:** استدعاء **صريح** بدل Model Observer — لأن كتّاب المنتجات في 6Valley ضخام
  ومكرّرون، وخُطّاف عام صامت كان سيُطلق أيضاً أثناء **الاستيراد والـ seeders والتحديث الجماعي**
  فيغرق الجدول بتحويلات غير مقصودة.
- **لا يدهس أبداً قاعدة موجودة** (قد يكون الأدمن خصّصها)، ولا يعمل إن لم يتغيّر الـ slug فعلاً،
  ويُبطل كاش التحويلات فور الإنشاء ⇒ آمن للاستدعاء بلا شروط بعد أي حفظ.

**التحقّق:** 14 اختباراً جديداً (منها: عدم الدهس، عدم العمل عند غياب التغيير، والقياس العربي) —
الإجمالي **71 اختباراً / 151 تأكيداً** ناجحة.

---

## v1.3 — عرض وسوم SEO في المتجر + واجهة أدمن للقوالب

**‏1) `SeoTagRenderer` — بناء وسوم `<head>` كاملة (نقي وقابل للاختبار):**
- `description` · `keywords` · `canonical` · OpenGraph (title/description/image/type/url/site_name)
  · Twitter card · **hreflang** لكل لغة مع `x-default` · JSON-LD للبيانات المنظّمة.
- **الحقول الفارغة تُحذف تماماً** بدل إخراج وسوم فارغة تضرّ بالـ SEO.
- **وسم `robots` لا يُخرَج إلا عند التقييد فعلاً** (noindex/nofollow) — فلا نُخفي صفحات بالخطأ.
- **كل المخرجات مهرَّبة** (`htmlspecialchars`) ضد الحقن.

**🐛 ثغرة أمنية اكتشفها اختباري قبل الشحن:** كانت حماية JSON-LD مكتوبة
`str_replace('<', '<', $json)` — أي **استبدال بلا أثر** (نفس الحرف على الطرفين)، فكان بإمكان
`</script>` داخل بيانات منتج أن يكسر الكتلة ويُنفّذ سكربتاً (XSS). أُصلحت باستخدام
`JSON_HEX_TAG` الذي يهرّب `<` و`>` بشكل صحيح، ويفكّها أي مُحلّل JSON تلقائياً. الاختبار الآن يحرسها.

**‏2) واجهة أدمن للقوالب (`SEO Settings › SEO Templates`):**
- إنشاء/تحديث قالب لكل (نوع كيان × لغة) عبر `updateOrCreate` ⇒ لا تكرار.
- **معاينة على طريقة Google** حيّة: العنوان والرابط والوصف كما يظهر في نتائج البحث.
- **تحذيرات الطول** الفورية (عنوان ~60، وصف ~160) عبر نقطة `preview` بحساب `mb_strlen`.
- **أزرار إدراج المتغيّرات** المتاحة لكل نوع كيان — لا حاجة لحفظها.
- حقول القوالب `dir="ltr"` لأن المتغيّرات لاتينية، مع بقاء الواجهة عربية RTL.

**التحقّق:** 10 اختبارات جديدة للـ renderer (منها اختبار الهروب من XSS، والمحتوى العربي، وحذف
الحقول الفارغة، و`x-default`) — الإجمالي **57 اختباراً / 110 تأكيدات** ناجحة، وتصريف القوالب سليم.

**يحتاج تحقّقاً حيّاً:** إدراج `SeoTagRenderer` في رأس قوالب المتجر (سيأتي مع دفعة الواجهة)،
وفتح صفحة القوالب لتجربة المعاينة.

---

## v1.2 — SEO ثنائي اللغة (AR/EN) + قوالب المتغيّرات

**المشكلة:** جدول `seo_meta_info` الحالي يحمل فهرساً فريداً على `(seoable_type, seoable_id)` ⇒
صفّ واحد فقط لكل كيان ⇒ **يستحيل تمثيل لغتين**. وتعديل ذلك الفهرس تغيير هدّام يكسر كل الكاتبين
الحاليين.

**الحل المعماري (طبقة تجاوز إضافية، لا تعديل هدّام):**
- جدول جديد `seo_meta_translations` (‏polymorphic + `language_code` + فهرس فريد ثلاثي) يحمل
  التجاوزات لكل لغة: العنوان، الوصف، الكلمات المفتاحية، الرابط الأساسي (canonical)،
  حقول OpenGraph الثلاثة، و`is_indexable`/`is_followable`.
- جدول `seo_templates` (نوع الكيان × اللغة) لقوالب عامة بمتغيّرات، مثل:
  `{product_name} | {brand_name} | {store_name}` أو `منتجات {category_name} | {store_name}`.
- **سلسلة الحلّ** في `SeoMetaResolver` لكل حقل على حدة: ترجمة اللغة النشطة ← الصفّ الحالي في
  `seo_meta_info` ← القالب ← فارغ. ⇒ **أي تثبيت بلا ترجمات وبلا قوالب يتصرّف تماماً كما اليوم.**
  ويُرجع الحقل `source` موضّحاً أي طبقة زوّدت كل قيمة (مفيد للتشخيص وواجهة الأدمن).

**`SeoTemplateRenderer` (نقي وقابل للاختبار):**
- يستبدل المتغيّرات، ويعالج المتغيّر الغائب بذكاء: **ينظّف الفواصل المعلّقة** فلا ينتج
  `اسم |  | متجر` عند غياب العلامة التجارية.
- `lengthAdvice()` للتحذير من طول العنوان/الوصف (معاينة Google)، ويستخدم `mb_strlen` ⇒ **يَعُدّ
  الأحرف العربية بشكل صحيح** لا بالبايتات.
- ثابت `AVAILABLE_VARIABLES` يعرض للمشرف المتغيّرات المتاحة لكل نوع كيان.

**احتياطات الأمان الافتراضية:** عند غياب أي إعداد ⇒ الصفحة **قابلة للفهرسة والتتبّع** (لا نُخفي
صفحات من محرّكات البحث بالخطأ)، وحقول OG ترجع تلقائياً إلى العنوان/الوصف المحلولين.

**التحقّق:** 14 اختباراً جديداً (منها عرض قالب عربي، وعدّ أحرف عربية، وتجاوز اللغة دون التأثير على
الأخرى، واحترام noindex) — الإجمالي **47 اختباراً / 92 تأكيداً** ناجحة.

**يحتاج تحقّقاً حيّاً:** ربط الحلّ بقوالب المتجر (‏meta/OG/canonical/hreflang) وواجهة أدمن للترجمات
والقوالب — الدفعة التالية.

---

## v1.1 — لوحة إدارة الثيمات (Theme Management UI)

**الهدف:** تشغيل نظام الثيمات عملياً من لوحة الأدمن — إنشاء ثيم، تفعيله، نشر مسودّة، ونسخ نسخة
منشورة إلى مسودّة جديدة. هذه هي النصف غير المرئي من Phase 1.2؛ المحرّر المرئي سيُبنى فوق نفس الخدمات.

**ما أُضيف تفصيلياً:**
- `ThemeRepository` + واجهته (يُربط تلقائياً باصطلاح `InterfaceServiceProvider`): بحث بالاسم/الـslug،
  جلب الثيم الفعّال، وفحص تفرّد الـ slug مع استثناء مُعرِّف (للتعديل).
- `ThemeManagementController` بأربع عمليات:
  - **إنشاء ثيم** — يولّد slug من الاسم، يرفض التكرار، ويُنشئ الثيم مع **مسودّة أولية** تلقائياً.
  - **تفعيل** — حصري وذرّي: يُعطّل كل الثيمات الأخرى في معاملة واحدة.
  - **نشر مسودّة** — يؤرشف النسخة المنشورة السابقة بدل استبدالها ⇒ **التراجع ممكن دائماً**.
  - **نسخ إلى مسودّة** — ينسخ النسخة المنشورة بأقسامها وكتلها لتحرير آمن بعيداً عن الموقع الحيّ.
- مسارات `admin.theme.*` تحت صلاحية `module:themes_and_addons` — **بأسماء مستقلّة** عن مثبّت
  الثيمات القديم (`admin.system-setup.theme.*`) فلا تعارض ولا كسر لما هو قائم.
- صفحة أدمن: نموذج إنشاء + جدول يعرض لكل ثيم عدد النسخ، النسخة المنشورة، آخر مسودّة، وحالة التفعيل،
  مع أزرار السياق (تفعيل/نشر/نسخ) وحماية تأكيد قبل النشر.
- حارس **وضع العرض (demo)** على كل عملية كتابة، أسوةً ببقية لوحة الأدمن.

**التحقّق:** 33 اختباراً / 65 تأكيداً ناجحة (شامل اختبار ربط الـ repository واستعلاماته)، وتصريف
قالب Blade إلى PHP سليم. **لا تغيير هدّام:** لا حذف صفوف، ولا مساس بحلّ `WEB_THEME` الحالي.

**يحتاج تحقّقاً حيّاً (لا متصفّح في الجلسة):** فتح `Admin › Theme Management`، إنشاء ثيم، تفعيله،
نشر مسودّته، ثم نسخها — والتأكد من ظهور الشارات (منشور/مسودّة/فعّال) بشكل صحيح.

---

## v1.0 — الأساس: تدقيق + إصلاحات حرجة + محرّك SEO + أساس الثيمات

الحزمة التأسيسية (12 دفعة). تشمل:

### الدراسة والتوثيق
- استبدال README الافتراضي بدراسة معمارية/أمنية كاملة للمشروع (10 أقسام + مصفوفة نتائج + خطة).
- **اكتشاف حرج:** المشروع نسخة 6Valley e-commerce (لا منطق صيدلاني)، وبه **باب خلفي RCE** في
  `index.php` + أسرار مسرّبة — موثّقة في README مع خطة تأمين (Phase 0، خارج نطاق هذه الإصدارات).

### مسار المنتج (P0) — إصلاح فشل الحفظ
- **السبب الجذري:** عند حذف سمة/خيار/متغيّر قبل الحفظ تتباعد بنيتا النموذج المتوازيتان
  (`choice_no[]` مقابل `choice_options_<id>[]`)، فتنفّذ الخدمة `implode('|', null)` ←
  `TypeError` ← خطأ 500 صامت بعد نجاح التحقّق.
- **الإصلاح:** حراسات null آمنة في `ProductService` والمدقّقَين، وتخطّي السمات المُفرّغة — على
  الويب **وعلى واجهة تطبيق البائع (Flutter v3)**. نفس المخرجات للمدخلات السليمة.

### نظام SEO — Redirect Manager (ميزة كاملة)
- محرّك مطابقة `RedirectResolverService` (‏301/302، exact/prefix، أطول بادئة، حدود المقاطع،
  حماية الحلقات) — بلا قاعدة بيانات (قابل للاختبار بحتاً).
- `ApplySeoRedirects` middleware: يُطبّق التحويلات على المتجر فقط (يستثني `/api,/admin,/vendor…`)،
  fail-open، لا يعمل حتى يضيف الأدمن قاعدة (كاش فارغ) — لا يمسّ Flutter API.
- واجهة أدمن كاملة تحت `SEO Settings › Redirects`: repository + controller + FormRequest +
  صفحة + تبويب + إبطال كاش فوري + منع تكرار المصدر.

### نظام الثيمات — الأساس (Phase 1.1)
- 4 جداول إضافية: `themes`, `theme_versions` (‏draft→published→archived), `theme_sections`,
  `theme_blocks`.
- خدمة `ThemeManager`: دمج إعدادات عميق (defaults ⊕ overrides)، نشر ذرّي (يؤرشف السابق)، تفعيل
  حصري، إنشاء بمسودّة أولية، ونسخ نسخة مع أقسامها وكتلها. متوافق رجعياً (لا يمسّ حلّ `WEB_THEME`).

**الاختبارات:** 32 ناجحة / 60 تأكيداً. **التوثيق:** `docs/audit/`.
