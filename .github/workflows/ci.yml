name: CI

on:
  push:
    branches: [ main, "claude/**" ]
  pull_request:
    branches: [ main ]

jobs:
  tests:
    name: PHPUnit (sqlite :memory:)
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup PHP
        uses: shivammathur/setup-php@v2
        with:
          php-version: '8.2'
          extensions: curl, dom, fileinfo, gd, intl, json, libxml, mbstring, openssl, pdo_sqlite, sqlite3, zip
          coverage: none

      - name: Cache Composer packages
        uses: actions/cache@v4
        with:
          path: vendor
          key: composer-${{ hashFiles('composer.lock') }}
          restore-keys: composer-

      # Authenticate Composer with the workflow token so codeload.github.com dist downloads are not
      # rate-limited (unauthenticated pulls hit HTTP 429 and fail the job). Retry once with source
      # fallback for the rare case a dist zip is still unavailable.
      - name: Configure Composer auth
        run: composer config -g github-oauth.github.com "${{ secrets.GITHUB_TOKEN }}"

      - name: Install dependencies
        run: |
          composer install --no-interaction --prefer-dist --no-progress \
            || composer install --no-interaction --prefer-source --no-progress

      - name: Prepare environment
        run: |
          cp .env.example .env
          php artisan key:generate
          php artisan passport:keys --force

      - name: Run test suite
        run: php artisan test --stop-on-failure
