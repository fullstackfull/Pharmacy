{{-- Shim: this card now lives in <x-k.product-card>, which the four grid-card
     partials share. Kept so existing include sites need no change. --}}
<x-k.product-card :product="$product" variant="inline" />
