{{-- The single toast region. One queue for the whole panel, replacing toastr,
     ToastMagic and SweetAlert2 all firing their own styles from different pages.
     Anchored with logical properties so it never lands on the RTL sidebar. --}}
<div class="k-toasts" id="k-toasts" role="status" aria-live="polite" aria-atomic="false"></div>
