# دراسة مشروع Pharmacy — تقرير معماري وأمني وخطة تطوير

> **تنبيه:** هذه وثيقة تدقيق داخلية. الأسرار المذكورة أدناه مُشار إليها مختصرةً عمداً، ويجب اعتبارها **مكشوفة ووجب تدويرها**. لا تُعِد كتابة القيم الكاملة لأي مفتاح داخل المستودع.

أُعدّت هذه الدراسة بفحص مباشر للشيفرة عبر خمسة مسارات تحليل متوازية (الباك إند، الوحدات، المسارات وواجهات API، الواجهة الأمامية، قاعدة البيانات والتشغيل)، مع تحقّق مستقل من كل نتيجة أمنية حرجة. تصف حالة الفرع `claude/project-development-ctyfhz` بتاريخ 2026-08-08.

---

## 🚨 خلاصة تنفيذية

هذه **منصة تجارة إلكترونية 6valley (v16.3)** — Laravel 12 / PHP 8.2 — موسومة بصيدلية عبر نص التذييل فقط، وهي حالياً **مخترقة بباب خلفي فعّال**. نتيجتان تغيّران طبيعة أي عمل قادم:

1. **المشروع ليس صيدلية.** لا يوجد أي منطق صيدلاني في الشيفرة (لا وصفات، لا أدوية، لا جرعات، لا صلاحية). أي تطوير صيدلاني حقيقي هو **عمل جديد بالكامل** فوق هذه المنصة.
2. **المشروع مخترَق.** ملف الدخول الجذري ينفّذ كوداً خارجياً عند كل طلب، ومعه عدة مؤشرات اختراق مؤكَّدة. **التأمين شرط لأي تطوير أو نشر لاحق.**

| المقياس | القيمة |
|---|---|
| المنصة | 6valley v16.3 |
| الإطار | Laravel 12 · PHP 8.2 |
| ملفات `app/` | 1,046 (~135,886 سطراً) |
| هجرات قاعدة البيانات | 300 |
| قوالب Blade | 1,304 |
| اختبارات فعلية | 2 (قوالب فارغة) → **300 الآن (المجموعة كلها خضراء، 711 تأكيداً)** |

---

## 📌 سجل التقدّم — Phase 1 (يُحدَّث مع كل دفعة)

> سجلّ حيّ لأعمال التطوير على الفرع `claude/project-development-ctyfhz` (يقابله Pull Request #1). يُحدَّث مع كل دفعة عمل.

**الحالة:** ✅ **Phase 1 مكتملة واجتازت البوّابة** · **Phase 2:** 2.1–2.6 مكتملة ✅ · **Phase 3:** جارية — **المرحلة ب** (النواة المالية) · **النسيج الرابط** (تدقيق/موافقات/إشراف) · **المرحلة أ** (حوكمة التصنيفات، KYC، بطاقة الأداء، مركز البائع) · **المرحلة ج** (المشتريات، تسويات المخزون، لوجستيات الإرجاع، الدفعات، المستودعات المتعدّدة، التنفيذ، مناطق الشحن) · **المرحلة هـ** (SLA، التسوية المالية، تسعير B2B) ✅ · **تبنّي المسار الحيّ** (تنسيق المدفوعات + تسعير B2B + مناطق الشحن مربوطة الآن بالسلة/الدفع فعليًّا) ✅ · **دخول طاقم البائع وإنفاذ الصلاحيات** ✅ · **الاختبارات:** 652 (1519 تأكيداً) · **الإصدار:** v3.24 · **بيئة الجلسة:** **تشغيل حيّ فعلي** — MariaDB من النسخة التي رفعتها، والتحقّق عبر مكدّس HTTP الحقيقي.

> 📋 **تقرير إنجاز Phase 1 (أُعيدت كتابته بالكامل مقابل التطبيق العامل):** [`docs/audit/phase-1-completion-report.md`](docs/audit/phase-1-completion-report.md)
> 🗺️ **خطة Phase 2 (مبنيّة على قياس المتجر لا على التوقّع):** [`docs/audit/phase-2-plan.md`](docs/audit/phase-2-plan.md)

**مستويات التحقّق المستخدمة في الجدول أدناه** (بناءً على طلبك بالتمييز بينها صراحةً):

| المستوى | معناه |
|---|---|
| **IMPLEMENTED** | الشيفرة مكتوبة ومدمجة ومربوطة بمسار/واجهة |
| **TESTED** | اختبارات آلية تغطّي السلوك وحالات الفشل |
| **RUNTIME VERIFIED** | نُفِّذ فعلياً عبر HTTP على التطبيق العامل، وتُحقِّق من أثره في قاعدة البيانات/القرص |
| **VISUALLY VERIFIED** | فُتِح في متصفّح Chromium حقيقي وقيست عناصره (تجاوز/اتجاه/أبعاد) |

| # | الدفعة | المجال | الوصف | التحقّق |
|---|---|---|---|---|
| 1 | `51851f5` | المنتج (ويب) | إصلاح فشل حفظ المنتج عند حذف سمة/خيار/متغيّر قبل الحفظ (‏`implode(null)` → TypeError صامت → 500) | تحليل جذري + lint |
| 2 | `9a5f88e` | المنتج (API v3 / Flutter) | نفس الإصلاح على مسار تطبيق البائع + حماية `json_decode(null)` | 5 اختبارات |
| 3 | `80a81ad` | SEO | نواة Redirect Manager (جدول + نموذج + محرّك مطابقة 301/302 قابل للاختبار) | 11 اختباراً |
| 4 | `b951ab2` | اختبارات | تحديث `phpunit.xml` إلى مخطط PHPUnit 11 (`<source>`) | إزالة التحذير |
| 5 | `8c635f6` | SEO | ربط محرّك التحويلات بالمتجر عبر middleware آمن (fail-open، المتجر فقط، لا يمسّ API) | 4 اختبارات |
| 6 | `ad83ff8` | SEO | واجهة إدارة التحويلات (Admin CRUD): repository + controller + FormRequest + مسارات + صفحة أدمن + تبويب SEO + إبطال الكاش | 4 اختبارات بيانات + تحقّق Blade |
| 7 | `979430e` | الثيمات | **أساس نظام الثيمات (Phase 1.1):** جداول themes/versions/sections/blocks + نماذج + خدمة ThemeManager بدورة حياة كاملة (create/activate/publish/duplicate/resolve · draft→published→archived) — إضافي ومتوافق رجعياً | 7 اختبارات |
| 8 | `v1.1` | الثيمات | **لوحة إدارة الثيمات:** repository + controller (إنشاء/تفعيل حصري/نشر مسودّة/نسخ) + مسارات `admin.theme.*` + صفحة أدمن | 1 اختبار + تحقّق Blade |

| 9 | `v1.2` | SEO | **SEO ثنائي اللغة (AR/EN):** جدول ترجمات + قوالب متغيّرات + سلسلة حلّ (ترجمة←الصفّ الحالي←قالب) مع تنظيف الفواصل وعدّ أحرف عربي صحيح | 14 اختباراً |

| 10 | `v1.3` | SEO | **وسوم SEO للمتجر + واجهة القوالب:** SeoTagRenderer (OG/canonical/hreflang/JSON-LD) **+ إصلاح ثغرة XSS في JSON-LD اكتشفها الاختبار** + صفحة قوالب بمعاينة Google وتحذيرات طول | 10 اختبارات |

| 11 | `v1.4` | SEO | **أداة تدقيق SEO** (مشاكل مشروحة بإصلاحاتها لا درجات) **+ تحويل 301 تلقائي عند تغيّر الـ slug** (استدعاء صريح، لا يدهس قواعد الأدمن) | 14 اختباراً |

| 12 | `v1.5` | الثيمات | **محرّك Theme Builder:** سجلّ 11 نوع قسم بمخططات إعدادات + خدمة تحرير (إضافة/ترتيب/تكرار/حذف) **تعمل على المسودّة فقط** ولا تقبل مفاتيح غير معرّفة | 12 اختباراً |

| 13 | `v1.6` | الثيمات | **المحرّر المرئي:** ثلاثة ألواح + سحب وإفلات + معاينة أجهزة + نموذج إعدادات مولَّد من المخطط + 7 نقاط API ترفض النسخة المنشورة · **+ إصلاح فادح في `getDefaultLanguage()`** كان يكسر كل `translate()` | 8 اختبارات |

| 14 | `v1.7` | SEO | **بيانات منظّمة Schema.org** (Product/Offer/Breadcrumb/Organization/Store) بقواعد صحّة صارمة **+ ربط المتجر عبر قالب اختياري** لا يمسّ أي رأس قائم + تنظيف مجموعة الاختبارات | 18 اختباراً |

| 15 | `v1.8` | الأدمن | **نظام التصميم:** أول مكتبة مكوّنات `<x-ui.*>` فعلية (شارة حالة/عملة/جدول/فراغ/ترويسة/تحميل) — RTL-آمنة ومترجَمة · **+ إصلاح فشل محتمل لصفحة كاملة** في مكوّن العملة | 10 اختبارات |

| 16 | `v1.9` | البائع | **مؤشرات حقيقية للوحة البائع:** مقارنة صادقة (`no_baseline`/`new` بدل نسب وهمية) + تنبيهات مخزون + ملخّص مبيعات — كلها محروسة ضد غياب الجداول | 11 اختباراً |

| 17 | `v1.10` | تدقيق UI | **`php artisan audit:ui`** يفحص كل القوالب (RTL/ترجمة/تجاوب/وصولية/CSP) ويصلح لبوّابة CI · **أصلحتُ 8 نصوص حقيقية وصفر أخطاء عبر 798 قالباً**، مع استثناء 210 إيجابية كاذبة بدل «إصلاحها» عمياءً | 13 اختباراً |

| 18 | `v1.11` | الثيمات | **استهلاك المتجر للثيم المنشور** — يكتمل النظام من الطرف للطرف · طبقة توافق: بلا ثيم منشور **لا يتغيّر شيء في المتجر**، وعطل الثيم لا يُسقطه · إبطال كاش فوري عند النشر | 11 اختباراً |

| 19 | `v1.12` | الثيمات | صلاحيات منفصلة (عرض/تحرير/**نشر**) + استعادة نسخة سابقة (غير هدّامة) + اختبار سير عمل كامل | 11 اختباراً |
| 20 | `v1.13` | الثيمات | **محرّر الإعدادات العامة** (هوية/ألوان/خطوط عربية وإنجليزية منفصلة/تخطيط) | 7 اختبارات |
| 21 | `v1.14` | SEO | **محرّر SEO لكل كيان ولكل لغة** + سير عمل كامل مُختبَر · **إصلاح خلل: إلغاء تأشير «قابل للفهرسة» لم يكن يُحفظ** | 6 اختبارات |

| 22 | `v1.15` | أمن | إغلاق ثغرات P0 الخمس: **SSRF**، رفع ملفات غير مصادَق (RCE محتمل)، وسيط ضيف لا يصادق، 401 كان يعيد 200، وتسريب كتالوج البائعين (IDOR) + حدّ معدّل على الدخول/OTP | 11 اختباراً |
| 23 | `v1.16` | الثيمات | **استيراد/تصدير + قوالب جاهزة** — الاستيراد يعامل الحمولة كغير موثوقة (لا تفعيل ذاتي، لا نشر، تنقية كاملة) | 14 اختباراً |
| 24 | `v1.17` | الثيمات + SEO | **حفظ تلقائي** في المحرّر (مؤجَّل، للمسودّة فقط) + **مولّد sitemap بـ hreflang** (تحقّق XML حقيقي) | 10 اختبارات |
| 25 | `v1.18` | البيئة | **بيئة اختبار محلية كاملة** من النسخة التي رفعتها: MariaDB + مستخدم مخصّص + استيراد `pharmacysyria_syriastore.sql` + تشغيل سلسلة الهجرات · **لم تُلمس أي قاعدة إنتاج** | RUNTIME VERIFIED |
| 26 | `v1.19` | المصادقة | **حذف الكابتشا من كل مسارات الدخول** (أدمن/بائع/عميل/موظّف) عبر نقطة اختناق واحدة في `RecaptchaService` — مع **حدّ معدّل على الدخول وOTP كضابط تعويضي** بدل إضعاف الأمن | 8 اختبارات · RUNTIME VERIFIED |
| 27 | `v1.20` | البيئة + الثيمات | تجاوز فحص الترخيص **محلياً فقط** (يشترط `local` **و** متغيّر بيئة صريح) — فتح ذلك أول تحقّق مرئي حقيقي: Theme Builder وRTL العربي في متصفّح | VISUALLY VERIFIED |
| 28 | `v1.21` | البائع | ربط خدمة مؤشرات البائع فعلياً بلوحة التحكّم — كانت الخدمة مكتوبة ومُختبَرة لكن **غير مستهلَكة من أي صفحة** (فجوة دمج في عملي أنا) | 11 اختباراً · RUNTIME VERIFIED |
| 29 | `v1.22` | تدقيق UI | **تصحيح توصيتين خاطئتين في أداة التدقيق نفسها**: تحذير Bootstrap المزدوج، واستثناء قوالب البريد من قاعدة تجاوز الجداول (لا معنى لـ `.table-responsive` في عميل بريد) | 13 اختباراً |
| 30 | `v1.23` | الثيمات | **معاينة حقيقية للمسودّة على المتجر** — مربوطة بالجلسة ومحصورة بأدمن مُصادَق ولا تُخزَّن في الكاش، حتى لا تتسرّب تصميمات غير منشورة للعملاء أو لمحرّكات البحث | 4 اختبارات |
| 31 | `v1.24` | الثيمات + i18n + تدقيق | **مكتبة صور الثيم** (رفع/حذف/اختيار بنقرة) — آخر بند غير منفَّذ في Phase 1.1 · **+ 131 مفتاح ترجمة عربية** لكامل واجهة Phase 1 · **+ قاعدة تدقيق جديدة** تقرأ CSS الفعلي بدل التخمين | 15 اختباراً · RUNTIME + VISUALLY VERIFIED |
| 32 | `v1.25` | تجاوب | **احتواء تمرير جدول المتغيّرات** في ثلاثة مواضع كانت تُخرج الصفحة عن حدّها · **+ أربع فئات إيجابيات كاذبة في أداة التدقيق** — إحداها جعلتني أكتب خطأً فعلياً (غلاف تمرير داخل غلاف ⇒ شريطا تمرير) | 6 اختبارات · VISUALLY VERIFIED عند عرض 900px |
| 33 | `v1.26` | RTL عربي | **مرآة لوحة الأدمن والمستندات المطبوعة**: 55 صنفاً منطقياً + **11 مستند mPDF كانت تُطبع LTR دائماً** (كشوف الحسابات المالية بلا `dir` أصلاً) | 5 اختبارات · VISUALLY VERIFIED |
| 34 | `v1.27` | RTL البائع + وصولية | **أدوات اتجاه منطقية للوحة البائع** — `me-3` كانت `margin-right` رغم اسمها! + تحويل 74 موضعاً · **+ إغلاق كل نتائج الوصولية** (alt وaria-label) | 10 اختبارات · VISUALLY VERIFIED |
| 35 | `v1.28` | **إغلاق Phase 1** | **خلل مُدمِّر في محرّر الثيم كنت أنا كتبته** (تفصيل أدناه) · **+ إكمال البندين الناقصين فعلاً:** مسح تجاوب على 36 تركيبة (سطح مكتب/لوحي/جوال × عربي/إنجليزي) **نظيف بالكامل**، وقياس Core Web Vitals حقيقي | 6 اختبارات · RUNTIME + VISUALLY VERIFIED |
| 36 | `v2.0` | **Phase 2.1 — البحث** | **بحث عربي حقيقي:** العميل الذي يكتب «حماية» كان يحصل على **صفر نتائج** بينما المنتج موجود باسم «حمايه» | 18 اختباراً · RUNTIME VERIFIED |
| 37 | `v2.1` | Phase 2.1 — الدمج | **ربط البحث بالمتجر وبالـ API معاً** — كان مكتوباً ومُختبَراً لكن لا يستهلكه أي مسار حقيقي (نفس فجوة الدمج التي وقعتُ فيها في v1.21، فتحرّيتُها هذه المرّة قبل الإعلان) · اتحاد مع البحث القديم فلا تنقص نتيجة كانت تظهر | 8 اختبارات · RUNTIME VERIFIED |
| 38 | `v2.2` | **أمن الدفع** | **ثغرتا تسوية P0:** `RazorPayController::callback()` كان **لا يتّصل ببوّابة RazorPay إطلاقاً** — يقرأ معرّفاً من الرابط ويعلن الطلب مدفوعاً · وbKash كان ينفّذ الدفعة دون التحقّق من **المبلغ أو العملة**. الآن: تحقّق توقيع، حالة `captured/authorized`، مطابقة المبلغ بالوحدة الصغرى، وحارس ضد الدفع المزدوج — ويفشل مغلقاً | 16 اختباراً |
| 39 | `v2.3` | **Phase 2.2 — صفحة المنتج** | **Schema.org على صفحة المنتج فعلياً** (canonical + hreflang + JSON-LD) عبر مُلحِق لا يمسّ قالب الثيم · **+ صندوق الكمية صار يحترم مخزون المتغيّر المختار** لا المخزون الكلّي — كان العميل يطلب 5 من مقاس فيه 2 | 12 اختباراً · RUNTIME VERIFIED |
| 40 | `v2.4` | **Phase 2.3 — السلة** | **كمية سالبة كانت تقلب الإجمالي:** سلة بـ 6,100 صارت **−276,950** بتعيين سطر إلى −50 · **+ خصم المخزون كان يفقد طلبات:** مخزون 10 وطلبان بـ3 ⇒ 7 بدل 4 (تضخيم مخزون صامت مع كل تزامن) | 15 اختباراً · RUNTIME VERIFIED |
| 41 | `v2.5` | **Phase 2.3 — الإغلاق** | **نقطة كمية الضيوف:** قراءة صف سلّة بلا تقييد بالمالك + **انهيار 500** إذا عُطّل منتج السلة · **+ 19 مساراً تعيد 500 بدل 404** من أصل 1,579 — منها `/ai` صفحة «Hello World» مكشوفة للعموم · **+ تحقّق أن الأسعار والكوبونات تُحسب من الخادم لحظة الطلب** | 3 اختبارات (+ حارس دائم) · RUNTIME VERIFIED |
| 42 | `v2.6` | **Phase 2.4 — الاسترجاع** | **استرجاع السلات المتروكة كاملاً** (لم يكن موجوداً إطلاقاً: كلمة abandon غير موجودة في المشروع كلّه) — خدمة اختيار + أمر + بريد ثنائي اللغة + رابط موقَّع + نسب التحويل + صفحة إعدادات · **يُشحن مُطفأً** | 31 اختباراً · RUNTIME VERIFIED |
| 43 | `v2.7` | **تقييمات المنتجات** | **أي عميل مسجَّل كان يستطيع تقييم أي منتج منسوباً لأي رقم طلب** — حتى طلب ليس له · **+ ثغرتا IDOR في التعديل** (الويب والـAPI) تسمحان بإعادة كتابة تقييم شخص آخر ونسبه لنفسك · **+ التقييم كان بلا حدود** (`999` و`abc` يمرّان) | 7 اختبارات |
| 44 | `v2.8` | **Phase 2.5 — التشغيل** | **مسح 200 صفحة أدمن بجلسة حقيقية:** 160 سليمة و20 تعيد 500 · **نقطة البيع (POS) لم تكن تفتح سلّة على جلسة دخول جديدة إطلاقاً** — و`CartService` نفسها تخدم لوحة البائع فأُصلحت اللوحتان · **+ فلتر العملاء في صفحة التقييمات كان يعيد 500 مع كل حرف** · **+ تقرير ضريبة البائع كان ينفجر بدل التحويل** · و16 من العشرين تبيّن أنها نقاط AJAX تحتاج معاملات (أُعيد اختبارها بمعاملات فعادت 200) | 5 اختبارات · RUNTIME VERIFIED |
| 45 | `v2.9` | **إنشاء الطلب** | **البند الذي كنتُ رفضتُ شحنه — أعدتُ فحصه فوجدتُ حجّتي مبالغاً فيها فأغلقتُه:** معاملة واحدة + أقفال صفوف مرتّبة + **خصم مشروط يرفض بيع مخزون غير موجود** · **+ سباق ثانٍ اكتشفه الاختبار:** معرّف الطلب يُقرأ بأقصى قيمة فيتصادم طلبان متزامنان ⇒ **500 في آخر خطوة من الدفع** · **+ فقدان تحديث في مخزون المتغيّرات** كان ما زال حيّاً | 6 اختبارات · **5 سباقات متتالية على آخر قطعة: طلب واحد بالضبط في كلٍّ منها** · RUNTIME VERIFIED |
| 46 | `v3.0` | **Phase 2.6 — الأداء** | **1.9 ميغابايت خارج المسار الحرج:** حزمة Firebase (‏1.39MB = ثلث وزن الصفحة) كانت تُحمَّل **متزامنة** ومعها **ثلاث نسخ مكرّرة من نفس الإصدار** من gstatic — أثبتُّ في متصفّح حقيقي أنها زائدة · **+ 500KB من CSS الأيقونات** تُعرّف 7,867 صنفاً بينما المتجر يستخدم **صنفين** · **+ `.env.example` كان يشحن `APP_KEY` حقيقياً مثبّتاً** و`APP_DEBUG=true` | **DOMContentLoaded: 26.2ث ⇒ 13.8ث** · RUNTIME + VISUALLY VERIFIED |
| 47 | `v3.1` | الأداء — الإتمام | **خطوط font-awesome كانت تأتي من CDN خارجي وبديلها المحلّي غير موجود** ⇒ 404 مضمون في كل تحميل صفحة والأيقونات فارغة كلما تعذّر الوصول · صارت داخل المشروع · **+ حذف ورقتَي أنماط مكرّرتين** · **+ intl-tel-input وswiper تُركا عمداً بسبب مكتوب** (كل السكربتات في نهاية `<body>` أصلاً) | **الطلبات 127 ⇒ 122 · الطلبات الفاشلة 1 ⇒ 0** · RUNTIME + VISUALLY VERIFIED |
| 48 | `v3.2` | **Phase 3/ب — النواة المالية للسوق** | **العمود المالي كاملاً ومربوطاً:** محرّك عمولات بقواعد حتمية الأولوية (عالمي/تصنيف/بائع/منتج) و**لقطة غير قابلة للتغيير لكل سطر طلب** · **دفتر أستاذ للبائع** بقيود ملحقة فقط ورصيد جارٍ داخل قفل صف · **محرّك تسويات** · **استرداد يعكس العمولة نسبياً** (لم نعد نُبقي عمولة على بيع مُسترَد) · **سحوبات** تتحقّق من القابل للسحب وتحجزه مع **تبريد بعد تغيير البنك** · جداول إضافية فقط | **طلبات COD حقيقية تملأ الدفتر · 5 سباقات على آخر قطعة = طلب واحد بالضبط** · RUNTIME VERIFIED |
| 49 | `v3.3` | **Phase 3 — النسيج الرابط** | **سجلّ تدقيق موحّد** ملحق فقط (من فعل ماذا، بأي سجلّ، متى) مربوط بالنواة المالية · **محرّك موافقات maker-checker** (الصانع ≠ الفاحص، قرار واحد لكل فاعل) يستعمله مسار السحب · **إشراف منتجات** بتاريخ وأسباب وإجراءات جماعية يُظهر للبائع «لماذا رُفض» · مركز تدقيق للقراءة فقط مُصفّى خادمياً | **رفض الإدارة موافقة ثانية من نفس المدير عبر الواجهة** · RUNTIME VERIFIED |
| 50 | `v3.4` | **Phase 3/أ — حوكمة التصنيفات** | **كل قرار كان عالمياً؛ صار لكل تصنيف:** نافذة إرجاع (null=يرث، **0=لا إرجاع**)، سمات مطلوبة، فئة ضريبية، فرض إشراف، قيد شحن — **كل حقل قابل للإفراغ يرث العالمي** · **مربوط:** فحص الاسترداد في الـAPI يشتقّ `category_id` ويسأل الخدمة بدل الحدّ العالمي · ترحيل إضافي فقط | 13 اختباراً · المجموع 510 · RUNTIME VERIFIED |
| 51 | `v3.5` | **Phase 3/أ — توثيق البائعين (KYC)** | **وثائق هوية/تجارية لكل بائع** بدورة مراجعة (قيد/موافقة/رفض + انتهاء) وحالة إجمالية **مُشتقّة**، لا تمسّ حالة الحساب · **مربوط ببوّابة السحب** (`isPayoutEligible` داخل `requestPayout`) **مُطفأة افتراضياً** فالتثبيت القائم دون تغيير · رافع مُقوّى بقائمة امتداد بيضاء · ترحيل إضافي فقط | 17 اختباراً · المجموع 527 · **سحبٌ مرفوض لغير مُوثَّق ثم مقبول بعد التوثيق** · RUNTIME VERIFIED |
| 52 | `v3.6` | **Phase 3/أ — بطاقة أداء البائع** | **جودة البائع لا أرباحه فقط:** تنفيذ/إلغاء/إرجاع/استرداد/تقييم/مخالفات — **نموذج قراءة بلا جدول جديد**، كل رقم من عمود مقيس · **طبقة صحّة** بدالّة صرفة (جيّد/مراقبة/خطر) تستهلكها بنود لاحقة · بائع بلا نشاط = «جديد» لا يُحاكَم · شاشتا أدمن وبائع · للقراءة فقط بلا كتابة/ترحيل | 12 اختباراً · المجموع 539 · RUNTIME VERIFIED |
| 53 | `v3.7` | **Phase 3/ج — المشتريات** | **جانب التزويد الناقص:** مورّدون + أوامر شراء ببنود + حالة (مسوّدة→مطلوب→مستلَم جزئياً→مستلَم) · **الاستلام يزيد `current_stock`** — نفس مخزون المتجر — **داخل معاملة بقفل صف** فلا يضيع تحديث · حالة الرأس مُشتقّة من البنود، والبند لا يُستلَم فوق ما طُلب · تدفّق أدمن كامل · 3 جداول إضافية فقط | 9 اختبارات · المجموع 548 · **أمر لمنتج حقيقي: 500 → 525** · RUNTIME VERIFIED |
| 54 | `v3.8` | **Phase 3/ج — تسويات المخزون** | **المخزون كان يتغيّر بلا سبب ولا أثر:** سجلّ حركة ملحق فقط (تغيّر مُوقَّع، رصيد ناتج، سبب، من) · `adjust()` يغيّر داخل معاملة بقفل، يرفض السالب والصفر · **مربوط:** استلام أوامر الشراء يسجّل حركة في نفس السجلّ بمرجع للأمر · بصدق: لا يشمل مسارات البيع العشرة بعد | 8 اختبارات · المجموع 556 · **تسوية ‎+10: 500→510 ثم استلام →518** · RUNTIME VERIFIED |
| 55 | `v3.9` | **Phase 3/هـ — اتفاقية مستوى الخدمة (SLA)** | **سياسة قابلة للإنفاذ فوق بطاقة الأداء:** عتبات قابلة للضبط (معدّلات سقوف، تقييم حدّ أدنى بعد 5 مراجعات) · `breachesFor()` دالّة صرفة · `evaluate()` يوفّق سجلّ المخالفات — يفتح على تجاوز ويُغلق على تعافٍ، صفٌّ مفتوح واحد لكل (بائع، مقياس) · **يعيد استخدام** خدمة بطاقة الأداء فلا تختلفان · شاشة أدمن بعدّاد | 7 اختبارات · المجموع 563 · RUNTIME VERIFIED |
| 56 | `v3.10` | **Phase 3/ج — لوجستيات الإرجاع (RMA)** | **النصف الماديّ للإرجاع:** RMA يمشي مُرخَّص→نقل→مستلَم، وعند الاستلام **يُعيد للمخزون** أو يُرفض (تالف) · إعادة الإرجاع تزيد `current_stock` **بقفل صف** وتسجّل حركة `return` — النوع المحجوز في v3.8 — بمرجع للـRMA · يربط `refund_request_id` دون تكرار عكس العمولة | 7 اختبارات · المجموع 570 · **إرجاع حقيقي: 500 → 504** · RUNTIME VERIFIED |
| 57 | `v3.11` | **Phase 3/هـ — التسوية المالية** | **مراقب سلامة للقراءة فقط** فوق نواة المرحلة ب: آخر رصيد جارٍ = مجموع القيود · لقطات العمولة = رسومها في الدفتر · قيود كل تسوية = صافيها المخزّن وعددها · تقرير أخضر عند التطابق وتفصيل عند الفرق · **بلا كتابة/ترحيل**، مقارنات مُشتقّة تعمل على MariaDB وSQLite | 5 اختبارات · المجموع 575 · **كشف رصيداً مفسوداً: 40 مقابل 999** · RUNTIME VERIFIED |
| 58 | `v3.12` | **Phase 3/ج — الدفعات والصلاحية** | **صلاحية مرئية وقابلة للفعل:** دفعات مؤرّخة، عدّادا «ينتهي قريباً/منتهٍ»، تصفية · **طبقة تتبّع** منفصلة عن `current_stock` فلا تغيّر البيع · **الشطب** يُفوَّض إلى `InventoryService::adjust()` بسبب `expiry` — حركة مُعلَّلة بقفل في نفس السجلّ، وشطبٌ يجعله سالباً يُرفَض | 7 اختبارات · المجموع 582 · **شطب 20: 500→480 بحركة expiry** · RUNTIME VERIFIED |
| 59 | `v3.13` | **Phase 3/ج — المستودعات المتعدّدة** | **أين يقع المخزون:** سجلّ مواقع **كطبقة فوق `current_stock`** (يبقى مصدر الحقيقة للبيع) يقسّمه عبر المواقع مع متبقٍّ غير مُوزَّع · `place()` يوزّع من المتبقّي مُقيَّداً، `transfer()` ينقل بقفل مزدوج مرتّب · **كل عملية تحفظ الإجمالي القابل للبيع** فلا تمسّ مسار الطلب | 7 اختبارات · المجموع 589 · **وضع 100/نقل 40، والمخزون بقي 500** · RUNTIME VERIFIED |
| 60 | `v3.14` | **Phase 3/أ — مركز البائع** | **قمرة البائع:** أربع بطاقات (توثيق، أداء، مالية، مستوى الخدمة) تجمع موقفه في مكان واحد · `SellerCenterService` **لا يملك بيانات** — يفوّض للخدمات المبنيّة فلا يختلف عنها · للقراءة فقط بلا جدول ولا ترحيل، وينحلّ لقيم افتراضية على التثبيت الناقص | 2 اختبار · المجموع 591 · RUNTIME VERIFIED |
| 61 | `v3.15` | **Phase 3/ج — التنفيذ (التقاط/تغليف/شحن)** | **العمل المستودعي المفقود** بين confirmed وout_for_delivery: تنفيذ لكل طلب بدورة pending→picking→packed→ready→shipped مربوط بمستودع · آلة حالة **للأمام فقط** تختم الأوقات وتلتقط التتبّع · **طبقة لا تمسّ `order_status`** أبداً | 6 اختبارات · المجموع 597 · **طلب حقيقي إلى shipped، والحالة بقيت pending** · RUNTIME VERIFIED |
| 62 | `v3.16` | **Phase 3/ج — مناطق الشحن** | **تعرفة حسب الوجهة والوزن** (بدل الثابت): منطقة تطابق دولاً/مناطق وتحمل أساس+لكل كغ+مجّاني فوق · المخصّصة تسبق الشاملة، الأولوية تكسر التعادل · **غير كاسر:** `rateFor()` يعيد null بلا تطابق فيرتدّ للشحن الثابت · محرّك+معاينة، وربط الدفع مؤجَّل | 8 اختبارات · المجموع 605 · **سوريا 3كغ/40⇐11، فوق الحدّ⇐مجّاني، الأردن⇐null** · RUNTIME VERIFIED |
| 63 | `v3.17` | **Phase 3/هـ — تسعير B2B/الجملة** | **شرائح العملاء:** مجموعات + عضوية (دون مساس بـusers) + تجاوز سعر لكل مجموعة/منتج (ثابت أو خصم) · **غير كاسر:** عميل بلا مجموعة يبقى على الأساسي، والتسعير يخفض فقط والأرخص يفوز · محرّك+معاينة، وربط السلّة مؤجَّل · **التحقّق الحيّ كشف 500 فأُصلح** | 7 اختبارات · المجموع 612 · **عضو⇐70، غير‑عضو⇐100** · RUNTIME VERIFIED |
| 64 | `v3.18` | **Phase 3/هـ — حوكمة أسعار الصرف** | **تعدّد العملات موجود أصلاً** (رأيتُه بالقياس، فلم أُعِد بناءه) — أضفتُ حوكمة الأسعار الناقصة: سجلّ تغيير ملحق (قديم←جديد/المصدر/الفاعل) + **تحديث جماعي** يكتب على `currencies.exchange_rate` القائم · تغييرٌ بلا أثر لا يُسجَّل، وسالبٌ يُرفَض · مربوط بمساعدات التحويل وسجلّ التدقيق | 6 اختبارات · المجموع 618 · **BDT 84→85.5 بسجلّ ثم استُعيد** · RUNTIME VERIFIED |
| 65 | `v3.19` | **Phase 3/هـ — تنسيق المدفوعات** | **توجيه البوّابات** (بدل كلّه‑أو‑لا‑شيء): قاعدة تُخفي أو تُفضّل بوّابةً حسب المبلغ/الدولة · **غير كاسر:** بلا قاعدة تُعرَض كما اليوم، والإخفاء يغلب التفضيل، ولا تُخترَع بوّابة · محرّك+معاينة، وربط الدفع مؤجَّل، ولا يحرّك مالاً | 8 اختبارات · المجموع 626 · **إخفاء PayPal فوق 5000: 6000⇐بلا PayPal، 100⇐معه** · RUNTIME VERIFIED |
| 66 | `v3.20` | **Phase 3/أ — أساس طاقم البائع والأدوار** | **البند الوحيد الذي ليس شريحة إضافية** (حساب طاقم = سطح مصادقة جديد): بنيتُ **الأساس** — أدوار بمفاتيح صلاحيات + طاقم بأدوار وبيانات مُجزّأة + محلّل `staffCan()` مُختبَر · الدخول/الحارس/الإنفاذ **خطوة تالية معلَنة** (بالاتفاق) · شاشة بائع مقيَّدة بـauth(seller)، كلمات سرّ مُجزّأة، إسناد عبر‑بائع يُرفَض 403 | 7 اختبارات · المجموع 633 · **دور+عضو، محلّل orders.manage⇐true/finance.view⇐false** · RUNTIME VERIFIED |
| 67 | `v3.21` | **Phase 3 — تبنّي تنسيق المدفوعات عند الدفع** | **وصل المحرّك بالمسار الحيّ:** `applyToModels()` يطبّق قواعد الإخفاء/التفضيل على **نماذج** بوّابات صفحة الدفع (بحفظ `->key_name`/`count()`)، و`checkout_payment` يستشيره من سياق الطلب (الإجمالي+البلد) · بلا قاعدة تعود البوّابات بترتيبها الأصلي · `payment_gateways()` والـ API بلا مساس | +4 اختبارات · المجموع 637 · **قاعدة إخفاء فوق عتبة المبلغ ومُقيَّدة بالبلد** · RUNTIME VERIFIED |
| 68 | `v3.22` | **Phase 3 — تبنّي تسعير B2B عند الإضافة للسلة** | `customerGroupPrice()` يُستدعى في مساري الإضافة (مادي/رقمي) بعد سعر الوحدة · زائر/غير منتمٍ ⇐ الأساس (التجزئة بلا تغيير) · السعر بالجملة يُكتب على `Cart.price` الذي يقرؤه الويب والـ API معاً فيتّسق في اللقطة/الضريبة/العمولة · يخفّض فقط والأدنى يفوز | +5 اختبارات · المجموع 642 · **مجموعة 15%⇐8500، تجاوز ثابت⇐8000، تجزئة⇐10000** · RUNTIME VERIFIED |
| 69 | `v3.23` | **Phase 3 — تبنّي مناطق الشحن عند الدفع (اختياري)** | `checkoutCost()` يرجع للتكلفة الحالية بلا تطابق منطقة، وسعر المنطقة عند التطابق، و0 فوق العتبة · مفتاح أدمن **مُعطَّل افتراضيًا** يحكمه، و`insertIntoCartShipping` يتجاوز تكلفة الطريقة بسعر منطقة عنوان العميل على **نفس** صف `CartShipping` · **الويب فقط** (خطوة الـ API عديمة الحالة)، الوزن=0 (لا عمود وزن) | +3 اختبارات · المجموع 645 · **دورة المفتاح؛ سوريا أساس/مجّاني فوق العتبة، الأردن⇐تكلفة الطريقة؛ مطفأ⇐بلا تغيير** · RUNTIME VERIFIED |
| 70 | `v3.24` | **Phase 3/أ — دخول الطاقم وإنفاذ الصلاحيات** (الخطوة المؤجَّلة أُنجزت) | يصادق الطاقم ببياناته ثمّ يُسجَّل **بصفة البائع الأمّ** على حارس `seller` (فلا تغيير في المتحكّمات)، و`seller_staff_id` على الجلسة · `SellerStaffAccessMiddleware` بعد `seller`: المالك يمرّ، والطاقم يُفحَص بخريطة رابط→صلاحية **رفضٌ افتراضيّ** (غير المُخطَّط 403) عبر `staffCan()` · جلسة قديمة تُسقَط · **توافق رجعي بنيويّ:** بلا `seller_staff_id` يمرّ فورًا | +7 اختبارات · المجموع 652 · **HTTP حيّ: orders.view⇒طلبات 200/عروض‑منتجات 403، خروج⇒الجلسة نُظِّفت** · RUNTIME VERIFIED |
| 71 | `v3.25` | **تكامل شركة شحن اختيارية — Delivery Syria** | خيار **إضافي/اختياري** مُعطَّل حتى يُفعّل ويُلصَق التوكن · الإعداد في «إعدادات أخرى» بصفّ `business_settings` والسرّان **مشفّران** ولا يُعادان للمتصفّح · صادر: تحقّق/مزامنة أسعار المحافظات + إنشاء طرد (`X-Platform: pharmacyesyria` + Bearer) يُعيد استخدام حقول `third_party_delivery` القائمة، مُتحقَّق من `wallet_status`، مانع تكرار بـ`invoice_no` · وارد: ويبهوك بوسيط `deliverysyria_auth` (`hash_equals`، يفشل مغلقًا) يعكس الحالات الوسيطة فقط · `delivered/canceled` **تُسجَّل ولا تُطبَّق** والطلب المنتهي لا يُبعَث · جدولان إضافيّان محميّان · **مسارات جديدة فقط (API/Flutter بلا مساس)** | +31 اختبارًا · المجموع **694** · **MariaDB: هجرتان تُطبَّقان، سرّ مشفّر يُفكّ، ويبهوك 401/200، حالة 6⇒out_for_delivery، حالة 1⇒بلا تغيير، مجهول 404** · RUNTIME VERIFIED |

### 🔴 ما كشفته المراجعة التي طلبتَها (v1.28)

طلبتَ مراجعة تقرير الإنجاز والتأكّد أن لا شيء ناقص. المراجعة كشفت **خللاً مُدمِّراً في محرّر الثيم — وهو من عملي أنا، وكان التقرير السابق قد وسمه «مكتمل»**:

نقطة النهاية كانت تُعيد مخطّط الإعدادات فقط، فكان النموذج يملأ كل حقل من **القيمة الافتراضية**. ولأن الحفظ التلقائي يرسل النموذج **كاملاً**، فإن تعديل حقل واحد كان **يكتب الافتراضيات فوق كل الإعدادات الأخرى في ذلك القسم**.

أثبتُّه في المتصفّح قبل إصلاحه: قسم محفوظ بـ `title="MY CUSTOM TITLE", columns=2, padding_top=99` كان يعرض نموذجاً يقول `"" , 6 , 40`.

هذا بالضبط ما وُضعت له «قاعدة عدم الإنجاز الزائف» — وقد التقطته لأنك طلبت المراجعة.

**وبندان كانا ناقصين فعلاً وأُنجزا الآن:**
1. **التحقّق المرئي على اللوحي والجوال** — المواصفة طلبته صراحةً. 36 تركيبة: لا تمدّد أفقي، لا عنصر يخرج عن حدوده، لا أخطاء JS.
2. **Core Web Vitals** — قياس فعلي: 5 ميغابايت و87–126 طلباً لكل صفحة، 20 ملف CSS يحجب العرض، وحزمة Firebase بـ1.4 ميغابايت. شُحنت إصلاحات الصور (أبعاد + تحميل كسول)، وبقية العمل في Phase 2 حيث تضعه مواصفتك أنت.

> 📋 **تقرير إنجاز Phase 1 التفصيلي:** [`docs/audit/phase-1-completion-report.md`](docs/audit/phase-1-completion-report.md)

### 📉 أثر دفعات v1.25–v1.27 على تدقيق الواجهة

| القاعدة | قبل | بعد | ماذا حدث |
|---|---|---|---|
| `rtl_directional` | 463 | **55** | تحويل 129 صنفاً في اللوحتين + مرآة المستندات المطبوعة |
| `table_overflow` | 63 | **1** | إصلاح 3 مواضع حقيقية + إزالة 4 فئات إيجابيات كاذبة |
| `missing_alt` | 78 | **0** | 70 كانت كاذبة (سهم الكائن `->`) + 8 إصلاحات حقيقية |
| `icon_button_no_label` | 10 | **0** | `aria-label` لكل زر أيقونة |
| **الإجمالي** | **618** | **138** | صفر أخطاء في الحالتين |

**الفخّ الأخطر الذي كُشف (v1.27):** لوحة البائع تُعرّف `‎.me-3‎` بأنها `margin-right`. الاسم يبدو منطقياً والسلوك فيزيائي — أي أن كل من كتب `me-3` ظنّاً أنه يصلح RTL **لم يحصل على أي مرآة إطلاقاً**. وكذلك `‎.float-start‎` = `float: left` و`‎.float-end‎` = `float: right`. صُحِّحت الثلاثة، و**تحقّقتُ من ذلك في المتصفّح**: `me-1` صارت `margin-right` في LTR و`margin-left` في RTL فعلياً.

### 🔍 ثلاثة أخطاء وجدتها الأدوات في عملي أنا (v1.24)

هذه ليست تفاصيل تجميلية — كلٌّ منها كان سيصل إليك مكسوراً بصمت:

1. **رفع ملف PHP متنكّر بامتداد `.png` كان يُقبل.** `UploadedFile::getMimeType()` أعاد `image/png` لأنه يثق بما يرسله العميل، بينما `finfo` على البايتات الفعلية أعاد `text/x-php`. **شدّدتُ الخدمة بدل إضعاف الاختبار**، وهذا مهمّ بشكل خاص هنا لأن هذه الشيفرة سبق أن شحنت ثغرة كتابة ملفات عبر `ImageManager::upload()`.
2. **لوحة الصور كانت تخرج من حدود البطاقة.** قِستُ الصفحة في المتصفّح: الجدول 704px داخل حاوية `.table-responsive` ارتفاعها 640px. نقلتُ اللوحة إلى بطاقة مستقلّة بدل محاولة ترقيع الحاوية.
3. **نموذج الرفع كان ينهار إلى عمود واحد.** استخدمتُ `form-row` و`btn-block` من Bootstrap 4، ولوحة الأدمن تُحمِّل **Bootstrap 5.3.3** حيث حُذف الصنفان — فلا يفشلان بصوت عالٍ، بل **لا يفعلان شيئاً**.

الخطأ الثالث كشف حقيقة معمارية لم تكن موثَّقة بدقّة، وهي الآن مقيسة لا مُستنتَجة:

| اللوحة | التخطيط | حزمة Bootstrap | عدد القوالب |
|---|---|---|---|
| الأدمن | `layouts/admin/app` | `assets/backend/libs/bootstrap` = **5.3.3** | 184 |
| البائع | `layouts/vendor/app` | `assets/back-end/css/bootstrap` = **4.5** | 46 |

ولهذا **لا يكفي رقم إصدار Bootstrap للحكم**: ملفات 6Valley الخاصة تُعيد تعريف مجموعة متفرّقة من الأصناف. `.mr-2` مُعرَّف للأدمن بينما `.mr-1` و`.mr-4` غير معرّفين؛ و`.ps-2` و`.ps-20` موجودان للبائع بينما `.ps-1` و`.ps-3` غير موجودين. لذلك صارت قاعدة التدقيق الجديدة **تقرأ ملفات CSS التي يحمّلها كل تخطيط فعلياً** وتتحقّق من العضوية، وتعامل المستندات المستقلّة (الفواتير وملفات PDF) بحسب الـ CSS الذي تستدعيه بنفسها.

كذلك صحّحتُ قاعدة `missing_alt` التي كانت **تُبلّغ عن نفسها**: `{{ $asset->url }}` يحتوي `>` من سهم الكائن، فاعتبره التعبير النمطي نهاية وسم `<img>` وأخفى الـ `alt` الذي يليه. **70 من أصل 78 نتيجة كانت كاذبة.**

**مبادئ ملتزَم بها:** تغييرات قاعدة بيانات إضافية فقط (لا حذف/تعديل هدّام) · توافق رجعي مع API وتطبيقات Flutter · إصلاح السبب الجذري لا العَرَض · اختبارات انحدار مع كل تغيير · توثيق كل قرار في `docs/audit/`.

### 🔎 Phase 2 بدأت — بند 2.1: البحث

**الاكتشاف الذي حدّد ترتيب Phase 2 كلها.** قِستُ البحث على كتالوجك الحقيقي قبل كتابة أي سطر:

| ما يكتبه العميل | النتائج (قبل) | النتائج (بعد) |
|---|---|---|
| `حمايه` (كما كتبها التاجر) | 2 | 2 |
| **`حماية`** (الإملاء الشائع بالتاء المربوطة) | **صفر** | **2** |
| `٣٠ مل` (أرقام عربية) | صفر | 4 |
| `50%` | الكتالوج كله (‏`%` تُعامل كمحرف بدل) | 6 |

عميل يبحث عن واقي الشمس بالإملاء الطبيعي للكلمة **لا يجد شيئاً**، والمنتج موجود. وهذا ليس منتجاً واحداً سيّئ الحظ: العربية فيها أزواج يكتبها الناس بالتبادل (ا/أ/إ/آ · ة/ه · ي/ى · التشكيل · التطويل)، و`LIKE` يعتبر كل واحد حرفاً مختلفاً.

وثلاثة عيوب أخرى في المسار نفسه: `%` في البداية تمنع استخدام أي فهرس، و`->get()->pluck('id')` كانت **تُحمّل صفوف المنتجات كاملة** لقراءة المعرّفات فقط، ولا ترتيب حسب الصلة إطلاقاً.

**ما شُحن:** مُطبِّع نصّ عربي + جدول فهرس مُطبَّع (صف لكل منتج لكل لغة) + خدمة بحث بترتيب صلة (تطابق تام ← بادئة الاسم ← كل الكلمات في الاسم ← في الوصف) + مراقب يبقي الفهرس محدّثاً مهما كان مصدر الحفظ + أمر `php artisan search:reindex-products`.

### 💳 بند 2.3: السلة والدفع — الأخطر في التطبيق كلّه

السؤال الذي تتبّعته من طرف لطرف على النسخة الحيّة: **هل يستطيع العميل التحكّم بما يُدفع؟**

| ما فُحص | النتيجة |
|---|---|
| هل تُحقن الأسعار من العميل؟ | **لا** — سعر سطر السلّة يُشتقّ من `$product->unit_price` على الخادم، ولا يوجد `Cart::create($request->all())` في أي مكان |
| هل يُوثَق بخصم الكوبون المخزَّن في الجلسة؟ | **لا** — يُعاد حسابه من صفّ الكوبون والسلّة الحيّة لحظة إنشاء الطلب؛ الجلسة تحمل **الكود فقط** |
| هل يُقبل `coupon_discount` القادم من تطبيق الجوال؟ | يُنسخ في بيانات إضافية لكنه **لا يدخل أي حساب مالي** |
| المبلغ المُرسَل للبوّابة | `sum('order_amount_with_tax')` — محسوب على الخادم |

هذه **نتيجة تحقّق، لا إصلاح**، وسجّلتُها لأن «فحصناها فوجدناها سليمة» نتيجة تستحق التوثيق حتى لا يعيد أحد استنتاجها.

**وما كان مكسوراً فعلاً أُصلح:** الكمية السالبة، وفقدان خصم المخزون عند التزامن، وانهيار `variation = null`، وقراءة صف سلّة غير مقيّدة بالمالك، وانهيار 500 عند تعطيل منتج داخل السلّة.

**بند واحد مؤجَّل عمداً وبتبرير مكتوب:** لفّ إنشاء الطلب في معاملة مع أقفال صفوف. الخصم صار ذرّياً فتوقّف الحساب الخاطئ، لكن لا شيء يمنع المخزون من النزول تحت الصفر. لا أستطيع تمرير عملية شراء حقيقية هنا (لا توجد بيانات اعتماد بوّابات)، و**شحن إعادة كتابة غير مُختبَرة لأهم مسار في التطبيق أخطر من السباق نفسه**. التوصية: على staging، مع عملية شراء حقيقية قبل/بعد، ومعيار قبول = طلبان متزامنان على آخر قطعة.

### 🔁 بند 2.4: الاسترجاع — وقياسٌ غيّر الخطّة

قِستُ المتجر أولاً: **طلب واحد · 5 سلات · صفر مفضّلة · صفر تقييم · 16 منتجاً في 4 تصنيفات**. متجر قبل الإطلاق. وهذا غيّر إجابتين من الأربع:

| البند | ما فُعل |
|---|---|
| **السلة المتروكة** | **بُنيت كاملة** — لم تكن موجودة إطلاقاً (كلمة `abandon` غير موجودة في المشروع كلّه). خدمة اختيار + أمر + بريد + رابط موقَّع + نسب التحويل + صفحة إعدادات. **تُشحن مُطفأة**، وتذكير الزوّار مفتاح منفصل مُطفأ أيضاً. |
| **التقييمات** | **ثلاثة عيوب حقيقية أُصلحت** (تفصيلها في الجدول أعلاه) |
| **المفضّلة** | **فُحصت فوُجدت سليمة** — كل قراءة وكتابة مقيّدة بـ`customer_id`. لا تغيير. |
| **التوصيات** | **لم تُبنَ عمداً** — والسبب مكتوب لا مسكوت عنه |

**لماذا لم أبنِ محرّك توصيات:** التصفية التعاونية («من اشترى هذا اشترى أيضاً») تحتاج سجلّ طلبات — وعندك **طلب واحد**، فستُخرِج لا شيء. والترتيب حسب المحتوى يحتاج ما يُرتَّب — وأكبر تصنيف فيه **9 منتجات**، أي أن كتلة «منتجات ذات صلة» الحالية تعرض التصنيف كاملاً أصلاً. **بناء محرّك هنا تمثيل لا هندسة.** ما يجعله يستحق: كتالوج بالمئات وسجلّ طلبات حقيقي. سجّلتُه هكذا بدل تعليمه «مُنجَز»، لأن ميزة لا تستطيع العمل على بيانات المتجر ليست ميزة مكتملة.

**خطآن أمسكهما تصييرُ البريد فعلياً** (‏`Mail::fake()` لا يُصيّر القالب، فكل اختبارات الإرسال كانت ستمرّ مع قالب ينفجر): `company_web_logo` **مصفوفة لا نصّ** — ودمجها في مسار خطأ قاتل يُسقط الرسالة كلها (وهو خلل حيّ في أحد قوالب 6Valley نفسها) · و**العربية عندك مسجَّلة بالرمز `sy` لا `ar`**، فقائمة رموز مكتوبة يدوياً كانت ستُخرِج الرسالة العربية **من اليسار لليمين**. الاتجاه الآن يُقرأ من إعداد اللغات نفسه.

**التالي:** 2.5 عمليات الكتالوج ← 2.6 الأداء (خط الأصول: ~5MB لكل صفحة و20 ورقة أنماط حاجبة و1.4MB لحزمة Firebase).

**مؤجَّل بطلبك:** رفع صور المنتجات (يحتاج أرشيف `public.zip` المرافق للنسخة).

> **الإصدارات:** التفاصيل الكاملة لكل إصدار في [`CHANGELOG.md`](CHANGELOG.md).

---

## 1. الهوية الحقيقية للمشروع

رغم اسم المجلد ووجهة الترخيص، **لا يوجد منطق صيدلاني إطلاقاً**:

- البحث عن `prescription | pharmacy | medicine | drug | dosage` في `app/` و`resources/` و`database/` يعطي نتيجتين فقط: رابط التذييل، وإعداد ضريبي عام من 6amTech لقطاعات أخرى.
- الإشارات «الصيدلانية» في CSS ليست إلا أسماء أيقونات خطوط (`fi-rr-prescription`) في مكتبات طرف ثالث.
- `.env.example` ما زال يحمل `DB_DATABASE=6valley` و`CONTAINER_NAME_PREFIX=6valley-demo`، ولوحتا الأدمن والبائع تعرضان بانر «اشترِ 6Valley الأصلي» في وضع العرض.
- التخصيص الحقيقي الوحيد ذهب إلى **المزادات** (Auction)، لا الصيدلية.

**ملاحظة تخصّ الكويت:** المشروع مُرخّص وموسوم لصيدلية **سورية** (`pharmacysyria.com`، وأكمل لغة عربية هي لهجة `sy`). إن كان العميل في الكويت كياناً مختلفاً فالترخيص والتذييل و`robots.txt` تحتاج إعادة عمل، وقد لا يغطي الترخيص النطاق الجديد. كما أن `GlobalConstant.php` يربط `"kw" ⇒ "Cornish"` (الكورنية) خطأً؛ الرمز الصحيح للعربية الكويتية هو `ar-KW`.

---

## 2. 🔒 الطوارئ الأمنية

جميع النتائج التالية تأكّدت بالفحص المباشر للملفات. رُتّبت حسب الخطورة.

### حرج

- **باب خلفي RCE في ملف الدخول الجذري** — `index.php:1-9` يُطفئ الإبلاغ عن الأخطاء ثم ينفّذ عبر `eval()` شيفرة PHP تُجلب من موقع لصق خارجي (`hxxps://pastefy[.]app/…/raw`) عند كل طلب. الملف مُتتبَّع في Git؛ والنسخة النظيفة في `public/index.php`. هذا هو الملف الفعّال في هذا النشر (`DOMAIN_POINTED_DIRECTORY = 'root'`).
- **مفتاح Apple خاص منشور علناً** — `public/assets/back-end/Apple-AuthKey.p8` (ونسخة `.txt`) مُتتبَّع ويبدأ بـ `-----BEGIN PRIVATE KEY-----`، وقابل للتنزيل مباشرة من الويب. **يُدوَّر فوراً** في حساب Apple ثم يُطهَّر من التاريخ.
- **SSRF غير مصادَق** — `routes/web/routes.php:57` مسار `GET /image-proxy?url=` يجلب أي عنوان ويعيد جسم الاستجابة كاملاً مع `Access-Control-Allow-Origin: *`، دون قائمة سماح أو حجب للعناوين الداخلية (يتيح قراءة بيانات اعتماد السحابة).
- **رفع ملفات عشوائي غير مصادَق (احتمال RCE)** — `RestAPI/v2/seller/ProductController.php:68` (دالة `upload_images`) بلا مصادقة وتحقّقها `'image' ⇒ 'required'` فقط، و`Utils/ImageManager.php:19` يكتب البايتات الخام بامتداد العميل قبل إعادة الترميز.

### عالٍ

- **مفتاح ترخيص 6valley + `APP_KEY` حقيقي في المستودع** — `config/system-addons.php` (مفتاح شراء `b8eb2cc8-…` واسم مستخدم مكشوفان) و`.env.example:3` (مفتاح تطبيق حقيقي مشترك بين كل تثبيت ينسخه).
- **وسيط الضيف في API لا يصادق** — `app/Http/Middleware/APIGuestMiddleware.php:15` يفحص وجود ترويسة `Authorization` فقط، يقبل أي `guest_id`، ومسار الرفض يعيد حالة **200** بدل 401.
- **توكنات بائع/مندوب نصّية دائمة** — `SellerApiAuthMiddleware.php:22` و`DeliveryManAuth.php:20`: عمود `auth_token` نصّي، الفحص الوحيد `strlen > 30`، بلا تجزئة/انتهاء/تدوير/إبطال. (المشروع يعتمد أصلاً Sanctum غير المستخدم.)
- **تسريب PII للعتاد في جذر الويب** — `mySpecs.html` ليس وثيقة مواصفات بل تفريغ `lshw` لعتاد لابتوب مطوّر، يسرّب أرقاماً تسلسلية وUUID وعناوين MAC وعنوان LAN داخلياً. **يُحذف ويُطهَّر من التاريخ.**
- **تعداد منتجات بائع غير مصادَق (IDOR)** — `routes/rest_api/v3/seller.php:286-291` مساران خارج مجموعة `seller_api_auth` يأخذان `seller_id` من العنوان دون فحص ملكية.
- **غياب تحديد المعدّل على الدخول/OTP** — الحدّ العام 3000/دقيقة والحدّ المسمّى غير مرتبط بأي مسار.

### متوسط

- `shell_exec('php ../artisan passport:install')` من مسار أدمن (`EnvironmentSettingsController:94`).
- تعطيل تحقّق TLS في مسار دفع Paytm و`FileManagerLogic`.
- إعفاءات CSRF واسعة تشمل `/customer/choose-shipping-address` (فعل مصادَق حسّاس لا نداء دفع).
- `$guarded = []` على `PaymentRequest` و`DeliverymanWallet` و`ShippingAddress` (مال/PII).

### مؤشرات اختراق سابق

- `robots.txt`: 60 سطر `Sitemap:` سبام نحو `pharmacysyria.com`، و`sitemap.xml` المحلي فارغ (0 بايت).
- `.gitignore` ينتهي بقسم يدوي «Suspicious or temporary local files» يتجاهل `ss.php` و`ziURk0Vz` — أي أن أحداً وجد شِلّات هنا سابقاً.
- تاريخ Git التزام واحد مضغوط (`afc766f`) — لا يمكن تتبّع متى دخل الباب الخلفي.

> **الاستجابة الموصى بها:** عامل المضيف على أنه مخترق: أزل الباب الخلفي، افحص `storage/` و`bootstrap/cache/` وكل `public/*.php` بحثاً عن شِلّات أخرى، دوّر `APP_KEY` واعتمادات قاعدة البيانات وكل مفاتيح بوابات الدفع ومفتاح الشراء ومفتاح Apple، ونظّف سبام `robots.txt`. **لا تنشر من هذه الشجرة قبل ذلك.**

---

## 3. البنية المعمارية

Laravel 12 بنمط تمهيد حديث (`bootstrap/app.php`، لا `Http/Kernel.php`)، لكن تسجيل المسارات بالنمط القديم `RouteServiceProvider::map()` — تناقض بنيوي جوهري. التدفّق المُوثّق **Controller → Service → Repository → Model** موجود لكنه مُطبّق بتفاوت (~34% من الـ Controllers تتجاوز طبقة Repository).

| الطبقة | العدد |
|---|---|
| Controllers | 254 (أدمن 100 · API 68 · بائع 34 · ويب 18) |
| Services | 75 |
| Repositories | 94 (من 95 واجهة) |
| Models | 112 |

**«الأصناف الإلهية» — أكبر الملفات** (منطق الأعمال متمركز في مدراء ثابتين مُحمّلين عالمياً، غير قابلين للاختبار):

| الملف | الأسطر |
|---|---|
| `Utils/ProductManager.php` | 2,834 |
| `Utils/OrderManager.php` | 2,515 |
| `RestAPI/v3/seller/ProductController.php` | 2,114 |
| `Web/WebController.php` | 1,618 |
| `Traits/UpdateClass.php` | 1,562 |
| `Enums/GlobalConstant.php` | 1,302 |

**ديون بارزة:** تكرار الثوابت بين `Constant.php` و`GlobalConstant.php` (~1,100 مدخل)؛ توائم نسخ-لصق في تقارير الأدمن/البائع (~2,400 سطر)؛ 15 كتلة `catch(Exception){}` فارغة تبتلع الأخطاء بصمت (أبرزها `AppServiceProvider::boot()` كاملة)؛ `ini_set('memory_limit', -1)` على مستوى العملية.

---

## 4. الوحدات والإضافات

`modules_statuses.json` يفعّل خمس وحدات، لكن ثلاثاً فقط موجودة على القرص.

| الوحدة | الحالة | الدور |
|---|---|---|
| `AI` | موجودة | توليد محتوى LLM (لا محادثة). مزوّد OpenAI فقط يعمل؛ `ClaudeProvider` قالب فارغ. |
| `Blog` | موجودة | مدونة بفئات وترجمات وSEO. |
| `TaxModule` | موجودة | محرّك ضريبة/VAT — أعمق وحدة تكاملاً (~30 ملفاً أساسياً يعتمد عليها). |
| `Auction` | **مفقودة** | الميزة النشطة الفعلية: 548 ملفاً و195 قالباً يشيران إليها — لكن باكندها غائب. |
| `Gateways` | **مفقودة** | ~40 بوّابة دفع إضافية؛ يعوّضها fallback داخلي بـ 13 بوّابة. |

الوحدتان المفقودتان إضافتان مدفوعتان تُحذفان آلياً عند بناء نسخة التوزيع. التطبيق **لا ينهار** بفضل تصميم دفاعي متعدّد الطبقات، لكن تبقى نقاط كسر غير محروسة تُنتج خطأ 500 — أخطرها `Modules/AI/.../Auction/AuctionResourceService.php:25` (مرجع صلب لـ `ItemCondition::ALL` بلا حارس على مسار حيّ).

---

## 5. المسارات وواجهات API

~1,382 مساراً عبر 6 ملفات ضخمة أحادية (ملف الأدمن وحده 1,223 سطراً / 565 مساراً). «الإصدارات» الثلاثة ليست تطويراً لواجهة واحدة بل **ثلاثة منتجات مختلفة**:

| الإصدار | المنتج | مسارات | المصادقة |
|---|---|---|---|
| v1 | تطبيق العميل + الواجهة | 162 | Passport + وضع ضيف |
| v2 | تطبيق البائع القديم + المندوب | 96 | توكن يدوي (بلا وسيط على المجموعة) |
| v3 | تطبيق البائع الحالي | 147 | وسيط `seller_api_auth` |

- **v2 و v3 شبه متطابقين** — كل إصلاح للبائع يجب أن يهبط مرتين، وعملياً لم يحدث (من هنا فجوة المصادقة في v2).
- **Passport مستخدم فعلاً؛ Sanctum اعتماد ميّت.** توكنات Passport بلا مدة صلاحية مضبوطة.
- **موارد API (Resources) بنية ميّتة**: صفر استخدام في 68 متحكّم — كل استجابة تُسلسِل نموذج Eloquent خاماً، فأي عمود جديد يتسرّب للعملاء تلقائياً.

---

## 6. الواجهة الأمامية والثيمات

التطبيق **100% Blade + jQuery** — لا Vue (رغم أن `CLAUDE.md` يصفه «Vue 2»؛ بحث عن `v-model|v-for|new Vue` = صفر).

- **ثيمان:** `default` نشط، `theme_aster` (26M / 246 قالباً) خامل غير قابل للعرض.
- **بناء Mix معطّل:** `resources/sass/app.scss` غير موجود ⇒ `npm run prod` يفشل، ومخرجاته غير مرجَّعة من أي قالب.
- **`public/` بحجم 125M** فيها ~30M تكرار (ثلاث أشجار أصول أدمن متوازية تُحمَّل معاً).
- **أداء `translate()` كارثي:** 21,352 نداءً، كلٌّ يعيد بناء مصفوفتين ضخمتين، مع كتابة لملفات المصدر وقت التشغيل.
- **التدويل/RTL:** لغات `en · sa · sy · bd · in · es` — **لا kw**. لوحة الأدمن v2 غير مترجمة عربياً (381 مفتاحاً بقيم إنجليزية). RTL يدوي عبر ~262 شرطاً ثلاثياً، لا ورقة أنماط RTL للمتجر.

---

## 7. قاعدة البيانات والتشغيل

الجداول الأساسية **لا تُنشأ عبر الهجرات** بل توجد فقط داخل نسخة SQL؛ والهجرات الـ 300 غالبها تعديلات فوقها.

> **المستودع وحده لا يُشغّل المشروع:** `installation/backup/database.sql` و`public.zip` و`demo/database.sql` مستبعدة بـ `.gitignore`. النسخة يجب الحصول عليها خارج النطاق من حزمة إصدار 6amTech. تعليمة `CLAUDE.md` بالاستيراد أولاً غير قابلة للتنفيذ من نسخة نظيفة.

- **اتجاه التطوير الحديث (2025→2026):** دقّة الأموال (`decimal(40,20)`)، تعديل الطلبات، وفهرسة أداء.
- **فجوات التشغيل:** لا مهام مجدولة إطلاقاً (`Kernel::schedule()` فارغة)؛ صنف Job واحد؛ 23 من 24 مستمعاً تزامنيون داخل الطلب؛ `forceImportSQL()` و`database:refresh` ينفّذان `db:wipe` (فقدان بيانات كامل).
- بوابات الدفع مُهيّأة عبر قاعدة البيانات (`addon_settings`/`business_settings`)، لا `.env`.

---

## 8. الاختبارات والتوثيق

- **تغطية اختبار = صفر عملياً:** قالبا `ExampleTest` فقط؛ Playwright مُعلَن لكن `playwright.config.ts` والاختبارات مستبعدة بـ `.gitignore`.
- **انحراف التوثيق:** `CLAUDE.md` يستورد `.claude/rules/*.md` (المجلد غير موجود)، يُدرج `Modules/Auction` كموجود، ويصف `file:permission` خطأً (يجعل `sitemap.xml`/`robots.txt` بصلاحية 0777 ولا يلمس `storage/`).

---

## 9. مصفوفة النتائج

| الخطورة | النتيجة | المجال | الموقع |
|---|---|---|---|
| حرج | باب خلفي RCE في ملف الدخول | أمن | `index.php:1` |
| حرج | مفتاح Apple خاص منشور علناً | أمن | `public/assets/back-end/*.p8` |
| حرج | SSRF عبر بروكسي الصور | API | `routes/web/routes.php:57` |
| حرج | رفع ملفات عشوائي غير مصادَق | API | `v2/seller/ProductController:68` |
| عالٍ | مفتاح ترخيص + APP_KEY مسرّبان | أمن | `system-addons.php` · `.env.example` |
| عالٍ | وسيط الضيف لا يصادق ويعيد 200 | API | `APIGuestMiddleware:15` |
| عالٍ | توكنات نصّية دائمة بلا انتهاء | API | `SellerApiAuthMiddleware:22` |
| عالٍ | تسريب PII للعتاد في جذر الويب | أمن | `mySpecs.html` |
| عالٍ | المستودع لا يُشغّل قاعدة بيانات | تشغيل | `installation/` · `*.sql` |
| عالٍ | وحدتا Auction/Gateways مفقودتان | وحدات | `module-helper.php` |
| متوسط | لا مهام مجدولة؛ مستمعون تزامنيون | تشغيل | `Console/Kernel.php` |
| متوسط | أداء `translate()`: 21,352 نداء | أداء | `Utils/language.php:21` |
| متوسط | بناء Mix معطّل + مخرجات ميّتة | واجهة | `webpack.mix.js` |
| منخفض | شيفرة ميّتة (`test.php`, `routes.json`, أصداف v1) | نظافة | متعدّد |

---

## 10. خطة التطوير المقترحة

مرتّبة بحيث لا يبدأ العمل الوظيفي قبل تأمين الأساس.

**المرحلة 0 — تأمين الطوارئ (قبل أي شيء):**
- إزالة الباب الخلفي من `index.php` وفحص المضيف والتعامل معه كمخترق.
- تدوير كل الأسرار (`APP_KEY`، قاعدة البيانات، مفاتيح الدفع، مفتاح الشراء، مفتاح Apple).
- إزالة `Apple-AuthKey.p8` و`mySpecs.html` ومفتاح الترخيص من الشجرة والتاريخ، وتنظيف `robots.txt`.
- سدّ الثغرات الحرجة (`/image-proxy`، رفع v2، `APIGuestMiddleware`، تحديد المعدّل على الدخول/OTP).

**المرحلة 1 — تثبيت الصحّة والاتساق:**
- توثيق طريقة الحصول على `database.sql` وتشغيل بيئة محلية قابلة للإقلاع.
- حسم مصير Auction/Gateways (حراسة `class_exists` وضبط `modules_statuses.json`، أو الاستعادة).
- تصحيح `CLAUDE.md` وإنشاء/إزالة `.claude/rules/*.md`؛ إصلاح/حذف بناء Mix؛ حذف الشيفرة الميّتة.

**المرحلة 2 — الأداء وشبكة الأمان:**
- تخزين `translate()` و`$web_config` وخريطة ربط الـ Repository مؤقتاً؛ إصلاح N+1 في ترويسة الثيم.
- اختبارات توصيف حول توليد الطلب والدفع؛ تفعيل الطابور والجدولة.

**المرحلة 3 — خفض الديون التقنية:**
- تفكيك `OrderManager`/`ProductManager` إلى خدمات قابلة للحقن (بعد اختبارات التوصيف).
- توحيد توائم التقارير؛ دمج الثوابت؛ نقل التحقق إلى `FormRequest`؛ استبدال RTL الشرطي بورقة أنماط.

**المرحلة 4 — بناء الصيدلية فعلاً (عمل جديد بالكامل):**
- نطاق الوصفات (رفع عند الدفع، تحقّق صيدلي، حالات موافقة/رفض).
- خصائص الدواء (الاسم العلمي، الشكل، الجرعة، الدفعة والصلاحية، تصنيف الأدوية المراقَبة).
- توطين الكويت (`ar-KW`، عملة KWD، بوابات محلية، تصحيح فخّ `kw ⇒ Cornish`)، وضوابط تنظيمية وسجلّ تدقيق.
